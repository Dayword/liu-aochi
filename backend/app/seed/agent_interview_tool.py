"""AI Agent 岗位面试引导学习 · 分类：Function Calling 与工具设计

===========================================================================
【字段契约】—— 所有 agent_interview_*.py 必须严格照此结构，一个字段都不能少
===========================================================================

QUESTIONS: list[dict]，每项字段如下（顺序也保持一致）：

    code            str   「ia-<分类键>-<两位序号>」，如 "ia-llm-01"、"ia-rag-08"
    stage           str   分类名，9 类之一，与本文件文件头注释里的分类名完全一致
    title           str   题目本身。**用面试官会说的原话**，不要改写成知识点名。
                          反例："幻觉的成因" ← 这是知识点名
                          正例："大模型为什么会产生幻觉？" ← 这是面试官原话
    summary         str   一句话概要（15 字以内），列表里显示
    definition      str   考察点。以 "## 面试官在考什么" 开头，说清这题背后想验证什么能力，
                          以及答到哪一层算合格、哪一层算加分。markdown。
    plain           str   大白话拆解。假设学生完全没接触过，用比喻/类比讲明白原理。
                          要能让一个本科生看懂。markdown。
    example         str   满分回答。**用口语写，像真的在面试现场说出来的一段话**，
                          不是书面提纲。要有结构（结论先行 → 分点 → 例子），
                          长度 200-400 字，学生可以照着背。markdown。
    pitfalls        list[str]  4 条。**"这么说会减分"**，不是"注意点"。
                          要写清「错误的说法是什么 + 为什么错 + 应该怎么说」。
    followups       list[dict] 2-3 条追问链，每条 {"q": 追问原话, "a": 怎么接}。
                          追问要到第二三层，浅问不算。
    answer_keywords list[str]  6-10 个踩分词。AI 评分与"你漏了什么"的反馈都用它。
                          必须是学生答案里能自然出现的**术语**，不要放整句。
    answer_min_score int       过关阈值，默认 70。（个别极难题可放宽到 65）
    answer_hint     str        学生卡住时的一句话提示（不直接给答案，给思考方向）。

【质量红线】
1. title 必须是面试官原话，读起来像在问你，不像目录标题。
2. example 必须是**口语化的完整回答**，能直接背；禁止写成"1. 2. 3."的要点清单。
3. pitfalls 写完自己读一遍：这条真的会让人减分吗？还是有它也行？
   只有"真有杀伤力"的才留下。
4. followups 必须是真追问。像"能详细说说吗"这种无效追问不要。
5. 全部中文。技术术语保留英文原词（如 tool_calls、schema、MCP）。
6. 内容要针对 **AI Agent 岗**，不要写成通用后端/算法面试题。
"""

QUESTIONS: list[dict] = [
    {
        "code": "ia-tool-01",
        "stage": "Function Calling 与工具设计",
        "title": "Function Calling 的完整流程能讲一遍吗？从模型返回 tool_calls 到最后你把结果回填，中间都发生了什么？",
        "summary": "工具调用闭环全流程",
        "definition": (
            "## 面试官在考什么\n\n"
            "这是工具类问题的**地基题**，这条链路讲不清，后面的设计题全是空中楼阁。"
            "面试官想验证三件事：\n\n"
            "1. **你是否真的手写过这条链路**，还是只会调框架的现成 agent。"
            "能顺口说出 tool_calls 里带 id、arguments 是 JSON 字符串这种细节，"
            "说明你真调试过、踩过坑。\n"
            "2. **你是否理解「模型不执行任何东西」**。它只输出一个结构化的调用意图，"
            "真正的执行发生在你的代码里。这一点说反了，直接判外行。\n"
            "3. **你是否知道这是个多轮循环**，而不是一次问答。\n\n"
            "合格线：说清「模型输出意图 → 你的代码执行 → 结果以 tool 角色回填 →"
            "再请求一次模型」这个闭环。\n"
            "加分项：主动讲到 tool_call_id 的对应关系、循环的终止条件、"
            "以及一次返回多个 tool_calls 时怎么处理。"
        ),
        "plain": (
            "先建立一个关键认知：**模型没有手脚**。它不能查数据库、不能下单，"
            "它唯一会做的事是「吐出文字」。\n\n"
            "那工具调用是怎么发生的？想象你请了一个只会说话的顾问，"
            "你递给他一张菜单，上面写着：「如果你需要查订单，就说一句暗号，"
            "并且告诉我订单号」。\n\n"
            "- 顾问看完问题，说：「请帮我查订单，订单号是 123」——"
            "这就是 **tool_calls**，一坨结构化的话，不是行动。\n"
            "- 你（也就是你的代码）听到暗号，真的去查了，拿到结果。\n"
            "- 你把结果写在纸条上递回去：「查询结果：已发货，预计明天到」——"
            "这就是 **tool 角色的消息回填**。\n"
            "- 顾问拿到纸条，才能对用户说人话：「您的包裹已发货」。\n\n"
            "两个关键点：一，**执行永远是你的代码干的**；二，**这是一来一回的循环**，"
            "模型不看到执行结果就不会收尾。\n\n"
            "还有个容易踩的细节：模型可能一次递给你三张暗号纸条（多个 tool_calls），"
            "你得每张都执行、每张都回填，并且用 id 一一对应，不然它会串。"
        ),
        "example": (
            "我按一次真实调用链路说。第一次请求时，我把工具列表以 schema 的形式"
            "一起传给模型，每个工具声明名字、description 和参数结构。"
            "如果模型判断需要调工具，它返回的 message 里就不是普通文本，"
            "而是一个 tool_calls 数组，每一项有 id、function name 和 arguments，"
            "**arguments 是个 JSON 字符串不是对象，得自己解析**。\n\n"
            "到这里模型的工作就结束了——它没有执行任何东西，执行是我这边的代码干的。"
            "我按 name 找到本地注册的函数，反序列化参数，做一层校验，"
            "然后真正去调接口或者查库。\n\n"
            "拿到结果后，我把结果包成一条 role 为 tool、带 tool_call_id 的消息，"
            "追加进对话历史，id 必须和上面那个对上，否则模型不知道这是哪次调用的返回。"
            "然后我拿更新后的历史**再请求一次模型**，这时它才基于真实结果生成给用户的回答。\n\n"
            "如果它又返回了新的 tool_calls，那就再来一轮。所以本质是个 while 循环，"
            "我会设最大轮数，比如 5 到 10 轮，防止它来回打转把 token 烧光。"
        ),
        "pitfalls": [
            "**说「模型会去调用接口」**——这是最致命的口误。模型只会输出 tool_calls "
            "这个结构化意图，真正发请求的是你的代码。正确说法：模型负责「决定调什么」，"
            "runtime 负责「真的去调」，这两件事必须分清。",
            "**说「把工具结果拼成一段文本塞回 prompt」**——把工具结果和普通对话混在一起，"
            "会破坏消息的角色边界，多个并行调用时也无法区分谁是谁。"
            "应该用 role 为 tool 的独立消息，并带上 tool_call_id 精确对应。",
            "**不提循环的终止条件**——只说「一直循环到它不调工具为止」。"
            "真实系统必须设最大轮数或超时预算，否则遇到模型反复调同一个工具，"
            "token 会被烧穿，请求也永远不返回。",
            "**把 arguments 当 dict 直接用**——多数 API 返回的 arguments 是 JSON 字符串，"
            "直接取键会崩。要显式反序列化，并且捕获解析失败的情况，"
            "别让一个格式错误把整个链路打断。",
        ],
        "followups": [
            {
                "q": "如果模型给的 arguments 反序列化出来不是合法 JSON，你怎么办？",
                "a": "不能吞掉，也不能直接抛异常给用户。我的做法是把它当成一次失败的"
                     "工具结果回填：明确告诉模型「你给的参数不是合法 JSON，"
                     "错误在第几位，请重新生成」。同时记日志。"
                     "重试要有上限，连续失败两三次就停止这一轮工具调用，"
                     "降级成自然语言回答或者转人工，绝不无限重试。",
            },
            {
                "q": "模型编了一个你根本没注册过的工具名，会发生什么？",
                "a": "分两种情况。如果模型是走原生 function calling，工具名通常会被约束在"
                     "你给的列表里，基本编不出来，因为解码时会做 schema 约束。"
                     "但如果是让模型输出 JSON 文本再自己解析，它就会编。"
                     "所以我会在查表前先做一次白名单校验，不认识的名字直接拒绝，"
                     "回填一条「不存在这个工具，可用工具是……」，"
                     "而不是让它继续瞎猜。",
            },
            {
                "q": "多轮循环里消息会越堆越长，你怎么控制上下文？",
                "a": "三个手段叠加。一，工具返回的大结果不原样塞回去，"
                     "只在上下文里留摘要或者关键字段，完整数据放在外部存储按 id 引用。"
                     "二，历史做滑动窗口，只保留最近若干轮，更早的用摘要压缩。"
                     "三，把工具结果里重复的字段去掉，很多时候模型只需要看结论。"
                     "最后会给整个循环设一个总 token 预算，超了就强制收尾。",
            },
        ],
        "answer_keywords": [
            "tool_calls", "arguments", "JSON", "schema", "role", "tool_call_id",
            "回填", "循环", "校验", "最大轮数",
        ],
        "answer_min_score": 70,
        "answer_hint": (
            "先记住一句话：模型只负责「说它想调什么」，真正动手的是你的代码。"
            "然后顺着两个问题往下捋——谁执行？执行结果怎么还给模型？"
        ),
    },
    {
        "code": "ia-tool-02",
        "stage": "Function Calling 与工具设计",
        "title": "工具一多模型就选错，你觉得工具的名字和 description 到底该怎么写？",
        "summary": "工具命名与描述设计",
        "definition": (
            "## 面试官在考什么\n\n"
            "这题表面是「写文档」，实际考的是**你有没有把 prompt 工程落到 schema 层面**。"
            "面试官想验证：\n\n"
            "1. 你是否知道 **description 就是给模型的 prompt**，它的写法直接决定选对率。\n"
            "2. 你是否理解**歧义才是选错的头号原因**——两个职责重叠的工具并列，"
            "模型一定会摇摆。\n"
            "3. 你有没有**可量化**的意识。能说「我拿一批真实 query 跑选对率」的人，"
            "比只会讲玄学的人高一个层次。\n\n"
            "合格线：说出命名要动宾明确、description 要写清什么时候用和什么时候不用、"
            "参数要写清含义和取值。\n"
            "加分项：能举一个「两个工具边界怎么划」的真实例子，"
            "并知道枚举（enum）比自由文本更能防填错。"
        ),
        "plain": (
            "把模型当成一个**新来的实习生**：他懂技术，但从没接触过你的系统。"
            "你给他的不是代码，而是一张写着工具名和一句话说明的卡片。\n\n"
            "现在想：实习生为什么会点错菜？\n\n"
            "- **卡片名字起得太虚**。叫 `process` 的工具，谁知道是处理啥的。\n"
            "- **两张卡片长得太像**。`search_user` 和 `query_user` 并列摆着，只能靠猜。\n"
            "- **说明只写「查询用户」，没写什么时候不该用**。他可能拿它去查订单。\n"
            "- **参数没写清**。`status` 到底传 1 还是 active？不写他只能编。\n\n"
            "所以写工具描述的目标不是「写得准确」，而是**写得不歧义**："
            "动词开头、边界说清、可选值写死。这跟写给人看的 API 文档刚好相反——"
            "人看不懂会来问你，模型看不懂就直接瞎猜，而且猜错了也不会举手。"
        ),
        "example": (
            "我的经验是：**工具描述本质上就是 prompt，它跟写给人看的文档目标不一样，"
            "人要的是准确，模型要的是不歧义**。我一般按四点来定。\n\n"
            "第一，命名用动词开头的 snake_case，并且带上操作对象，"
            "像 get_order_status，不要叫 order 或者 process。名字本身是个强信号，"
            "模型犹豫时会靠它判断。\n\n"
            "第二，description 一定回答三个问题：干什么、什么时候该用、"
            "**什么时候不该用**。最后这条最容易被忽略，但对功能相近的工具特别有用，"
            "我会写「查用户资料用这个，查订单请用 get_order_status」。\n\n"
            "第三，参数要写清语义和取值。能枚举的把 enum 列全，别让它自由生成；"
            "每个参数字段也要有 description，比如 status 只能传 pending、paid、shipped。\n\n"
            "第四也是最关键的：**把重叠的工具合并或者划清边界**。如果两个工具老是选错，"
            "往往不是描述问题，是它们本来就不该共存，该合成一个带 type 参数的工具。\n\n"
            "最后我会拿一批真实 query 跑一遍选对率，改完描述再跑，用数据看有没有变好，"
            "而不是凭感觉说好像准了。"
        ),
        "pitfalls": [
            "**认为描述越长越全越好**——堆一堆背景说明会把关键信息淹没，"
            "模型反而抓不住重点。应该结论先行，把「何时用、何时不用」放最前面，"
            "补充细节放后面。",
            "**在系统 prompt 里加一句「注意区分 A 和 B」就完事**——这是治标不治本。"
            "根因通常是两个工具职责重叠，正确做法是合并或改 schema，"
            "让这个错误在结构上不可能发生，而不是靠模型自觉。",
            "**参数不给 enum、不写字段说明**——然后抱怨模型编值。"
            "能枚举的就把可选值写死，把校验放在结构层而不是指望它猜对你的取值范围。",
            "**说「描述随便写，模型很聪明」**——这是最外行的一句。"
            "模型只能依据你给的字做判断，工具选错很大一部分就是描述歧义造成的，"
            "你没写清它就只能猜，而它猜错了也不会来问你。",
        ],
        "followups": [
            {
                "q": "如果两个工具确实功能有区别，但模型就是分不清，你会怎么改？",
                "a": "我会先看是不是命名或者描述里用了同一个词，"
                     "比如一个叫 refund 一个叫 cancel，中文描述都写了「取消」，那肯定乱。"
                     "常见做法是合并成一个工具，加一个显式的 action 参数，"
                     "把「二选一」这个判断从模型脑子里挪到参数枚举里——"
                     "枚举是有限集合，选错的概率低得多。"
                     "如果业务上真的不能合并，就在描述里加正反例，"
                     "写清「A 场景用我，B 场景别用我」。",
            },
            {
                "q": "你怎么衡量工具描述改得好不好？",
                "a": "我会建一个小的评测集，从真实日志里抽一两百条 query，"
                     "标注出每条正确答案应该调哪个工具、参数应该是什么。"
                     "然后跑一遍，看两个指标：**工具选对率**和**参数填对率**。"
                     "每次改描述或者 schema 都回归跑一遍，用数字说话。"
                     "只看「感觉变准了」不算数，因为模型行为对措辞很敏感，"
                     "很可能改好一个场景、弄坏另一个。",
            },
        ],
        "answer_keywords": [
            "description", "prompt", "歧义", "snake_case", "动宾", "enum",
            "取值", "边界", "选对率", "合并",
        ],
        "answer_min_score": 70,
        "answer_hint": (
            "换个身份想：如果这张工具卡片是给一个从没接触过你系统的实习生看的，"
            "他能不能只看卡片就选对？选不对的话，卡片缺了什么信息？"
        ),
    },
    {
        "code": "ia-tool-03",
        "stage": "Function Calling 与工具设计",
        "title": "我们要接几十上百个工具，全塞进上下文模型就晕了，你会怎么组织这些工具？",
        "summary": "大规模工具的检索与路由",
        "definition": (
            "## 面试官在考什么\n\n"
            "这题考三层：**有没有成本的意识、有没有分级的手段、有没有兜底的边界感**。\n\n"
            "1. **成本意识**：知道全量注入不是免费的吗？工具 schema 每轮都要重复计费，"
            "延迟也会叠加。\n"
            "2. **手段**：只会说「用 embedding 检索工具」是单层答案，"
            "能说出路由缩域、检索缩量、分层暴露的组合才算有体系。\n"
            "3. **边界感**：路由和召回都可能选错，选错了对的工具根本没进候选，"
            "模型再聪明也无米下炊。\n\n"
            "合格线：说清「不该全量注入」这个判断，并给出至少两种组织手段。\n"
            "加分项：主动讲兜底策略，并能指出「工具数量爆炸本质上是业务边界不清」，"
            "这是个明显的加分视角。"
        ),
        "plain": (
            "想象你去一家有 300 道菜的餐厅点菜。如果服务员把整本菜单从头念一遍，"
            "你要听十分钟，而且听完前三页已经忘了前面。现实中的做法是："
            "**先问你想吃中餐还是西餐，再给你那一本菜单**。\n\n"
            "工具也是一样的道理。全量注入有三个代价：\n\n"
            "- **贵**：每个工具的 schema 都要占 token，上百个工具光定义就上千 token，"
            "而且每一轮请求都要重付一次。\n"
            "- **晕**：候选太多，模型的注意力被稀释，选错率反而上升。\n"
            "- **慢**：输入越长，首字延迟越高，用户能直接感觉到。\n\n"
            "所以思路就是「先分类、再精选」：先按业务域分组（订单、用户、物流），"
            "用一个便宜的小模型或者规则判断用户想干什么，只把那一组的工具给它；"
            "组内还多的话，再做一次「工具检索」，把工具描述做成 embedding，"
            "按 query 召回最相关的五到十个。\n\n"
            "但要记住代价：**路走错了，对的工具压根不在候选里，模型再强也调不出来**。"
            "所以路由层必须留兜底。"
        ),
        "example": (
            "我不会把上百个工具一次性注入。先算笔账：每个工具的 schema 加描述大概几十到"
            "上百 token，一百个工具光定义就上万 token，每轮都要付这个钱，"
            "首字延迟也明显上去；更麻烦的是候选太多模型会挑花眼，选错率反而升高。"
            "所以我的原则是**每轮只给它当前真正可能用到的五到十个**。\n\n"
            "落地我一般做两层。第一层是**路由**：按业务域把工具分组，"
            "比如订单、用户、物流、售后，先用一个便宜的小模型或者关键词把 query 分类到"
            "某一组，只注入这一组的工具。第二层是**检索**：如果组内工具还是很多，"
            "就把工具的名字和 description 做成 embedding，按 query 语义召回 top-k，"
            "跟 RAG 是一个思路。\n\n"
            "还有个技巧是**分层暴露**：先只给几个粗粒度的入口工具，"
            "模型调用之后再动态展开下一层的具体工具，这样上下文里的工具数始终可控。\n\n"
            "最后我一定会补一句：**路由和召回都会选错，一旦对的工具没进候选，"
            "模型就是无米之炊**。所以要做兜底——给它一个「找不到合适工具」的出口，"
            "路由置信度低时放宽 top-k 甚至走全量，并把路由决策记进日志，方便排查迭代。"
        ),
        "pitfalls": [
            "**说「全塞进去就行，现在上下文很大」**——这是没算过账的表现。"
            "工具定义每轮都要重复付费，延迟还会叠加，而且候选过多会拉低选对率。"
            "应该主动把 token 和延迟这两笔成本摆出来。",
            "**只说「用 embedding 检索工具」**——单层答案。更完整的是路由缩域 +"
            "检索缩量 + 分层暴露三级组合，能让面试官看到你有体系而不是碰巧想到一招。",
            "**不讲兜底**——被追问「路由错了怎么办」就卡壳，说明你只想过顺利路径。"
            "必须有明确退路：置信度低时放宽候选、保留「无合适工具」的出口、"
            "或者反问用户澄清。",
            "**把它当纯技术优化，完全不碰业务**——工具数量爆炸很多时候是业务边界乱。"
            "能指出「与其让模型从 100 个里选，不如先跟业务把域拆清楚」，"
            "是明显高一层视角。",
        ],
        "followups": [
            {
                "q": "如果用户一句话跨了两个域，比如既要查订单又要改地址，你的路由器怎么处理？",
                "a": "单标签路由在这里就废了。我会让它允许多标签，"
                     "把命中域的工具都注入，或者并行跑两个子任务再汇总。"
                     "另外要保留一个通用的兜底入口，"
                     "当所有域的置信度都不高时，把候选放宽到跨域 top-k。"
                     "关键是别让路由变成一个硬开关，它应该是个可回退的软判断。",
            },
            {
                "q": "工具检索和文档 RAG 有什么不一样？",
                "a": "三个不一样。一，语料不一样，工具描述很短，信息密度高；"
                     "二，匹配的难点不一样，用户说「催一下」，工具叫 escalate_ticket，"
                     "这是词汇不匹配，光靠向量召回容易漏，我会加关键词和同义词做混合召回；"
                     "三，工具之间相似度很高，容易互相挤占名额，"
                     "所以召回时要刻意做去重和多样性控制，不能五个结果全是同一类工具。",
            },
        ],
        "answer_keywords": [
            "token", "路由", "分组", "检索", "embedding", "top-k",
            "分层", "召回", "兜底", "延迟",
        ],
        "answer_min_score": 70,
        "answer_hint": (
            "先算一笔成本账：每轮多带上百个工具定义，你要多付多少 token 和延迟？"
            "想明白这笔账，再想怎么才能「只给它当前可能用到的那几个」。"
        ),
    },
    {
        "code": "ia-tool-04",
        "stage": "Function Calling 与工具设计",
        "title": "模型一次返回好几个 tool_calls，你怎么判断哪些能并行、哪些必须串行？",
        "summary": "并行调用与依赖编排",
        "definition": (
            "## 面试官在考什么\n\n"
            "这题考的是**编排能力**，也是区分「会调 API」和「做过系统」的分水岭。"
            "三个层次：\n\n"
            "1. 是否知道原生 API 允许一次返回多个 tool_calls，并且这些调用可以并发执行。\n"
            "2. 是否理解**并行只在无依赖时成立**，一旦后一个调用的参数来自前一个的结果，"
            "就必须串行。\n"
            "3. 是否知道真实系统里的并行要配套并发上限、错误隔离、副作用幂等。\n\n"
            "合格线：能清楚区分「参数无依赖 → 并行」和「后一个吃前一个的结果 → 串行」。\n"
            "加分项：主动讲并发度上限、部分失败隔离、以及有副作用工具必须幂等"
            "（否则重试会变成重复下单）。"
        ),
        "plain": (
            "并行还是串行，其实就一句话：**后一个工具要吃前一个工具的输出吗？**\n\n"
            "- 不吃，就是并行。比如用户问「上海今天天气怎么样，顺便看下我的订单」，"
            "查天气和查订单毫无关系，两个请求一起发出去，总耗时取决于最慢的那个，"
            "比一个一个来快一倍。\n"
            "- 要吃，就必须串行。比如「把订单 A 的地址改成订单 B 的地址」，"
            "我得先查出 B 的地址，才能拿去改 A，这时候并行物理上就不可能。\n\n"
            "打个生活比方：早上出门，烧水（5 分钟）和穿衣服（3 分钟）可以同时干；"
            "但「先穿袜子再穿鞋」必须有序，因为鞋依赖袜子已经穿好。\n\n"
            "有个关键细节：**模型自己会一次说好几个动作，但它不保证这些动作的先后**。"
            "它完全可能把两个有依赖关系的调用一起返回。所以依赖判断这件事，"
            "往往要由你的编排层再兜一层：看后面那个调用的参数里有没有引用前面调用的结果，"
            "有就降级成串行。"
        ),
        "example": (
            "我分两种情况。**第一种是参数取值互相不依赖，就能并行。** 比如用户问"
            "「上海天气怎么样，另外帮我查下订单 123 到哪了」，这两个调用没有任何数据依赖，"
            "模型通常一次返回两个 tool_calls，我这边直接并发执行，"
            "整体耗时取决于最慢的那个，比串行快很多，用户感知的响应时间明显更短。\n\n"
            "**第二种是后一个调用的参数来自前一个的结果，就必须串行。** 比如"
            "「把订单 A 的收货地址改成订单 B 的地址」，地址得先从 B 查出来，"
            "才能拿去改 A，这是天然的数据依赖，只能一轮一轮来。\n\n"
            "实现上我有个 ToolExecutor，拿到 tool_calls 数组后先做一次依赖分析："
            "如果某个调用的 arguments 引用了前面调用的输出，就标成有依赖，只能顺序执行；"
            "剩下的无依赖调用走并发池，并发度我会设上限，比如 8，避免一下子打爆下游。\n\n"
            "还有两个工程细节我会注意：一是**部分失败要隔离**，"
            "一个工具超时不能让整批都失败，每个调用单独回填结果或者错误；"
            "二是**有副作用的工具必须幂等**，并行重试如果不幂等，可能变成下了两次单，"
            "这类工具我会标成不可重试、必须串行。"
        ),
        "pitfalls": [
            "**认为「模型一次返回多个就一定可以并行」**——不一定。"
            "模型可能把有依赖的调用一起返回，直接全并发会拿到错误的参数。"
            "应该先做依赖分析，检测到引用关系就降级为串行。",
            "**说「串行最安全，全都串行」**——安全但慢，会被认为没有优化意识。"
            "无依赖的场景该并行就并行，这是实打实的延迟收益，不做等于白丢分。",
            "**说「并行就是一把 gather 发出去」**——不提并发上限和下游承载能力，"
            "等于埋雷。要说清设并发度上限、加超时、必要时对下游做限流，"
            "否则一次并发几十个请求很容易把下游打挂。",
            "**失败就整批重来**——如果其中有写操作，重试会造成重复执行，"
            "可能重复下单、重复扣款。必须区分幂等和非幂等：非幂等的失败只能单点处理，"
            "绝不能整批重试。",
        ],
        "followups": [
            {
                "q": "如果两个调用都无依赖，但都要写数据库，你还敢并行吗？",
                "a": "写操作我会更谨慎。先看它们写的是不是同一张表、同一行，"
                     "如果有事务一致性要求，比如两个字段必须一起改，那就串行，"
                     "或者把逻辑收到服务端一个事务里做。"
                     "能并行的是读多写少、彼此无关的资源。"
                     "另外所有写操作都要带幂等键，这样即使因为重试被执行两次，"
                     "服务端也能去重，这才是敢并行的前提。",
            },
            {
                "q": "多个工具的结果一起回填时，顺序会影响模型理解吗？",
                "a": "影响可控。因为每条 tool 消息都带 tool_call_id，"
                     "模型是按 id 对应的，实际排列顺序影响不大。"
                     "不过我一般仍然按 tool_calls 的原始顺序回填，"
                     "减少不必要的不确定性，也方便出问题时对照日志。"
                     "真正需要注意的是别漏回填——少了一条，模型可能会以为那个工具没执行完，"
                     "然后重复调用。",
            },
        ],
        "answer_keywords": [
            "tool_calls", "并行", "串行", "依赖", "参数", "ToolExecutor",
            "并发", "幂等", "超时", "隔离",
        ],
        "answer_min_score": 70,
        "answer_hint": (
            "只要问一句就能判断：后面那个工具要用到前面那个工具的输出吗？"
            "要用就是串行，不用就可以并行。剩下的都是工程细节。"
        ),
    },
    {
        "code": "ia-tool-05",
        "stage": "Function Calling 与工具设计",
        "title": "工具执行失败或者超时了，你直接把报错扔回给模型，让它自己重试吗？",
        "summary": "工具失败的分类与重试",
        "definition": (
            "## 面试官在考什么\n\n"
            "这道题的重点不是「重试几次」，而是**你会不会给错误分类**。"
            "面试官想验证：\n\n"
            "1. 你是否知道错误要分三类：**参数错（模型能修）**、"
            "**系统错（模型修不了）**、**业务错（本来就不该重试）**。\n"
            "2. 你是否知道回填给模型的东西必须是**可行动的可读描述**，"
            "而不是一坨 traceback。\n"
            "3. 你是否知道重试要有界限，要区分幂等。\n\n"
            "合格线：能区分「值得让模型重试的错」和「重试也没用的错」，"
            "而不是把所有失败都塞回去。\n"
            "加分项：讲清重试次数上限、指数退避、幂等键，"
            "以及连续失败后的降级路径。"
        ),
        "plain": (
            "先问一个问题：**让模型重试，到底是在修什么？**\n\n"
            "把错误分成三类，你就明白了。\n\n"
            "- **第一类：模型能自己修的。** 比如参数类型错了、少了必填字段、"
            "值不在枚举里。这类错误本质上是它「填表填错了」，"
            "把错误原因告诉它，它有很大概率改对。**这类值得回填让它重试。**\n"
            "- **第二类：模型修不了的。** 比如下游服务 500、网络超时、"
            "数据库连接池满了。模型既不知道你机房的情况，也改不了任何参数，"
            "你回填一百次它也只能重新发一遍同样的请求。"
            "**这类要在你的代码里重试或者降级，别指望模型。**\n"
            "- **第三类：本来就不该重试的。** 比如「余额不足」「库存没了」「无权操作」。"
            "这不是错误，是**业务结果**，重试只会一直失败。"
            "**应该直接告诉模型这是结论，让它去跟用户解释。**\n\n"
            "把这三类混成一锅「工具执行失败」全扔给模型，是最常见的坑："
            "模型会开始瞎试参数，几轮之后 token 烧完了，问题一点没解决。"
        ),
        "example": (
            "不会无脑回填。我先给错误分类，因为**不同错误该由不同的角色来修**。\n\n"
            "第一类是**参数类错误**，比如类型不对、缺必填字段、值不在枚举里。"
            "这类是模型填错了表，把清晰的原因回填给它，它自己就能改，"
            "我会允许它重试一到两次。\n\n"
            "第二类是**系统类错误**，超时、503、连接失败。模型改不了任何参数，"
            "回填给它纯属浪费轮次。这类我在代码里处理：幂等的读操作做指数退避重试，"
            "最多两三次；写操作靠幂等键，避免重试变成重复下单；"
            "再不行就走降级，返回一个「暂时不可用」给用户。\n\n"
            "第三类是**业务类结果**，余额不足、库存没了、无权限。"
            "这根本不是错误，是业务的正常结论。我会把它当成成功的工具输出回填，"
            "内容写着「操作未成功，原因是库存不足」，让模型去解释给用户，"
            "而不是它自己在那反复重试。\n\n"
            "回填的内容也有讲究：**不能扔 traceback**，模型看不懂还白占 token；"
            "我会转成一句人能读懂的话，写清哪个参数错了、正确的范围是什么。\n\n"
            "最后是界限：每个工具的重试有上限，整个循环有最大轮数和超时预算，"
            "连续失败到阈值就停止，转成明确的失败话术或者转人工。"
        ),
        "pitfalls": [
            "**把 exception 直接转成字符串回填**——一坨 traceback 又长又难懂，"
            "模型只能瞎猜，还白占上下文。应该转写成结构化的、可行动的说明："
            "哪个参数错、期望什么、该怎么改。",
            "**所有错误都让模型重试**——系统类错误模型根本修不了，"
            "只是白烧 token 和延迟。要分三类：参数错让它改、系统错代码层重试、"
            "业务失败不该重试。",
            "**重试没有上限**——不设次数和超时预算会导致死循环。"
            "必须设最大轮数和总超时，超了就强制走降级路径，这是硬性要求。",
            "**把业务失败也当异常抛**——库存不足是正常业务结果，"
            "抛异常会让模型以为是系统故障，转头去试别的参数，"
            "反而不告诉用户真相。应该作为成功的工具结果返回，让它去解释。",
        ],
        "followups": [
            {
                "q": "如果模型连续三次调用同一个工具都失败，你会怎么处理？",
                "a": "说明它修不好这个错误，这时候要止损。我会把该工具从本轮候选里"
                     "移除或者标记为不可用，强制它换别的方式回答，"
                     "或者直接降级成模板话术，告诉用户稍后再试。"
                     "同时把这条失败链路完整记进日志，事后排查是 schema 设计有问题、"
                     "描述有歧义，还是下游本身不稳定——"
                     "同一个工具反复失败通常是设计问题，不是运气问题。",
            },
            {
                "q": "工具失败的错误信息，你会原样透传给用户吗？",
                "a": "不会。给模型看的信息和给用户看的不是同一份。"
                     "模型要的是可行动的技术细节，比如参数范围、字段名；"
                     "用户要的是结论和下一步动作。"
                     "而且内部的错误码、下游服务名、堆栈这些都不该暴露给用户，"
                     "既是体验问题也是安全问题。所以中间要有一层翻译，"
                     "把人话的结论交给模型去组织回答。",
            },
        ],
        "answer_keywords": [
            "错误分类", "参数错误", "系统错误", "超时", "指数退避", "幂等",
            "重试上限", "降级", "回填", "业务失败",
        ],
        "answer_min_score": 70,
        "answer_hint": (
            "先问自己：这个错误，模型有能力修吗？它改一个参数就能解决，"
            "还是无论怎么改都没用？按这个标准把错误分成几堆，答案就出来了。"
        ),
    },
    {
        "code": "ia-tool-06",
        "stage": "Function Calling 与工具设计",
        "title": "模型经常编出根本不存在的参数值，比如状态传了个「处理中」这样的假值，你怎么防？",
        "summary": "幻觉参数的校验与纠偏",
        "definition": (
            "## 面试官在考什么\n\n"
            "这题考**防幻觉在工具层的落地**，本质是「你信不信模型给的参数」。"
            "三层递进：\n\n"
            "1. **schema 收窄**：能枚举的用 enum，能标类型的标类型，从源头压缩它编的空间。\n"
            "2. **服务端强校验**：JSON Schema 或者 Pydantic 过一遍，绝不裸信。\n"
            "3. **纠偏回填**：校验失败要告诉它正确的取值范围，让它重出，而不是简单报错。\n\n"
            "合格线：明确说出「模型给的参数是不可信输入，必须自己校验」这条底线。\n"
            "加分项：能给出产品侧手段——拿不准就让模型反问用户，"
            "而不是替用户猜一个默认值（对写操作尤其危险）。"
        ),
        "plain": (
            "模型编参数，跟它编事实是同一个毛病：**它追求的是「看起来合理」，"
            "不是「真的存在」**。\n\n"
            "比如你让它下单，它可能给你一个 `status` 是「处理中」，"
            "因为中文里这么说很自然；但你的系统只认 pending 和 shipped。"
            "它不是故意骗你，它是**不知道你的取值集合有多大**。\n\n"
            "防的办法分三层，从外往里收：\n\n"
            "1. **把值收窄。** 能在 schema 里写成 enum 的，就不要用自由字符串。"
            "告诉它只能从这三个值里选，它编的空间就小多了。参数名和字段说明也要写清，"
            "别让它在日期字段上传「明天」。\n"
            "2. **自己再校验一遍。** 它给的参数一定要过校验层，"
            "JSON Schema 或者 Pydantic 都行，不合法的直接拦住。"
            "**永远不要把模型的输出直接丢进数据库或者下单接口**——这是底线。\n"
            "3. **错误要能纠偏。** 校验失败时，把「你给的 status 是处理中，"
            "只允许 pending 或 shipped」这句回填给它，让它重出一次，"
            "它改对的概率其实很高。\n\n"
            "还有一层是产品侧的：如果用户本身就没说清（比如没说订单状态），"
            "与其让模型猜，不如让它问用户。**「不确定就问」比「赌一个」便宜得多。**"
        ),
        "example": (
            "我先说个原则：**模型给的参数永远是不可信输入，"
            "要当成外部用户传来的数据一样校验**，绝对不能直接进数据库或者下单接口。\n\n"
            "具体我做三层。**第一层是 schema 收窄。** 能枚举的参数一定用 enum "
            "把取值列全，比如 status 只允许 pending、paid、shipped；"
            "日期、金额写清 format 和单位；必填字段标 required。"
            "它编不出枚举之外的值，因为那些值根本不在选项里。\n\n"
            "**第二层是服务端强制校验。** 不管它返回什么，都先过一遍 JSON Schema 或者"
            "Pydantic，类型不对、值不在枚举、缺必填，直接拒绝执行。"
            "这层是底线，不能省，它也顺带挡掉了 prompt 注入这类意外输入。\n\n"
            "**第三层是纠偏。** 校验失败不是把错误吞掉，而是回填一句精确的话，"
            "比如「status 只能是 pending、paid、shipped，你给的是处理中，请重新生成」，"
            "它拿到具体范围后一般一次就能改对。\n\n"
            "还有个手段我常加：**参数拿不准的宁可留空也不让它猜**。"
            "关键参数用户没说、上下文也推不出来时，我会让它先调澄清工具去问用户，"
            "而不是编一个看起来合理的东西，事后把失败的 case 记下来，"
            "如果是某个参数经常被编错，那是 schema 设计的问题，得回头改工具。"
        ),
        "pitfalls": [
            "**相信模型返回的 JSON 就是合法参数，直接塞给下游**——这是最危险的。"
            "必须有一层自己的校验，对齐外部输入的防御标准，这条不可退让。",
            "**只靠 prompt 里写一句「不要编造参数」**——提醒有一点用但不可靠，"
            "模型不是每次都能守住，尤其是长上下文里指令会被稀释。"
            "结构上能用 enum 收窄的，就不该靠自觉。",
            "**校验失败就直接报错给用户**——用户会觉得莫名其妙，"
            "明明是你内部的参数问题。正确做法是回填给模型让它改正，"
            "改不了再走降级，不该把技术错误暴露到用户面前。",
            "**参数缺失就替它填一个默认值**——对写操作极其危险，"
            "比如默认收货地址可能把货发错地方。缺失的关键参数应该反问用户，"
            "而不是替用户做决定。",
        ],
        "followups": [
            {
                "q": "如果是自由文本参数，比如备注、地址，没法用 enum 约束，怎么办？",
                "a": "那就换别的约束方式。长度上限、正则做格式校验、屏蔽某些字符；"
                     "再叠一层安全过滤，防 prompt 注入和 SQL 注入，写库一律参数化查询。"
                     "语义层面还可以加一个后置校验，用一个便宜模型专门检查这个参数"
                     "是不是合规合理，比如地址是不是完整、备注里有没有夹带指令。"
                     "自由文本不可能完全防住编造，重点是限制它的破坏半径。",
            },
            {
                "q": "模型把一个数字参数传成了字符串「100 元」，这算严重问题吗？",
                "a": "单次不严重，但它是个信号——说明你的 schema 没写清类型和单位，"
                     "校验层也可能不到位。合理做法是两头都改："
                     "schema 里明确类型和单位，校验层对可容忍的类型做规范化，"
                     "比如把「100 元」解析成 100 分再传下去，"
                     "而不是一有格式差异就直接拒绝。关键是规范化要有确定规则，"
                     "不能变成我又在替模型猜它的意思。",
            },
        ],
        "answer_keywords": [
            "enum", "JSON Schema", "Pydantic", "校验", "类型", "required",
            "纠偏", "回填", "幻觉", "默认值",
        ],
        "answer_min_score": 70,
        "answer_hint": (
            "把模型的输出当成一个陌生用户提交的表单数据来想——"
            "你会直接相信它吗？会先做哪一步检查？从这个问题往下推。"
        ),
    },
    {
        "code": "ia-tool-07",
        "stage": "Function Calling 与工具设计",
        "title": "如果这个工具是下单、扣款这种有真实后果的操作，你会怎么设计权限边界和人工确认？",
        "summary": "危险操作的权限与确认",
        "definition": (
            "## 面试官在考什么\n\n"
            "这是 Agent 岗**最重要也最容易答砸**的一题。面试官在验证你的安全边界意识，"
            "而不只是功能实现能力：\n\n"
            "1. 你是否知道**工具必须分级授权**，读和写不能同等对待。\n"
            "2. 你是否理解一条铁律：**模型不拥有权限，权限在人和服务端手里**。"
            "模型说的「用户已确认」也是生成的文字，同样不可信。\n"
            "3. 你是否知道人工确认要做到「意图明确、参数可见、可撤销」。\n\n"
            "合格线：说出读写分权 + 关键操作必须人工确认 + 服务端强校验三件事。\n"
            "加分项：提到确认界面必须展示具体参数、幂等键防重复提交、"
            "以及最小权限加审计日志。"
        ),
        "plain": (
            "先记住一句话：**模型不应该拥有权限，它只能发起请求；"
            "权限永远在人和服务端手里。**\n\n"
            "想想现实里的银行 App：你可以随便查余额（读操作），"
            "但要转账，就得输密码、收短信验证码（写操作要二次确认）。"
            "这不是因为不信任你，而是因为**读错了没损失，写错了有真金白银的后果**。\n\n"
            "所以工具设计的第一件事是**分级**：\n\n"
            "- 只读工具（查订单、查库存）：可以自动执行，错了也没关系。\n"
            "- 低风险写工具（改备注、加购物车）：可以自动，但要能撤销。\n"
            "- 高风险写工具（下单、扣款、删除、改地址）：必须停下来等人确认。\n\n"
            "第二件事是**确认的内容要说人话**。如果只弹一句「是否执行 create_order」，"
            "用户根本不知道自己在同意什么。必须把动作翻译出来："
            "「即将为订单 123 支付 299 元，从尾号 8888 的卡扣款，确认吗？」\n\n"
            "第三件事是**服务端要再校验一遍**。不能因为模型说「用户已确认」就放行，"
            "那句话也是它生成的，同样不可信。"
        ),
        "example": (
            "我会分三步设计。**第一步是工具分级。** 我把工具分成只读、低风险写、"
            "高风险写三类。只读的比如查订单、查库存，允许自动执行，错了也没影响。"
            "低风险的写比如加购物车、改备注，可以自动但要留撤销入口。"
            "下单、扣款、退款、删除数据这类有真实后果的，一律走人工确认，"
            "模型只能提议，不能执行。\n\n"
            "**第二步是确认要让人看得懂。** 不能弹一句「是否执行 create_order」，"
            "用户不知道自己在同意什么。我会让模型先输出一个结构化的待确认动作，"
            "由前端渲染成人话：即将为订单 123 支付 299 元，从尾号 8888 的银行卡扣款，"
            "确认吗？**参数必须显式展示**，因为用户要确认的是这笔具体交易，"
            "不是一个工具名。\n\n"
            "**第三步是权限在服务端，不在模型。** 模型说「用户已确认」这句话本身也是"
            "生成的，同样不可信。真正的授权由后端做：校验用户权限、金额是否超限、"
            "需不需要二次验证。另外每个请求带用户身份和幂等键，"
            "防止重试或者连点造成重复下单。\n\n"
            "我还会统一加审计日志，谁在什么时间、基于哪次对话确认了什么操作，全部留痕。"
            "这块我的态度很明确：**能力上限可以交给模型，安全下限必须由我们在代码和"
            "流程里守住。**"
        ),
        "pitfalls": [
            "**让模型自己判断这个操作危不危险**——模型判断本身就可能被 prompt 注入"
            "诱导。危险等级必须是服务端的静态配置，不能是运行时的动态判断。",
            "**确认时只展示工具名**——用户不知道自己同意了什么，等于没有确认。"
            "必须把金额、操作对象、接收方这些关键参数翻译成人话展示出来。",
            "**允许模型在参数里写 confirmed 为 true 来跳过确认**——这是极其危险的漏洞。"
            "模型的输出永远不能当作授权凭证，授权只能来自用户的真实交互和服务端校验。",
            "**没有幂等保护**——模型重试或者用户网络抖动导致重复提交，"
            "可能重复扣款、重复下单。每个写操作都要有幂等键，由服务端去重。",
        ],
        "followups": [
            {
                "q": "用户确认之后，模型又改了参数，比如把金额改了，你怎么处理？",
                "a": "这是典型的 TOCTOU 问题：确认的是 A，执行的是 B。"
                     "我的做法是把确认和执行绑定成同一个 payload 的哈希，"
                     "确认之后参数不允许再变，一旦变了这张确认就作废，必须重新确认。"
                     "同时确认票据要有时效，比如几分钟内有效，过期重来，"
                     "避免一张旧票据被反复利用。原则就是：授权绑定的是具体动作，"
                     "不是工具本身。",
            },
            {
                "q": "那只读工具就完全不用管吗？",
                "a": "也要管。读可能涉及隐私和越权，比如查到别人的订单，"
                     "所以必须在数据层做行级权限过滤，不能让模型决定查谁。"
                     "另外读的量也有风险，比如批量导出全库，需要限流和字段脱敏。"
                     "还有一点：只读的数据也会进入上下文，如果服务端返回了不该给这个用户看的"
                     "字段，模型可能会把它说出来，所以脱敏要在返回前做，"
                     "不能指望模型帮你保密。",
            },
        ],
        "answer_keywords": [
            "分级", "只读", "写操作", "人工确认", "权限", "服务端",
            "幂等", "审计", "撤销", "最小权限",
        ],
        "answer_min_score": 70,
        "answer_hint": (
            "想一下银行 App：为什么查余额随便查，转账却要输密码收验证码？"
            "把这个直觉搬到工具设计上，再想想「模型的确认」到底算不算数。"
        ),
    },
    {
        "code": "ia-tool-08",
        "stage": "Function Calling 与工具设计",
        "title": "你怎么理解 MCP？它到底解决了什么问题，为什么大家都在提它？",
        "summary": "MCP 的定位与价值",
        "definition": (
            "## 面试官在考什么\n\n"
            "这题考**技术视野加判断力**，分三层递进：\n\n"
            "1. **能不能讲清它是什么**：一个标准化的开放协议，让模型客户端以统一方式"
            "接入外部工具和数据源。说它是模型或者框架的，直接扣分。\n"
            "2. **能不能讲清它解决的痛点**：**M×N 的集成爆炸**。"
            "把适配工作量从乘法降到加法，这是它最有说服力的价值。\n"
            "3. **有没有判断力**：它**不解决**什么——工具选不对、参数幻觉、安全边界，"
            "这些都不在协议范围内。能主动划清边界是明显加分项。\n\n"
            "合格线：说清「统一协议、减少重复适配」。\n"
            "加分项：提到传输方式（stdio / HTTP+SSE）、能力协商，"
            "以及它和 Function Calling 的关系——互补而非替代。"
        ),
        "plain": (
            "先看一个熟悉的痛点。早年你要对接不同的支付渠道：微信一套代码、"
            "支付宝一套、银联一套，每加一个渠道就重写一遍。"
            "后来有了统一的支付网关，你只对着一个标准接口写，网关去适配各家。\n\n"
            "MCP 就是这个角色，只不过换成了**模型和工具之间**。\n\n"
            "没有 MCP 的时候是什么样？假设你有 3 个模型客户端、5 个数据源"
            "（你的数据库、GitHub、飞书、Jira、文件系统）。"
            "想让每个客户端都能用每个数据源，你得写 3 乘 5 等于 15 套适配代码，"
            "而且各家 function calling 的格式还不一样。\n\n"
            "有了 MCP，每个数据源实现一个 MCP Server，每个客户端实现一次 MCP Client，"
            "加起来就是 3 加 5 等于 8 件事。**乘法变成了加法**，这就是它最大的价值。"
            "而且别人写好的 Server 你能直接复用，不用从零造。\n\n"
            "但也要清醒：MCP 只统一了「怎么接」，它不负责「模型会不会选对工具」"
            "「参数会不会编」「危险操作要不要确认」。那些还是你自己的事。"
        ),
        "example": (
            "我理解 MCP 是一个**把模型和外部工具、数据源之间的接入标准统一起来的"
            "开放协议**，它解决的问题一句话：**把集成的乘法变成加法**。\n\n"
            "举个具体例子。我有几个模型客户端，要接十几个数据源——数据库、GitHub、"
            "工单系统、文件系统。没有统一协议时，每个客户端对每个数据源都要写一套适配，"
            "各家 function calling 的格式还不一样，工作量是 M 乘 N，"
            "加一个新数据源就得把所有客户端重做一遍。\n\n"
            "有了 MCP，每个数据源实现一个 MCP Server，把自己的工具按协议暴露出来；"
            "客户端实现一次 MCP Client 就能对接所有 Server，复杂度从 M 乘 N 降到"
            "M 加 N，别人写好的 Server 还能直接复用。\n\n"
            "机制上它是 client-server 架构，Server 声明自己提供哪些 tools、resources"
            "和 prompts，Client 连上后通过能力协商知道对方支持什么，"
            "传输可以是本地 stdio 或远程 HTTP 加 SSE。\n\n"
            "有一点我想说清楚：**MCP 和 Function Calling 不是替代关系，是互补的。**"
            "Function Calling 是模型侧的机制，负责输出调用意图；MCP 是工具侧的接入标准，"
            "负责工具怎么被发现和调用，MCP 暴露的工具最终仍要通过 function calling "
            "让模型去选。所以它也不是银弹：工具描述歧义、参数幻觉、权限边界，它都不解决。"
        ),
        "pitfalls": [
            "**说 MCP 是一个模型或者一个框架**——这是概念错误。"
            "它既不是模型也不是 Agent 框架，而是一个通信协议，"
            "规定 server 和 client 怎么描述、发现和调用能力。说错显得只看过宣传文章。",
            "**说 MCP 能替代 Function Calling**——两者层次不同。"
            "FC 是模型输出结构化调用的能力，MCP 是工具的接入和分发标准，"
            "MCP 暴露的工具最终仍要经过 FC 触发。说成二选一暴露理解不深。",
            "**说「有了 MCP 工具问题就都解决了」**——过度期待。"
            "工具选错、参数编造、危险操作确认这些全都不在协议范围内。"
            "能主动划清边界反而是加分项。",
            "**只背一句「MCP 是个标准协议」就停住**——没说出它到底解决什么痛点。"
            "要用「M 乘 N 变 M 加 N」或者具体的对接场景把它讲活，"
            "否则听起来只是听过这个词。",
        ],
        "followups": [
            {
                "q": "如果要接入一个第三方提供的 MCP Server，你会担心哪些问题？",
                "a": "安全是第一位的。第三方 Server 能看到我传过去的参数，"
                     "也可能是别人写的、有漏洞的代码。所以我会做权限隔离，"
                     "只给它完成任务必需的凭据，敏感字段先脱敏再传；"
                     "还要看它提供的工具描述质量如何，描述太差模型照样选错。"
                     "另外必须有超时和熔断，外部 Server 不稳定时不能把主链路拖死，"
                     "而且要能随时摘掉它。",
            },
            {
                "q": "如果你们的工具要在公司内部复用，你会怎么划分 MCP Server 的粒度？",
                "a": "我倾向按业务域拆，而不是一个工具一个 Server，也不是搞一个巨型 Server。"
                     "按域拆的好处是权限边界清晰、可以按需授权、故障也能隔离。"
                     "同时要控制单个 Server 暴露的工具数量，"
                     "一个 Server 上百个工具模型还是选不对，"
                     "得配检索或者分层暴露来收。粒度这件事本质上是权限设计和"
                     "上下文预算共同决定的，不是拍脑袋定的。",
            },
        ],
        "answer_keywords": [
            "MCP", "协议", "M×N", "M+N", "Client", "Server",
            "复用", "能力协商", "stdio", "Function Calling",
        ],
        "answer_min_score": 70,
        "answer_hint": (
            "回忆一下「统一支付网关」为什么有价值——各家渠道对接一次就够了。"
            "把模型客户端和数据源分别代入「渠道」这个位置，想想原来要写多少套适配。"
        ),
    },
]
