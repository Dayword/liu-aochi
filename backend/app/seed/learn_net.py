"""计算机网络：从传输层到应用层的面试高频链路。

按"面试问到就答得上"的顺序编排，全程不写代码，用报文、时序、状态机讲清楚。
"""

LESSONS: list[dict] = [
    # ------------------------------------------------------------------ net-01
    {
        "code": "net-01",
        "subject": "计算机网络",
        "stage": "传输层",
        "runner": "none",
        "title": "分层模型：OSI 与 TCP/IP",
        "summary": "网络为什么非要一层套一层",
        "definition": (
            "**分层的核心思想是：每一层只解决一类问题，并且只依赖下一层提供的服务。**"
            "这样每一层都能独立演进，协议栈才不会变成一坨。\n\n"
            "两个模型必须分清楚：\n\n"
            "- **OSI 七层**（ISO 的理论参考模型）：物理层 → 数据链路层 → 网络层 → 传输层 → 会话层 → 表示层 → 应用层\n"
            "- **TCP/IP 四层**（真实互联网）：网络接口层 → 网络层 → 传输层 → 应用层\n"
            "- **五层**（教材里的折中讲法）：物理层、数据链路层、网络层、传输层、应用层\n\n"
            "每层的职责、代表协议和数据单元（PDU）：\n\n"
            "| 层 | 解决什么问题 | 代表协议 | PDU |\n"
            "| --- | --- | --- | --- |\n"
            "| 应用层 | 业务语义（要什么、返回什么） | HTTP、DNS、SMTP、SSH | 报文 message |\n"
            "| 传输层 | **进程到进程**的通信 | TCP、UDP | 段 segment / 数据报 datagram |\n"
            "| 网络层 | **主机到主机**，跨网寻址与路由 | IP、ICMP、ARP | 分组 packet |\n"
            "| 数据链路层 | 同一链路上相邻节点的传输 | Ethernet、PPP、Wi-Fi | 帧 frame |\n"
            "| 物理层 | 比特在介质上怎么表示 | 双绞线、光纤、射频 | 比特 bit |\n\n"
            "发送方向下走是**封装**（逐层加头部），接收方向上走是**解封装**（逐层拆头部）。"
            "每一层只认自己那一层的头部，不关心上层塞了什么内容 —— 这就是分层的价值。"
        ),
        "plain": (
            "把网络想成**寄快递**。你（应用层）写好一封信，内容用什么语言、写什么，是你的事；"
            "把信装进信封、写上「收件人：张三」的那个「人」是谁，是**传输层的端口**；"
            "快递公司只按**地址**把包裹送到某个小区（网络层的 IP），路上每一段用卡车还是飞机（物理层）它不关心。\n\n"
            "这么分的好处是**互不干扰**：你换快递公司（换链路层协议），不需要重写信；"
            "你把地址从北京改成上海（换 IP），信的内容一个字都不用改。\n\n"
            "**为什么要记住两个模型？** 因为 OSI 是「教科书里的标准答案」，TCP/IP 是「真正在跑的东西」。"
            "面试时先答 OSI 七层，再说一句「实际的互联网是 TCP/IP 四层，OSI 的会话层和表示层在 TCP/IP 里被并进了应用层」，"
            "这句话能直接体现你是理解而不是背诵。"
        ),
        "example": (
            "同一个 HTTP 请求，从应用层往下走是这样被封装的：\n"
            "\n"
            "① 应用层：HTTP 请求报文\n"
            "   GET /index.html HTTP/1.1\n"
            "   Host: example.com\n"
            "\n"
            "② 传输层：TCP 加「源端口 / 目的端口 / 序号 / 窗口」→ 段（segment）\n"
            "   51000 → 80\n"
            "\n"
            "③ 网络层：IP 加「源 IP / 目的 IP / TTL / 协议号」→ 分组（packet）\n"
            "   192.168.1.7 → 93.184.216.34\n"
            "\n"
            "④ 链路层：以太网加「源 MAC / 目的 MAC / 帧校验」→ 帧（frame）\n"
            "   目的 MAC 是本网段网关的 MAC，不是目标服务器的\n"
            "\n"
            "⑤ 物理层：帧变成 0101… 的电信号 / 光信号发出去\n"
            "\n"
            "接收方反过来一层层拆头，最后把原始的 HTTP 文本交给监听 80 端口的进程"
        ),
        "example_output": (
            "每一层只看自己那一层的头部：链路层按 MAC 判断「是不是给我的」，"
            "网络层按目的 IP 判断「继续转发还是交上去」，传输层按端口号找进程，"
            "应用层才第一次看到 HTTP 文本。"
            "所以「同一层的两个实体在逻辑上直接对话」是成立的 —— 它们在读对端的同层头部。"
        ),
        "pitfalls": [
            "**只背了七层的名字，答不出「为什么要分层」**：面试官问分层不是考记忆，是想听「解耦、各层独立演进、只依赖下层接口」。答不上这一句，后面讲再多协议也显得是背的。",
            "**把传输层和网络层的职责搞混**：IP 只负责把分组送到对方**主机**，不保证送达、不保证顺序、不保证不重复；TCP 才负责**进程到进程**的可靠有序传输。答「IP 负责可靠传输」是硬伤。",
            "**拿 OSI 七层当实际部署模型**：真实互联网跑的是 TCP/IP，OSI 的会话层、表示层在 TCP/IP 里没有独立对应。面试时可以两个都讲，但一定要点明「OSI 是参考模型，TCP/IP 是事实标准」。",
            "**设备和层的对应记混**：集线器在物理层，交换机在数据链路层，路由器在网络层，负载均衡器和防火墙可能工作在传输层到应用层。张口就说「交换机是三层设备」会立刻露怯。",
        ],
        "task": "",
        "setup": "",
        "starter": "",
        "hint": "",
        "quizzes": [
            {
                "type": "choice",
                "stem": "面试官问：TCP 和 IP 分别在模型的哪一层，各自解决什么问题？",
                "options": [
                    "IP 在网络层负责寻址与路由（把分组送到目标主机），TCP 在传输层负责端到端的可靠传输（把数据可靠、有序地交给目标进程）",
                    "IP 在传输层负责可靠传输，TCP 在网络层负责寻址路由",
                    "两者都在传输层，IP 管地址，TCP 管端口",
                    "两者都在网络层，TCP 只是 IP 的可靠版本",
                ],
                "answer_index": 0,
                "explanation": (
                    "「TCP/IP」这个名字容易让人以为它们是一层的两个部分，其实是两层："
                    "IP 是网络层的分组投递服务，只管把包送到主机，是「尽力而为」的；"
                    "TCP 是传输层的端到端协议，靠确认、重传、序号把不可靠的 IP 服务包装成可靠的字节流。"
                ),
            },
            {
                "type": "choice",
                "stem": "下面哪一组「层 — 协议 — PDU 名称」的对应完全正确？",
                "options": [
                    "传输层 TCP → 段（segment）；网络层 IP → 分组（packet）；链路层 Ethernet → 帧（frame）",
                    "传输层 IP → 分组；网络层 TCP → 段；链路层 Ethernet → 帧",
                    "网络层 TCP → 段；传输层 IP → 分组；链路层 Ethernet → 包（package）",
                    "应用层 HTTP → 帧；传输层 TCP → 报文；网络层 IP → 段",
                ],
                "answer_index": 0,
                "explanation": (
                    "TCP 在传输层、数据单元叫段；IP 在网络层、数据单元叫分组；以太网在链路层、数据单元叫帧。"
                    "PDU 名字是面试里最容易被随口追问的细节，答错会显得基础不牢。"
                ),
            },
            {
                "type": "judge",
                "stem": "在 TCP/IP 四层模型中，网络接口层大致对应 OSI 的物理层和数据链路层。",
                "answer": True,
                "explanation": (
                    "TCP/IP 的网络接口层把 OSI 的物理层和数据链路层合在了一起，"
                    "因为它对底层的具体介质不做规定，只要能跑 IP 就行 —— 这也正是它能跑在以太网、Wi-Fi、光纤上的原因。"
                ),
            },
            {
                "type": "blank",
                "stem": "在分层模型里，数据链路层封装出来的数据单元（PDU）中文叫「___」。",
                "hint": "一个汉字，也可以是它的英文单词",
                "answer": "帧",
                "accept": ["frame", "Frame"],
                "explanation": (
                    "链路层加上 MAC 头、尾部校验之后的数据单元叫「帧（frame）」。"
                    "顺着往上记：帧（链路层）→ 分组（网络层）→ 段（传输层）→ 报文（应用层）。"
                ),
            },
            {
                "type": "short",
                "stem": "面试官问：网络为什么要分层？分层带来了什么好处，又付出了什么代价？",
                "keywords": ["解耦", "独立演进", "复用", "只依赖下层", "头部开销", "性能损耗"],
                "reference": (
                    "分层的本质是**分而治之**：把「怎么在介质上传输比特」「怎么把包送到对端主机」「怎么可靠地交给进程」"
                    "这些互相独立的问题拆开，每一层只依赖下层提供的接口，可以各自升级而不影响其他层 ——"
                    "所以你能在完全不改 HTTP 的情况下，把底层从以太网换成 Wi-Fi。同时分层让协议可以被复用，IP 上面同时跑 TCP、UDP、ICMP。\n"
                    "代价是**每个都要加头部**，数据包比载荷大出一截（一个空 TCP 包光头部就有 54 字节）；"
                    "层与层之间的拷贝和处理也带来性能损耗，所以高性能场景会做各种旁路优化（内核旁路、零拷贝）。"
                    "另一层代价是**跨层问题难处理**，比如 TCP 把丢包当拥塞信号，在 Wi-Fi 这种链路本身就会丢包的场景下判断就不准。"
                ),
                "explanation": (
                    "答「好处」和「代价」两条腿才完整。只讲好处会显得只会背课本，"
                    "能主动说出「跨层假设可能不成立」这一层，面试官会觉得你真的想过。"
                ),
            },
        ],
    },
    # ------------------------------------------------------------------ net-02
    {
        "code": "net-02",
        "subject": "计算机网络",
        "stage": "传输层",
        "runner": "none",
        "title": "TCP 三次握手",
        "summary": "两个陌生人怎么确认对方在线",
        "definition": (
            "三次握手的目的是：**双方各自确认「我能发、你能收」和「你能发、我能收」，"
            "并把各自的初始序号 ISN 同步给对方。**\n\n"
            "报文与状态迁移：\n\n"
            "1. 客户端 → 服务端：`SYN=1, seq=x`，客户端进入 `SYN_SENT`\n"
            "2. 服务端 → 客户端：`SYN=1, ACK=1, seq=y, ack=x+1`，服务端进入 `SYN_RCVD`\n"
            "3. 客户端 → 服务端：`ACK=1, seq=x+1, ack=y+1`，双方进入 `ESTABLISHED`\n\n"
            "**为什么是三次，而不是两次？** 核心原因是**防止失效的历史连接**。"
            "假设客户端发过一个 `SYN`，它在网络里绕了很久才到达服务端：如果是两次握手，服务端一收到这个旧 `SYN` 就认为连接建立、"
            "分配了缓冲区，而客户端根本不承认这条连接，这份资源就白占了，还可能被用来发垃圾数据。"
            "第三次 `ACK` 让服务端在「客户端确实收到了我的 SYN，并且愿意继续」之后才最终确认。\n\n"
            "**为什么不是四次？** 因为服务端的第二个报文已经把「自己的 SYN」和「对客户端 SYN 的 ACK」"
            "合并进了同一份报文，双向序号同步三次就够，不需要第四份。\n\n"
            "注意：`SYN` 和 `FIN` 都**消耗一个序号**（它们占位但不携带数据），所以 `ack = 对方的 seq + 1`。"
        ),
        "plain": (
            "想象你**打电话找人**：\n\n"
            "先喊一句「喂，能听到吗？」（第一次：我说话的能力 + 你的听力）；"
            "对方答「能听到，那你能听到我吗？」（第二次：我的听力 + 他的说话能力 + 他的听力提问）；"
            "你回「能听到，开始说吧」（第三次：他的说话能力被确认）。\n\n"
            "**少了第三次会怎样？** 你只喊了第一句，对方就说「好，我听到了」，然后开始自顾自讲话 ——"
            "他根本不知道你到底有没有在听。放在网络上，这就是那条「你根本不认的历史连接」占着服务端的资源。\n\n"
            "还有一个细节值得记住：**第三次握手是可以携带数据的**。"
            "客户端发出第三个 ACK 之后就进入 `ESTABLISHED` 了，后续数据可以直接搭在这个 ACK 上一并发出；"
            "而前两次不行 —— `SYN` 报文不能带数据，因为序号还没同步完。"
        ),
        "example": (
            "客户端                          服务端\n"
            "  │                              │  LISTEN\n"
            "  │── SYN=1, seq=x ────────────▶│  （客户端进入 SYN_SENT）\n"
            "  │                              │  （服务端进入 SYN_RCVD）\n"
            "  │◀── SYN=1, ACK=1, ──────────│\n"
            "  │    seq=y, ack=x+1            │\n"
            "  │                              │\n"
            "  │── ACK=1, seq=x+1, ────────▶│\n"
            "  │    ack=y+1                   │\n"
            "  │  ESTABLISHED                 │  ESTABLISHED\n"
            "\n"
            "状态迁移：\n"
            "  CLOSED ──发SYN──▶ SYN_SENT ──收SYN+ACK并回ACK──▶ ESTABLISHED\n"
            "  LISTEN ──收SYN──▶ SYN_RCVD ──收ACK──▶ ESTABLISHED"
        ),
        "example_output": (
            "三条报文之后双方都进入 `ESTABLISHED`。"
            "注意进入的时机不同：客户端**发出**第三个 ACK 就进入，服务端要**收到**第三个 ACK 才进入。"
            "所以如果第三个 ACK 丢了，客户端这边已经在发数据，服务端还停在 `SYN_RCVD` 并会重传 `SYN+ACK`；"
            "客户端收到重传后会再补一个 ACK，连接照样能建起来 —— 这也是「三次握手对丢包是自愈的」的含义。"
        ),
        "pitfalls": [
            "**答不出「两次为什么不行」**：只说「三次是为了确认双方的收发能力」只是标准答案的前半句，面试官一定会追问。必须补上「防止延迟到达的旧 SYN 让服务端建立一条客户端不认的连接、白占资源」。",
            "**seq 和 ack 的取值写错**：`ack` 的含义是「我期望收到的下一个字节序号」= 对方的 `seq + 1`，因为 `SYN` 消耗一个序号。写成 `ack=x` 或 `ack=x+2` 都是不懂序号语义。",
            "**以为 SYN 报文可以携带数据**：常规实现里 `SYN` 不携带数据（TFO 的 Cookie 是例外），但它**照样消耗一个序号**，这就是 `ack=x+1` 的来历。",
            "**不知道 SYN Flood 和半连接队列**：攻击者只发第一个 `SYN` 不回第三个 `ACK`，服务端的 `SYN_RCVD` 半连接队列会被迅速占满，正常请求也建不了连接。常见防护是开启 `tcp_syncookies`、调小 `SYN_RCVD` 超时、扩大/限制半连接队列，或者前置 SYN 代理和限速。",
        ],
        "task": "",
        "setup": "",
        "starter": "",
        "hint": "",
        "quizzes": [
            {
                "type": "choice",
                "stem": "面试官问：TCP 建立连接为什么是三次握手？两次为什么不行？",
                "options": [
                    "两次握手时服务端一发出 SYN+ACK 就认为连接已建立，历史上延迟到达的旧 SYN 会让它建立一条客户端根本不认的连接、白占资源；第三次 ACK 让服务端确认客户端确实收到了自己的 SYN 才最终建立",
                    "因为 TCP 是全双工，本来需要四个报文，只是服务端把其中两个合并了所以变成三次",
                    "因为客户端需要确认服务端的接收窗口大小，这个信息只能在第三个报文里传",
                    "两次握手无法同步序号，必须第三次才能把 MSS 和窗口缩放因子传过去",
                ],
                "answer_index": 0,
                "explanation": (
                    "B 说的是「为什么挥手是四次而握手是三次」的镜像答案，方向反了；"
                    "C、D 都是编出来的机制，窗口和 MSS 是在报文头部里带的，不依赖第几个报文。"
                    "这题的核心永远只有一句：**防止失效的历史连接**。"
                ),
            },
            {
                "type": "choice",
                "stem": "客户端发出的第三个报文是 `ACK=1, seq=x+1, ack=y+1`，其中 `ack=y+1` 表示什么？",
                "options": [
                    "期望收到对方下一个字节的序号是 y+1 —— 因为对方的 SYN 虽然不带数据，但消耗了一个序号",
                    "已经成功收到了 y+1 个字节的数据",
                    "客户端的接收窗口大小是 y+1",
                    "一个随机确认号，只要落在合法范围内都可以",
                ],
                "answer_index": 0,
                "explanation": (
                    "TCP 的确认号是**累积确认**语义：「这个序号之前的字节我都收到了，请从 y+1 开始发」。"
                    "`SYN`/`FIN` 各消耗一个序号，所以两者的确认值都要 +1。"
                ),
            },
            {
                "type": "judge",
                "stem": "三次握手中，如果服务端发出的 SYN+ACK 丢失，客户端会超时重传自己的 SYN。",
                "answer": True,
                "explanation": (
                    "客户端发完 `SYN` 后启动重传定时器，在 `SYN_SENT` 状态里等不到 `SYN+ACK` 就会重传 `SYN`（"
                    "Linux 默认大约重试 5 次、总耗时几十秒）。所以第一次握手的丢失靠客户端超时重传，第二次的丢失靠服务端重传 `SYN+ACK`。"
                ),
            },
            {
                "type": "blank",
                "stem": "SYN 报文本身不携带数据，但它会消耗 ___ 个序号。",
                "hint": "填一个数字",
                "answer": "1",
                "accept": ["一", "一个"],
                "explanation": (
                    "这也是为什么第二次握手的确认号是 `ack=x+1`，而第三次的序号是 `seq=x+1`。"
                    "FIN 同理，也消耗一个序号。"
                ),
            },
            {
                "type": "short",
                "stem": "面试官问：服务器被 SYN Flood 打挂了，你有哪些防护思路？",
                "keywords": ["syncookies", "半连接队列", "SYN_RCVD", "超时", "限速", "代理"],
                "reference": (
                    "先讲清攻击原理：攻击者大量发送第一个 `SYN` 但不回第三个 `ACK`，服务端的半连接队列（`SYN_RCVD` 队列）被占满，"
                    "正常用户也建不了连接，而且这些半开连接会占用内核内存。\n"
                    "防护从三个方向做：**一是让服务端不存状态**——开启 `tcp_syncookies`，把连接信息编码进 `SYN+ACK` 的序号里，"
                    "等客户端回 `ACK` 时再重建状态，队列被占满时自动启用；"
                    "**二是缩短和限制**——调小 `SYN_RCVD` 超时、合理设置半连接队列长度（`tcp_max_syn_backlog`），让脏连接快点被回收；"
                    "**三是前置清洗**——在网关/负载均衡/云 WAF 层做 SYN 代理、限速和 IP 黑名单，让脏流量根本到不了后端。\n"
                    "另外补一句：`syncookies` 会丢失一部分 TCP 选项（比如窗口缩放），属于「保命优先」的方案。"
                ),
                "explanation": (
                    "这题是「三次握手」最常见的延伸问：能答出「服务端不存状态」这个思路，说明你理解握手的本质是状态同步。"
                ),
            },
        ],
    },
    # ------------------------------------------------------------------ net-03
    {
        "code": "net-03",
        "subject": "计算机网络",
        "stage": "传输层",
        "runner": "none",
        "title": "四次挥手与 TIME_WAIT",
        "summary": "连接怎么优雅地拆掉，拆完还要等一会儿",
        "definition": (
            "TCP 是**全双工**的，两个方向的数据通道各自独立，所以要**各关一次**，一共四次挥手：\n\n"
            "1. 主动方 → 被动方：`FIN=1, seq=u`，主动方进入 `FIN_WAIT_1`\n"
            "2. 被动方 → 主动方：`ACK=1, ack=u+1`，被动方进入 `CLOSE_WAIT`，主动方进入 `FIN_WAIT_2`\n"
            "3. 被动方 → 主动方：`FIN=1, seq=w`，被动方进入 `LAST_ACK`\n"
            "4. 主动方 → 被动方：`ACK=1, ack=w+1`，主动方进入 `TIME_WAIT`，等待 2MSL 后真正 `CLOSED`\n\n"
            "**为什么挥手要四次，握手只要三次？** 因为握手时服务端收到 `SYN` 后可以立刻把 `SYN+ACK` 合并发出；"
            "而挥手时，被动方收到 `FIN` 只是知道「对方没数据要发了」，**它自己可能还有数据没发完**，"
            "所以先回一个 `ACK`（此时连接处于半关闭状态），等自己的数据发完了再单独发一个 `FIN`。这两个报文不能合并。\n\n"
            "**为什么主动方要等 2MSL？** 两个理由：\n\n"
            "1. **保证最后一个 ACK 能到达对方**。如果这个 ACK 丢了，对方会超时重传 `FIN`；主动方只要还在 `TIME_WAIT`，"
            "就能重新回一个 `ACK`。MSL（报文最大生存时间）是报文在网络里能存活的最长时间，一个来回最多 2MSL，"
            "所以等 2MSL 就能覆盖「ACK 丢失 + 对方重传 FIN」的最坏情况。\n"
            "2. **让本次连接的残余报文在网络中彻底消失**。否则旧连接的延迟报文可能被之后用同样四元组建立的新连接收下，造成数据错乱。\n\n"
            "Linux 里 `MSL` 固定为 30 秒，所以 `TIME_WAIT` 状态实际固定持续 **60 秒**。"
        ),
        "plain": (
            "还是**打电话**。挂电话不是一个人说挂就挂的：你先说「我没别的事了」（FIN），"
            "对方说「知道了」（ACK），但他可能还有话没说完，于是继续讲两句；等他说完了，才说「我也没事了」（FIN），"
            "你回一句「好，挂了」（ACK）。\n\n"
            "**为什么你要多等一会儿（2MSL）才挂？** 因为如果你说完「好，挂了」就立刻消失，"
            "万一这句话没传到他耳朵里，他会一直以为你还在听、不停地问「喂？喂？」。"
            "你多等一会儿，他重问的时候你还能再答一次。\n\n"
            "顺便把两个状态记牢，它们指向完全不同的问题：\n\n"
            "- **大量 TIME_WAIT**：主动关闭方太多、短连接太频繁。它是**正常现象**，占的是内核资源不是内存泄漏。\n"
            "- **大量 CLOSE_WAIT**：收到对方的 FIN 却**没有调 `close()`**，这是**应用代码的 bug**——"
            "比如 HTTP 客户端拿到的连接没关、连接池只借不还。看到 `CLOSE_WAIT` 堆积，先去看代码，不是调内核参数。"
        ),
        "example": (
            "主动方（客户端）                被动方（服务端）\n"
            "  │  ESTABLISHED                 │  ESTABLISHED\n"
            "  │── FIN=1, seq=u ─────────────▶│\n"
            "  │  FIN_WAIT_1                  │  CLOSE_WAIT（还能继续发数据）\n"
            "  │◀── ACK=1, ack=u+1 ──────────│\n"
            "  │  FIN_WAIT_2                  │\n"
            "  │        …… 服务端把剩余数据发完 ……        │\n"
            "  │◀── FIN=1, seq=w ────────────│  LAST_ACK\n"
            "  │── ACK=1, ack=w+1 ──────────▶│  CLOSED\n"
            "  │  TIME_WAIT（等 2MSL≈60 秒）  │\n"
            "  │  CLOSED                      │\n"
            "\n"
            "状态迁移：\n"
            "  主动方：ESTABLISHED → FIN_WAIT_1 → FIN_WAIT_2 → TIME_WAIT →（2MSL）→ CLOSED\n"
            "  被动方：ESTABLISHED → CLOSE_WAIT → LAST_ACK → CLOSED"
        ),
        "example_output": (
            "四个报文结束后连接才算彻底释放。"
            "`TIME_WAIT` 只出现在**主动关闭的那一方**——这一点非常重要："
            "如果服务端主动关连接（比如 `nginx` 设了 `keepalive_timeout` 到点主动断），"
            "那 `TIME_WAIT` 就在服务端堆积；如果是客户端（浏览器）主动关，就在客户端侧。"
            "另外，被动方如果收到 `FIN` 时确实没有数据要发，`ACK` 和 `FIN` 会被合并成一个报文，"
            "这时四次挥手就退化成「三次」——但这是特例，不是常态。"
        ),
        "pitfalls": [
            "**不知道 TIME_WAIT 只出现在主动关闭方**：看到服务器上一堆 `TIME_WAIT` 就归咎于「客户端不关连接」是错的。反过来，**大量 CLOSE_WAIT 才是应用层问题**——收到 FIN 后没调 `close()`，连接一直停在半关闭状态，这是代码 bug 不是网络问题。",
            "**说不出 2MSL 的两个理由**：只答「等一会儿让连接彻底结束」等于没答。必须给出「保证最后的 ACK 能重发」和「让残余旧报文消散，避免污染复用同一四元组的新连接」这两条。",
            "**以为四次挥手一定能合并成三次**：只有当被动方收到 FIN 时已经没有数据要发（因此 ACK 和 FIN 同发）才会退化。只要还有一个字节没发完，就必须四次。",
            "**一遇到 TIME_WAIT 过多就去调内核参数**：正确顺序是先判断角色（谁主动关）、再看是不是短连接太频繁，优先用长连接/连接池复用，其次才是 `SO_REUSEADDR`、`tcp_tw_reuse` 这些手段。直接改小 `tcp_fin_timeout` 是治标不治本，还可能引入旧报文串包的风险。",
        ],
        "task": "",
        "setup": "",
        "starter": "",
        "hint": "",
        "quizzes": [
            {
                "type": "choice",
                "stem": "面试官问：为什么要有 TIME_WAIT？为什么是等 2MSL 而不是 1MSL？",
                "options": [
                    "为了在最后的 ACK 丢失时能重发（对方重传 FIN 时自己还在，最坏一个来回需要 2MSL），同时让本次连接的残余旧报文在网络中彻底消失，避免污染复用同一四元组的新连接",
                    "为了让操作系统的 socket 缓冲区有时间被回收，等内核释放完内存才能关",
                    "为了让对方有时间把窗口里的数据全部确认完，1MSL 不够走完一个窗口",
                    "因为 MSL 是半个 RTT，2MSL 正好等于一个完整的往返时间",
                ],
                "answer_index": 0,
                "explanation": (
                    "B 和 C 都是编的，TCP 并不依赖 TIME_WAIT 来回收 socket。"
                    "D 是常见的混淆：MSL 是「报文最大生存时间」的工程上限，不是半个 RTT，也不是根据当前网络测出来的。"
                ),
            },
            {
                "type": "choice",
                "stem": "线上服务器出现几万条 `CLOSE_WAIT` 状态的连接，最可能的原因是什么？",
                "options": [
                    "应用收到对方的 FIN 后没有调用 close() 关闭 socket，连接一直卡在半关闭状态",
                    "服务器主动关闭了太多连接，内核参数 tcp_fin_timeout 设得太短",
                    "客户端一直没有回最后一个 ACK",
                    "网络中出现大量延迟的重复 FIN 报文",
                ],
                "answer_index": 0,
                "explanation": (
                    "`CLOSE_WAIT` 是被动方收到 FIN、回了 ACK 之后、还没发出自己 FIN 的状态，"
                    "它完全由应用层代码决定何时离开 —— 所以堆积只有一个解释：**代码没关连接**（连接池泄漏、异常路径漏了 `finally: close()`）。"
                    "`TIME_WAIT` 才是主动方的状态，不要混。"
                ),
            },
            {
                "type": "judge",
                "stem": "TIME_WAIT 状态出现在被动关闭连接的那一方。",
                "answer": False,
                "explanation": (
                    "`TIME_WAIT` 只出现在**主动关闭**的一方：它发出最后一个 ACK 之后进入 `TIME_WAIT` 并等 2MSL。"
                    "被动方最后的路径是 `LAST_ACK → CLOSED`，不会经过 `TIME_WAIT`。"
                ),
            },
            {
                "type": "blank",
                "stem": "TIME_WAIT 状态需要等待 ___ 倍的 MSL 才能真正关闭连接。",
                "hint": "填一个数字",
                "answer": "2",
                "accept": ["二", "2 倍"],
                "explanation": (
                    "2MSL 覆盖的是最坏情况：自己的 ACK 丢失（走 1 个 MSL）+ 对方的 FIN 重传到达（再走 1 个 MSL）。"
                    "Linux 内核里 `MSL` 固定为 30 秒，所以 `TIME_WAIT` 实际是 60 秒。"
                ),
            },
            {
                "type": "short",
                "stem": "面试官问：线上服务器 TIME_WAIT 连接数好几万，会有问题吗？你会怎么处理？",
                "keywords": ["主动关闭方", "短连接", "端口耗尽", "连接复用", "SO_REUSEADDR", "tcp_tw_reuse"],
                "reference": (
                    "先定性再处理。`TIME_WAIT` 本身**不是错误**，它占的是内核里一个很小的控制块，几十万条通常不影响性能，"
                    "所以第一步要看它有没有造成实际后果：如果是**服务端作为主动关闭方**且并发极高，几万条会吃掉内核内存并让 `TIME_WAIT` 回收变慢；"
                    "如果是**客户端侧**，真正的风险是**本地端口耗尽**（每个四元组都要一个临时端口），导致新连接建不起来。\n"
                    "处理顺序是从业务到底层：**一是拉长连接生命周期**，用长连接、连接池、`keepalive`，让 QPS 高的时候也不要每个请求都新建连接，"
                    "这是最有效的；**二是确认主动关连接的角色能不能换**，比如让客户端主动关；"
                    "**三是才考虑参数**，客户端开 `SO_REUSEADDR`、`tcp_tw_reuse` 允许复用 `TIME_WAIT` 端口（配合时间戳，仍能区分旧报文），"
                    "或适当调整 `tcp_max_tw_buckets` 做兜底。\n"
                    "最后补一句：`SO_LINGER=0` 那种「发 RST 跳过 TIME_WAIT」的激进做法会让对端收到错误，非必要不用。"
                ),
                "explanation": (
                    "这题考的是「先判断影响、再对症下药」的工程习惯，直接背参数名会被追问「为什么这么调」。"
                ),
            },
        ],
    },
    # ------------------------------------------------------------------ net-04
    {
        "code": "net-04",
        "subject": "计算机网络",
        "stage": "传输层",
        "runner": "none",
        "title": "TCP 可靠传输：序号、确认与重传",
        "summary": "在一个会丢包的网络上装作不会丢包",
        "definition": (
            "IP 层只提供**尽力而为**的服务：可能丢包、可能乱序、可能重复。"
            "TCP 要在它之上做出「可靠的、有序的字节流」，靠的是下面这一套机制：\n\n"
            "- **序号（seq）**：每个字节都有一个序号，发送方按序编号。初始序号 ISN 是随机生成的，"
            "不固定是为了防止历史连接的报文和重放攻击。\n"
            "- **确认（ACK）**：接收方回**累积确认**——「序号 n 之前的字节我全收到了，请从 n 开始发」。"
            "所以确认号是单调递增的，中间缺一段就卡在那里。\n"
            "- **超时重传**：发出数据后启动定时器，超过 RTO（重传超时时间）还没被确认就重传。"
            "RTO 不是固定值，而是根据 RTT 的平滑平均值和抖动动态计算出来的。\n"
            "- **快速重传**：接收方收到**乱序**的段时会立刻重复回同一个 ACK。"
            "发送方连续收到 **3 个重复 ACK（duplicate ACK）**，就判断中间那个段丢了，不等超时立刻重传 —— 这比等 RTO 快得多。\n"
            "- **SACK（选择性确认）**：告诉发送方「我收到了哪些不连续的块」，这样一次丢包只需要重传真正缺的那些段，"
            "不用把后面的全部重发。\n"
            "- **滑动窗口**：允许连续发送多个段而不用每段都停下等 ACK，把「停等」变成流水线，这是吞吐量的来源。\n"
            "- **校验和**：校验头部和数据，出错就丢弃并等发送方重传。\n\n"
            "但要注意 TCP 保证的边界到哪里为止：它保证的是**字节流**的可靠有序，"
            "**不保证「消息边界」**——这正好是应用层「粘包」问题的根源。"
        ),
        "plain": (
            "把 TCP 想成**寄挂号信**：每封信都有编号（seq），收件人收到 1、2 号后会回条子说「12 之前的都收到了」（累积 ACK）。"
            "如果寄件人迟迟没收到条子，就按超时时间重寄（超时重传）。\n\n"
            "**快速重传像是收件人主动催**：他收到了 1、3、4、5 号，缺了 2 号，于是连着给你回三张一样的条子「我还在等 2」。"
            "你一看三张重复的条子就明白了——2 号不是慢，是丢了，立刻补寄，不必等到超时。\n\n"
            "**为什么「3 个重复 ACK」这个数字是 3？** 因为网络本来就会乱序，收到一两个乱序段很正常，"
            "等攒到 3 个再判定丢包，是「反应速度」和「别误判」之间的折中。"
            "代价是如果只有 1 个包丢了而且后面没有足够的新包能触发 3 个重复 ACK，就只能等超时 ——"
            "这也是为什么小文件传输在丢包时反而更慢。"
        ),
        "example": (
            "场景：发送方连续发了 1、2、3、4 号段，其中 2 号丢了\n"
            "\n"
            "发送方                          接收方\n"
            "  │── 段 1 (seq=1) ────────────▶│  回 ACK=2（表示要 2）\n"
            "  │◀── ACK=2 ───────────────────│\n"
            "  │── 段 2 (seq=2) ──── ✗ 丢失   │\n"
            "  │── 段 3 (seq=3) ────────────▶│  乱序！回 ACK=2（重复 ACK #1）\n"
            "  │◀── ACK=2 ───────────────────│\n"
            "  │── 段 4 (seq=4) ────────────▶│  还是乱序，回 ACK=2（重复 ACK #2）\n"
            "  │◀── ACK=2 ───────────────────│\n"
            "  │── 段 5 (seq=5) ────────────▶│  回 ACK=2（重复 ACK #3）\n"
            "  │◀── ACK=2 ───────────────────│\n"
            "  │                              │\n"
            "  │ 收到 3 个重复 ACK → 触发快速重传\n"
            "  │── 段 2 (seq=2) 重传 ───────▶│  缺口补上，回 ACK=6\n"
            "  │◀── ACK=6 ───────────────────│  1~5 全部确认\n"
            "\n"
            "对比：如果接收方只收到段 3 之后就没了新包，凑不满 3 个重复 ACK，\n"
            "发送方只能等 RTO 到期才重传 —— 快速重传就退化成了超时重传。"
        ),
        "example_output": (
            "一次丢包被「3 个重复 ACK」快速捡回来，发送方没有等 RTO，吞吐量损失小得多。"
            "如果开启 SACK，接收方还能顺便告诉发送方「我已经拿到了 3、4、5」，"
            "发送方就只需要重传 2 号这一个段，而不是把 2 号之后的全部重发。"
        ),
        "pitfalls": [
            "**把「可靠」理解成「一定送达」**：TCP 保证的是「尽力把字节按序交给应用层，做不到就明确告诉你（`RST` 或超时断连）」，不是「一定能送到」。超时重传若干次仍然失败，连接会被判定为断开。",
            "**以为 RTO 是固定值，或者拿重传报文的 RTT 去估算 RTO**：RTO 基于平滑 RTT 和抖动动态计算；而**重传的报文不参与 RTT 采样**（Karn 算法），否则「重传 → RTT 采样偏大 → RTO 变大 → 下次重传更慢」会形成恶性循环。",
            "**把快速重传和超时重传混为一谈**：快速重传的触发条件是**3 个重复 ACK**，说明后面的包都到了、中间那个大概率丢了，可以立刻补；超时重传要等到 RTO 到期，慢一个数量级。丢包场景下后者是保底，前者才是常态。",
            "**以为 TCP 会自动分割消息边界**：TCP 是**面向字节流**的，没有「一次 send 对应一次 recv」的保证。两次 `send` 可能被一次 `recv` 读完（粘包），一次 `send` 也可能被拆成多次 `recv`（拆包）。应用层必须自己定边界：**长度前缀**（先发 4 字节长度）、**固定分隔符**（如 `\\n`）、或**定长消息**。",
        ],
        "task": "",
        "setup": "",
        "starter": "",
        "hint": "",
        "quizzes": [
            {
                "type": "choice",
                "stem": "面试官问：TCP 靠哪些机制保证可靠传输？",
                "options": [
                    "序号 + 累积确认 + 超时重传 + 快速重传/SACK + 滑动窗口 + 校验和，靠这套组合把不可靠的 IP 服务包装成有序的字节流",
                    "靠 IP 层保证，TCP 只负责加端口号",
                    "靠三次握手和四次挥手，握手保证了后续数据一定不会丢",
                    "靠应用层重发，TCP 只做连接管理和流量控制",
                ],
                "answer_index": 0,
                "explanation": (
                    "A 是完整答案，面试时最好按「怎么发现丢包（确认/重复 ACK）→ 怎么补救（重传）→ 怎么提高效率（窗口）」的顺序讲。"
                    "B 和 D 把责任推给了别的层，C 把手握的职责夸大了 —— 握手只同步序号，不保证数据不丢。"
                ),
            },
            {
                "type": "choice",
                "stem": "接收方连续回了 3 个重复 ACK（duplicate ACK），发送方会做什么？",
                "options": [
                    "立刻重传被判定丢失的那个段，不等 RTO 到期 —— 这就是快速重传",
                    "把拥塞窗口直接清零，从慢启动重新开始",
                    "认为连接已失效，主动发送 RST 断开连接",
                    "什么都不做，只把 RTO 翻倍，等超时再重传",
                ],
                "answer_index": 0,
                "explanation": (
                    "3 个重复 ACK 是「后面的段都到了，中间这个丢了」的信号，所以立刻补发。"
                    "重传之后通常会进入**快恢复**阶段（cwnd 减半而不是回到 1），而不是完全清零重来。"
                ),
            },
            {
                "type": "judge",
                "stem": "TCP 是面向字节流的协议，所以应用层必须自己处理消息边界，否则会出现「粘包」问题。",
                "answer": True,
                "explanation": (
                    "TCP 只保证字节有序，不保留你调用 `send` 的边界。"
                    "所以自定义协议要么在消息前加长度字段（最通用，二进制协议基本都这么做），要么用固定的分隔符（如 HTTP 头的 `\\r\\n\\r\\n`）。"
                    "注意：「粘包」不是 TCP 的缺陷，它只是字节流的正常表现。"
                ),
            },
            {
                "type": "blank",
                "stem": "触发快速重传，一般需要收到 ___ 个重复 ACK（duplicate ACK）。",
                "hint": "填一个数字",
                "answer": "3",
                "accept": ["三", "3 个"],
                "explanation": (
                    "这个数字是「反应速度」和「抗乱序误判」的折中：网络本身会乱序，收到一两个乱序段属于正常现象，"
                    "所以等攒到 3 个再判定丢失。这个阈值在 Linux 里由 `tcp_reordering` 之类的参数间接影响。"
                ),
            },
            {
                "type": "short",
                "stem": "面试官问：TCP 里说的「粘包」是怎么回事？你会在应用层怎么解决？",
                "keywords": ["字节流", "边界", "长度前缀", "分隔符", "定长", "协议设计"],
                "reference": (
                    "先纠正概念：**TCP 没有「包」**，它是面向字节流的，只是把一串字节按序送过去，"
                    "既不保留 `send` 的次数，也不保留每次的长度。所以「粘包」是应用层用「一次 recv 就该拿到一条完整消息」"
                    "的错误假设去读数据的必然结果 —— 两次 `send` 可能被一次 `recv` 读走，一次 `send` 也可能被拆成几次 `recv`。\n"
                    "解决办法只有一个方向：**在应用层协议里显式定义消息边界**。三种常见做法：\n"
                    "1. **长度前缀**（最通用）：消息头固定 4 字节存 body 长度，先读头、解析出长度、再循环读满 body；"
                    "2. **分隔符**：约定一个不会出现在正文里的分隔符（HTTP 用 `\\r\\n\\r\\n` 分头，用 `Content-Length` 或 chunked 定体长）；"
                    "3. **定长消息**：每条消息长度固定，不够就补位，简单但浪费带宽。\n"
                    "另外要提一句：接收端的读循环必须处理「**半包**」——读到的字节数可能小于期望，要攒够再解析，"
                    "不能假设一次 `recv` 就给全。"
                ),
                "explanation": (
                    "这题特别能区分「知道名词」和「真的写过协议」：能主动说出半包处理和读循环怎么写，就是后者。"
                ),
            },
        ],
    },
    # ------------------------------------------------------------------ net-05
    {
        "code": "net-05",
        "subject": "计算机网络",
        "stage": "传输层",
        "runner": "none",
        "title": "流量控制与拥塞控制",
        "summary": "一个管收得下，一个管扛得住",
        "definition": (
            "这两个机制都体现为「窗口」，但解决的是完全不同的问题：\n\n"
            "**流量控制（flow control）——解决「接收方处理不过来」**\n\n"
            "接收方在 TCP 头部里用自己的**接收窗口 rwnd** 告诉发送方「我缓冲区还能再收多少字节」，"
            "发送方的在途数据不能超过它。当 `rwnd = 0` 时发送方必须停发；"
            "如果接收方后来腾出了空间，会发一个窗口更新通知；**这个通知一旦丢失，双方就会互相干等（零窗口死锁）**，"
            "所以发送方会启动**持续计时器**周期性发**窗口探测**，逼对方重发窗口信息。\n\n"
            "**拥塞控制（congestion control）——解决「网络处理不过来」**\n\n"
            "发送方自己维护一个**拥塞窗口 cwnd**，通过丢包和时延间接推断网络是否拥塞，然后调整发送速率。四个阶段：\n\n"
            "1. **慢启动**：`cwnd` 从很小的值（传统 1 个 MSS）开始，每收到一个 ACK 就 +1 MSS，效果是**每个 RTT 翻倍**，指数增长，直到 `ssthresh`。\n"
            "2. **拥塞避免**：超过 `ssthresh` 后改为**每个 RTT 加约 1 个 MSS**，线性增长 —— 这就是 AIMD 里的「加法增」。\n"
            "3. **快重传**：收到 3 个重复 ACK 立刻重传丢失的段。\n"
            "4. **快恢复**：不回到慢启动，而是把 `ssthresh` 和 `cwnd` 减半，直接从拥塞避免继续 —— 这就是 AIMD 里的「乘法减」。\n\n"
            "**发送方实际能发多少 = min(rwnd, cwnd)**，取两者中更小的那个。\n\n"
            "算法演进：**Reno**（一个丢包就减半，AIMD 的经典实现）→ **CUBIC**（用三次函数增长，在高带宽长距离环境下恢复更快，"
            "长期是 Linux 默认）→ **BBR**（Google 提出，用带宽和最小 RTT 建模，**不把丢包当拥塞信号**，在有随机丢包的链路上表现更好）。"
        ),
        "plain": (
            "**流量控制 = 杯子的容量，拥塞控制 = 水管的粗细。**\n\n"
            "你往杯子里倒水。杯子里快满了，杯子（接收方）会喊「慢点，我这只能再盛 100 毫升了」——"
            "这是**流量控制**，管的是接收方的缓冲能力。杯子满了你还硬倒，水只会洒出来，所以必须停。\n\n"
            "但就算杯子无限大，**水管本身**就那么大，你拧到最大也没用，反而会在中间某处爆管（路由器丢包、排队时延飙升）。"
            "你看不到水管，只能靠「有没有水洒出来」（丢包）和「水是不是流得慢了」（RTT 变大）来猜——"
            "这是**拥塞控制**，管的是整条路径的承载能力。\n\n"
            "**为什么「慢启动」名字有误导性？** 它的「慢」说的是**起点小**（从 1 个 MSS 开始试探），"
            "但增长速度是**每 RTT 翻倍**的指数级。所以丢包一次要减半、重新爬回去却很快 ——"
            "整个 `cwnd` 曲线是一个不断从高点掉下来再锯齿式爬升的形状，这就是经典的 AIMD 锯齿。"
        ),
        "example": (
            "cwnd 随时间的变化（ssthresh 初始为 8 个 MSS）：\n"
            "\n"
            "RTT 轮次    阶段          动作                      cwnd\n"
            "  1        慢启动        从 1 开始                  1\n"
            "  2        慢启动        每个 RTT 翻倍指数量         2\n"
            "  3        慢启动                                  4\n"
            "  4        慢启动                                  8   ← 达到 ssthresh\n"
            "  5        拥塞避免      每个 RTT +1（线性）         9\n"
            "  6        拥塞避免                                10\n"
            "  7        拥塞避免                                11\n"
            "  8        ✗ 3 个重复 ACK → 快重传 → 快恢复\n"
            "            ssthresh = cwnd/2 = 5, cwnd = 5（减半，不回到 1）\n"
            "  9        拥塞避免      从 5 继续线性增长            6\n"
            " 10        拥塞避免                                 7\n"
            "  ……       不断重复「线性爬升 → 丢包减半」的锯齿\n"
            "\n"
            "如果第 8 轮不是快速重传而是**超时**重传：\n"
            "  ssthresh = cwnd/2，cwnd 直接回到 1，重新慢启动 —— 惩罚重得多。"
        ),
        "example_output": (
            "最后一行解释了为什么快速重传这么重要："
            "同样是丢包，**快速重传只让 cwnd 减半并继续线性增长**，而**超时重传会把 cwnd 打回 1 重新慢启动**，"
            "吞吐量恢复要慢得多。所以工程上 TCP 时延抖动大，往往就是因为发生了超时重传。"
        ),
        "pitfalls": [
            "**把流量控制和拥塞控制混为一谈**：前者看接收方缓冲区（`rwnd`，由**接收方**在报文头里告诉对方），后者看网络状况（`cwnd`，由**发送方**自己估算）。虽然最后都表现成窗口，但发送窗口取的是 `min(rwnd, cwnd)` —— 说错来源会被立刻追问。",
            "**以为「慢启动」真的很慢**：慢启动的「慢」只是起点小，实际是**指数增长**（每个 RTT 翻倍）。把慢启动描述成缓慢增长是最典型的背反了。",
            "**笼统地说「TCP 一丢包就把窗口减半」**：那是 **Reno** 的行为。Linux 长期默认的 **CUBIC** 在高 BDP 场景下恢复曲线不是简单减半，**BBR** 更是根本不把丢包当作拥塞信号。答题时先说明算法，再讲行为。",
            "**忽略零窗口死锁**：接收方 `rwnd=0` 后，如果「窗口更新」这个报文丢了，双方会陷入互相等待——接收方以为发送方知道窗口开了，发送方以为对方还没腾出空间。所以发送方必须有**持续计时器**定期发**窗口探测**报文，逼对方重发窗口信息。",
        ],
        "task": "",
        "setup": "",
        "starter": "",
        "hint": "",
        "quizzes": [
            {
                "type": "choice",
                "stem": "面试官问：流量控制和拥塞控制有什么区别？",
                "options": [
                    "流量控制看的是接收方缓冲区还能不能收（rwnd，接收方告知），拥塞控制看的是网络路径还能不能承受（cwnd，发送方靠丢包和时延自己估），实际发送窗口取两者的最小值",
                    "流量控制管发送方向接收方发的数据量，拥塞控制管接收方向发送方发的数据量",
                    "两者是一个东西的两种叫法，只是在不同 TCP 实现里的名字不同",
                    "流量控制是在传输层实现的，拥塞控制是在网络层由路由器实现的",
                ],
                "answer_index": 0,
                "explanation": (
                    "B 把「双向」当成区别，实际上两个机制都作用在发送方向上；"
                    "C 说成同义词是硬伤；D 的关键错误在于 —— **拥塞控制完全由端主机实现**，"
                    "路由器不参与（只做被动丢包），这正是「为什么网络公平性要靠端侧自律」的原因。"
                ),
            },
            {
                "type": "choice",
                "stem": "慢启动阶段，拥塞窗口 cwnd 是怎么增长的？",
                "options": [
                    "每收到一个 ACK 就把 cwnd 加 1 个 MSS，效果是每个 RTT 翻一倍 —— 指数增长",
                    "每个 RTT 只增加 1 个 MSS —— 线性增长",
                    "直接跳到 ssthresh 的值，然后开始线性增长",
                    "保持不变，直到收到重复 ACK 才调整",
                ],
                "answer_index": 0,
                "explanation": (
                    "「每个 ACK +1 MSS」听起来是加法，但因为一个 RTT 内会收到 cwnd 个 ACK，累加起来就是翻倍，"
                    "所以整体是指数增长。B 描述的是**拥塞避免**阶段的加法增长；C 跳过了「试探」这个核心目的。"
                ),
            },
            {
                "type": "judge",
                "stem": "TCP 的拥塞控制完全由端主机实现，路由器不参与决策；发送方只能靠丢包和 RTT 变化间接推断网络是否拥塞。",
                "answer": True,
                "explanation": (
                    "这是 TCP 拥塞控制最重要的一个约束，也是它的根本局限："
                    "路由器只做被动排队和丢包，不告诉主机网络状况，所以端主机只能「猜」——"
                    "经典算法把**丢包**当拥塞信号，这在无线链路（本身就会随机丢包）上会误判，"
                    "这也是 BBR 改用带宽和时延建模的动机。"
                ),
            },
            {
                "type": "blank",
                "stem": "在拥塞避免阶段，cwnd 大约每个 RTT 增加 ___ 个 MSS。",
                "hint": "填一个数字",
                "answer": "1",
                "accept": ["一", "1 个"],
                "explanation": (
                    "这就是 AIMD 里的「加法增」：每个 RTT 只加 1 个 MSS，线性缓慢上升，用来精细探测可用带宽上限。"
                    "与之配对的是丢包时的「乘法减」（窗口减半）。"
                ),
            },
            {
                "type": "short",
                "stem": "面试官问：既然有 TCP 拥塞控制，为什么还会出现 BBR 这类算法？它解决了什么问题？",
                "keywords": ["丢包不等于拥塞", "带宽时延积", "RTT", "缓冲区膨胀", "无线链路", "建模"],
                "reference": (
                    "经典算法（Reno/CUBIC）把**丢包等同于拥塞**，这个假设在两个场景下失效：\n"
                    "1. **有随机丢包的链路**：Wi-Fi、蜂窝网络、长距离光缆都会因为噪声或误码丢包，"
                    "此时丢包不代表网络真的拥塞，但 CUBIC 还是会大幅降低速率，导致带宽利用率很低；\n"
                    "2. **缓冲区膨胀（bufferbloat）**：路径中间有大的路由器缓冲区时，即使已经排队很久、RTT 已经飙升，"
                    "也要等到缓冲区真的溢出才开始丢包 —— 这时算法才发现拥塞，但用户早就感受到卡顿了。\n"
                    "BBR 的思路是换一套推断依据：不把丢包当信号，而是持续测量「**瓶颈带宽**」和「**最小 RTT**」，"
                    "把发送速率控制在两者的乘积（带宽时延积 BDP）附近，主动维持一个不大的排队量。"
                    "好处是在有随机丢包的链路上能跑满带宽、并且不容易把缓冲区撑爆，因此 RTT 更稳定。\n"
                    "代价是 BBR 与基于丢包的流共存时更激进，会挤占 CUBIC 流的带宽，公平性一直是它被讨论的点。"
                ),
                "explanation": (
                    "这题的关键是「丢包 ≠ 拥塞」这一个判断。能说清前提失效的场景，比记住 BBR 的全称有价值得多。"
                ),
            },
        ],
    },
    # ------------------------------------------------------------------ net-06
    {
        "code": "net-06",
        "subject": "计算机网络",
        "stage": "传输层",
        "runner": "none",
        "title": "UDP 与 TCP 怎么选",
        "summary": "一个什么都替你做，一个什么都不管",
        "definition": (
            "两者的差别可以逐项对照：\n\n"
            "| 维度 | TCP | UDP |\n"
            "| --- | --- | --- |\n"
            "| 连接 | 面向连接（三次握手、四次挥手） | 无连接，直接发 |\n"
            "| 可靠性 | 确认 + 重传，保证有序、不重复 | 不保证送达、不保证顺序、可能重复 |\n"
            "| 传输单位 | **面向字节流**，无消息边界 | **面向报文**，保留边界（一次 sendto 对应一次 recvfrom） |\n"
            "| 首部开销 | 20 字节起（含选项最多 60） | **固定 8 字节** |\n"
            "| 流量/拥塞控制 | 都有 | 都没有 |\n"
            "| 时延特性 | 有重传和退让，抖动大 | 无重传，时延平稳（但可能丢） |\n"
            "| 通信方式 | 只能一对一 | 支持一对一、一对多（组播）、广播 |\n\n"
            "**选型的基本判断**：\n\n"
            "- **要 TCP**：任何「一个字节都不能错、不能少、不能乱」的场景 —— 文件传输、HTTP/1.1 与 HTTP/2、数据库连接、SSH、SMTP。\n"
            "- **要 UDP**：**「晚到不如不到」**的场景 —— DNS 查询、DHCP、实时音视频、在线游戏的位置同步、SNMP；"
            "以及**需要在用户态自己实现可靠传输**的场景 —— QUIC（HTTP/3）、各种自研 RPC 框架。\n\n"
            "**关键认知**：UDP 不是「不可靠的协议」，而是「**不替你做可靠性**的协议」。"
            "QUIC 就是最好的证据 —— 它跑在 UDP 之上，在用户态重新实现了确认、重传、流控、拥塞控制，"
            "还顺带解决了 TCP 层的队头阻塞和握手时延问题。"
        ),
        "plain": (
            "**TCP 像挂号信**：要签收，丢了会重寄，收件顺序一定和你寄的顺序一样。但你得等，而且寄得慢。\n\n"
            "**UDP 像往墙上扔明信片**：扔出去就不管了，会不会到、按不按顺序到，全看运气。"
            "但胜在快、稳、不折腾。\n\n"
            "**举个视频通话的例子就懂了**：第 3 帧的画面丢了，如果像 TCP 那样停下来等重传，"
            "你看到的会是画面卡住然后突然跳出几帧——体验比直接丢掉这一帧、继续播第 4 帧差得多。"
            "实时音视频的哲学是「**宁可画面有点花，也不能卡**」，所以用 UDP。\n\n"
            "反过来，传一个安装包，少一个字节都装不上，那就必须等 TCP 把数据补齐。"
            "**所以选型的标准不是「哪个更快」，而是「丢一点数据能不能接受」。**"
        ),
        "example": (
            "用同一个场景对比两种选择：\n"
            "\n"
            "【场景一】下载一个 500MB 的安装包\n"
            "  TCP：分包有序到达，校验和验证，缺任何一个字节就重传补齐\n"
            "  结果：慢一点，但文件一定完整\n"
            "  → 必须 TCP\n"
            "\n"
            "【场景二】实时语音通话，每 20ms 发一个语音包\n"
            "  UDP：直接发，不等确认\n"
            "  丢了第 37 个包？直接丢掉，接着播第 38 个（丢包补偿算法去猜一下）\n"
            "  如果换成 TCP：第 37 个包丢失 → 后面的 38、39 全部卡在接收缓冲区等重传\n"
            "  → 用户听到的是一段几百毫秒的静默，然后突然加速播放\n"
            "  → 必须 UDP\n"
            "\n"
            "【场景三】DNS 查询「example.com 的 IP 是多少」\n"
            "  一个 UDP 包问、一个 UDP 包答，一问一答，总共 1 个 RTT\n"
            "  如果用 TCP：先三次握手（1 RTT）→ 再问 → 再四次挥手\n"
            "  就为了查一个 IP，先付 1 个 RTT 的建连成本不划算\n"
            "  → 默认 UDP，响应被截断（TC=1）或区域传送时才切 TCP"
        ),
        "example_output": (
            "三个场景的选择依据是完全一致的：**「丢包的代价」和「等待的代价」哪个更大。**\n"
            "安装包不能容忍丢包 → TCP；实时通话不能容忍等待 → UDP；DNS 只是不想付建连成本 → UDP。"
            "注意第三个场景里，如果响应体积超过了 UDP 能承载的限度，标准做法是**先用 UDP 试，"
            "被截断了再用 TCP 重来一次**，而不是为了保险一律走 TCP。"
        ),
        "pitfalls": [
            "**觉得「UDP 不可靠所以不能用在正经业务里」**：UDP 只是不在内核里替你实现可靠性。QUIC、KCP 这类协议在用户态把可靠性、拥塞控制全做了一遍，反而比 TCP 更灵活（可以自选重传策略、更快握手、更好的多路复用）。",
            "**把「面向报文」和「面向字节流」搞混**：UDP 一次 `sendto` 就是一个完整数据报，一次 `recvfrom` 就拿到它，**应用层不用处理粘包**；TCP 是字节流，必须自己定消息边界。这一点常被当成 TCP 的优点，其实是取舍。",
            "**以为 UDP 一定比 TCP 快**：两者都跑在 IP 之上，走同样的路径。UDP 的优势是**不重传、不等待、时延抖动小**，而不是「物理上更快」。而且 UDP 没有拥塞控制，把它塞满带宽照样丢包，还会挤占公平共存的 TCP 流。",
            "**忽略 UDP 的 IP 分片问题**：超过 MTU 的 UDP 数据报会被 IP 层分片，**任何一片丢失都会导致整个数据报作废**，重传成本反而更高。所以基于 UDP 的协议都要自己在应用层控制包大小（QUIC 就为此设计了路径 MTU 探测，并禁止 IP 分片）。",
        ],
        "task": "",
        "setup": "",
        "starter": "",
        "hint": "",
        "quizzes": [
            {
                "type": "choice",
                "stem": "面试官问：做一个实时语音通话，你选 TCP 还是 UDP？为什么？",
                "options": [
                    "选 UDP：实时音视频追求低时延，宁可丢一帧也不能因为等重传让整条流卡住；可靠性交给应用层的丢包补偿和抗丢包编码去处理",
                    "选 TCP：语音数据不能丢，必须保证每帧都完整到达",
                    "选 TCP：因为 UDP 会被运营商限速，通话质量没保障",
                    "选 UDP：因为 UDP 比 TCP 快，传输速率更高",
                ],
                "answer_index": 0,
                "explanation": (
                    "B 是最典型的错误答案 —— 它把「可靠性」当成了唯一指标，忽略了实时性的优先级。"
                    "C 的现象（运营商限制 UDP）确实存在，但选型依据不是这个。"
                    "D 的「UDP 更快」是错觉，UDP 的快来自**不等重传**，不是链路速率更高。"
                    "补充加分点：现在不少实时通信会走 **QUIC/WebRTC（基于 UDP）**，即在 UDP 之上自己做一套更可控的可靠性策略。"
                ),
            },
            {
                "type": "choice",
                "stem": "UDP 首部固定是 8 字节，那么 TCP 首部在不含选项时是多少字节？",
                "options": [
                    "20 字节",
                    "8 字节",
                    "12 字节",
                    "60 字节",
                ],
                "answer_index": 0,
                "explanation": (
                    "TCP 固定首部 20 字节（含选项最多可到 60 字节），UDP 首部固定 8 字节，"
                    "这也是 UDP 头部开销小的由来。"
                    "顺手机一个数字：一个不带数据的 TCP ACK 报文，加上 IP 头的 20 字节和以太网头的 14 字节，"
                    "光开销就是 54 字节。"
                ),
            },
            {
                "type": "judge",
                "stem": "UDP 没有拥塞控制机制，因此在公网上大量使用 UDP 传输有可能挤占其他 TCP 流的带宽。",
                "answer": True,
                "explanation": (
                    "TCP 流的公平性靠「大家都自觉退让」来维持，UDP 不参与这个博弈。"
                    "所以 QUIC 虽然跑在 UDP 上，仍然在用户态实现了拥塞控制 —— 这既是技术要求，也是「做互联网好公民」的要求。"
                    "实际网络中，运营商和 IDC 对异常 UDP 流量的限速/QoS 降级，就是这个问题的直接后果。"
                ),
            },
            {
                "type": "blank",
                "stem": "在 TCP 首部不携带任何选项的情况下，其长度是 ___ 字节。",
                "hint": "填一个数字",
                "answer": "20",
                "accept": ["二十", "20 字节"],
                "explanation": (
                    "TCP 固定首部 20 字节，选项字段最多再占 40 字节，所以最大是 60 字节。"
                    "常用的选项有时间戳（用于更准确的 RTT 测量，也是 `tcp_tw_reuse` 的前提）、MSS、窗口缩放、SACK 允许。"
                ),
            },
            {
                "type": "short",
                "stem": "面试官问：DNS 查询明明可以用 UDP，为什么响应很大或者区域传送的时候要改用 TCP？",
                "keywords": ["512", "TC 标志", "截断", "TCP 53", "区域传送", "可靠"],
                "reference": (
                    "DNS 默认用 **UDP 53**，因为它是一问一答的短报文，UDP 省掉了三次握手的 RTT。"
                    "但 UDP 单个数据报能承载的 DNS 响应在原始规范里被限制在 **512 字节**，超过就装不下。\n"
                    "处理方式是：服务端发现响应超过能承载的长度时，会把响应**截断**并置上 **TC（Truncated）标志**；"
                    "客户端看到 TC=1 就知道「UDP 装不下」，于是**改用 TCP 53 重发同一个查询**，TCP 的字节流没有长度上限，"
                    "自然能装下。EDNS0 扩展把 UDP 的上限提到了 1232~4096 字节，减少了走 TCP 的次数，但没有取消这个机制。\n"
                    "**区域传送（AXFR/IXFR）** 则是另一种情况：它是主从 DNS 服务器之间同步整个区文件，"
                    "数据量大、且必须完整可靠（差一条记录就是解析错误），所以标准直接就规定必须用 TCP。\n"
                    "一句话总结：**UDP 用于「小、快、丢了能重问」的普通查询，TCP 用于「大、必须完整」的场景。**"
                ),
                "explanation": (
                    "这题的价值在于「同一个协议里也做了分层取舍」这个视角：DNS 没有一刀切，而是按响应大小动态选择传输层。"
                ),
            },
        ],
    },
    # ------------------------------------------------------------------ net-07
    {
        "code": "net-07",
        "subject": "计算机网络",
        "stage": "应用层与安全",
        "runner": "none",
        "title": "HTTP 报文与常见状态码",
        "summary": "请求和响应长什么样，状态码怎么读",
        "definition": (
            "**HTTP 是纯文本的请求—响应协议**，报文格式固定：\n\n"
            "请求报文 = **请求行** + 请求头 + 空行 + 请求体\n"
            "响应报文 = **状态行** + 响应头 + 空行 + 响应体\n\n"
            "那个**空行（`\\r\\n\\r\\n`）是分隔头和体的关键**，解析器就是靠它判断头部结束的。\n\n"
            "请求行是 `方法 路径 版本`；方法里语义差别最大的是：\n\n"
            "- **GET**：获取资源，**幂等**（多次执行结果相同）、可缓存、参数在 URL 里\n"
            "- **POST**：提交数据，**非幂等**（重复提交会重复创建）、一般不缓存\n"
            "- **PUT / DELETE**：按语义是幂等的，用来替换/删除资源\n"
            "- **PATCH**：局部更新，通常不幂等\n"
            "- **HEAD / OPTIONS**：只取头部、探测服务端支持的方法（CORS 预检就用 OPTIONS）\n\n"
            "状态码按首位分五类：**1xx 信息、2xx 成功、3xx 重定向、4xx 客户端错误、5xx 服务端错误**。\n\n"
            "面试里真正高频的是这几个：\n\n"
            "| 状态码 | 含义 | 关键点 |\n"
            "| --- | --- | --- |\n"
            "| 200 / 201 / 204 | 成功 / 已创建 / 成功无内容 | 204 不带 body |\n"
            "| 301 | 永久重定向 | 浏览器和搜索引擎会缓存并换索引 |\n"
            "| 302 | 临时重定向 | 每次还会来问；历史上可能把 POST 变成 GET |\n"
            "| 307 / 308 | 临时/永久重定向，**保持原方法和 body** | 需要重发 POST 时用它 |\n"
            "| 304 | 资源未修改 | **只有响应头，没有 body** |\n"
            "| 400 / 401 / 403 / 404 / 405 / 429 | 参数错误 / **未认证** / **无权限** / 不存在 / 方法不允许 / 限流 | 401 和 403 最容易答错 |\n"
            "| 500 / 502 / 504 | 服务端异常 / 网关拿到非法响应 / 网关等待上游超时 | 502、504 通常是网关与后端之间的问题 |\n\n"
            "另外两个头字段必须知道：`Content-Length`（体有多长）和 `Transfer-Encoding: chunked`"
            "（长度未知时分块流式发送）—— **这两个不能同时出现**，否则会造成请求走私漏洞。"
        ),
        "plain": (
            "HTTP 报文就是**一封信的固定格式**。信纸最上面先写「寄给谁、要办什么事」（请求行），"
            "然后是几行备注（头部：内容类型、长度、认证信息），**空一行**之后才开始写正文（body）。\n\n"
            "**状态码就是你收到回执时看的第一眼**：\n\n"
            "- `2xx`：办成了\n"
            "- `3xx`：地址变了，去别的地方办\n"
            "- `4xx`：**你这封信写得有问题**（参数错了、没带证件、没权限、找不到人）\n"
            "- `5xx`：**对方内部出问题了**，跟你没关系，可以重试\n\n"
            "**记状态码最省力的方法是按责任方分**，而不是背数字：拿到一个错误码先问「这是谁的锅」。"
            "401 和 403 之所以最容易搞混，也是因为都在「你这边的锅」里 —— 区别是 **401 是「你没证明你是谁」，"
            "403 是「我知道你是谁了，但你不许进」**。"
        ),
        "example": (
            "【一个完整的 GET 请求】\n"
            "GET /api/users?id=1 HTTP/1.1\n"
            "Host: example.com\n"
            "User-Agent: curl/8.0\n"
            "Accept: application/json\n"
            "If-None-Match: \"a1b2c3\"\n"
            "\n"
            "（GET 没有 body，但那个空行必须存在）\n"
            "\n"
            "【对应的 200 响应】\n"
            "HTTP/1.1 200 OK\n"
            "Content-Type: application/json; charset=utf-8\n"
            "Content-Length: 27\n"
            "Cache-Control: max-age=3600\n"
            "\n"
            "{\"name\": \"小明\", \"age\": 18}\n"
            "\n"
            "【如果缓存还有效，服务端会这样回】\n"
            "HTTP/1.1 304 Not Modified\n"
            "ETag: \"a1b2c3\"\n"
            "\n"
            "（没有 body，客户端直接用本地缓存）\n"
            "\n"
            "【如果路径换了域名】\n"
            "HTTP/1.1 301 Moved Permanently\n"
            "Location: https://new.example.com/api/users?id=1\n"
            "\n"
            "【流式响应：长度未知，边生成边发】\n"
            "HTTP/1.1 200 OK\n"
            "Transfer-Encoding: chunked\n"
            "Content-Type: text/event-stream\n"
            "\n"
            "1a\\r\\n"
            "data: {\"token\": \"你\"}\\n\\n\\r\\n"
            "0\\r\\n\\r\\n"
            "（最后以长度为 0 的块表示结束）"
        ),
        "example_output": (
            "可以看出：响应体的有无、长度怎么算，完全由头部决定 —— "
            "`Content-Length` 明确给了长度就按长度读；`Transfer-Encoding: chunked` 就按块读直到读到 0 块；"
            "而 `304`、`204` 这类根本没有 body，读 body 会直接卡住或报错。"
            "大模型流式输出的 SSE，走的就是 `chunked` 这一套。"
        ),
        "pitfalls": [
            "**301 和 302 用混，301 被浏览器永久缓存**：换域名、做 HTTPS 强制跳转用 **301**（搜索引擎会更新索引）；临时维护、A/B 测试用 **302**。**301 一旦被浏览器和 CDN 缓存，撤都撤不掉**，所以不确定时先上 302。需要保留 POST 方法和 body 的重定向（比如换域名后仍要提交表单）必须用 **307/308**，因为 302 在历史实现上可能把 POST 降级成 GET。",
            "**401 和 403 分不清**：401 Unauthorized 名字起得有误导性，它实际是「**未认证**」（没带 token 或 token 无效，正确反应是去登录）；403 Forbidden 是「**已认证但没有权限**」（登录了依然不让看，再登录也没用）。429 是「请求太频繁，被限流了」，和权限无关。",
            "**以为 304 会返回响应体**：304 Not Modified **只有响应头，没有 body**，客户端直接使用本地缓存，这样才省流量。它的触发靠请求里的 `If-None-Match`（配合 `ETag`）或 `If-Modified-Since`（配合 `Last-Modified`）条件请求。",
            "**忽略 `Content-Length` 与 `Transfer-Encoding: chunked` 的冲突**：两者同时出现是非法的，不同中间件对「该信哪一个」的判断不一致，会被利用来构造**请求走私（request smuggling）**。长度已知就用 `Content-Length`，长度未知或需要边生成边发（流式响应）就用 `chunked`，绝不混用。",
        ],
        "task": "",
        "setup": "",
        "starter": "",
        "hint": "",
        "quizzes": [
            {
                "type": "choice",
                "stem": "面试官问：301 和 302 有什么区别？网站整体换了域名，应该用哪个？",
                "options": [
                    "301 是永久重定向，浏览器和搜索引擎会长期缓存并把权重转移到新地址；302 是临时重定向，每次还会回源确认。整体换域名用 301",
                    "301 是临时重定向，302 是永久重定向，换域名用 302",
                    "两者完全等价，只是历史遗留的两个编号，随便用哪个都行",
                    "301 只对搜索引擎生效，302 只对浏览器生效",
                ],
                "answer_index": 0,
                "explanation": (
                    "B 把两者说反了，这是最常出现的错误。C、D 纯属编造。"
                    "再加一句加分点：换域名同时还要保证 POST 请求不被降级，这种场景应该用 **308**（永久 + 保持方法），"
                    "因为 301/302 在历史上可能把 POST 改成 GET。"
                ),
            },
            {
                "type": "choice",
                "stem": "接口返回 403 Forbidden，说明什么？",
                "options": [
                    "请求已经通过了身份认证，但当前用户没有权限访问这个资源",
                    "用户没有登录，或者携带的 token 已经失效",
                    "服务器内部处理时抛出了未捕获的异常",
                    "请求的资源在服务器上不存在",
                ],
                "answer_index": 0,
                "explanation": (
                    "**403 是「已认证但无权限」**：比如普通用户去访问管理员接口。"
                    "B 对应 **401**（未认证）；C 对应 **500**；D 对应 **404**。"
                    "顺带提一个工程细节：有些系统为了避免暴露资源是否存在，会对无权限的资源统一返回 404 而不是 403。"
                ),
            },
            {
                "type": "judge",
                "stem": "304 Not Modified 响应里不包含响应体，客户端会直接使用本地缓存。",
                "answer": True,
                "explanation": (
                    "这正是 304 存在的意义：**只是告诉客户端「缓存还能用」**，省掉一次完整的 body 传输。"
                    "它靠请求头 `If-None-Match` / `If-Modified-Since` 触发条件请求，服务端比对 `ETag` / `Last-Modified` 后决定回 200 还是 304。"
                ),
            },
            {
                "type": "blank",
                "stem": "表示「资源已永久移动到新地址」的标准状态码是 ___。",
                "hint": "填一个三位数字",
                "answer": "301",
                "accept": ["301 ", "301 Moved Permanently"],
                "explanation": (
                    "301 Moved Permanently，配合响应头 `Location` 给出新地址。"
                    "对应的临时版本是 302，保持方法的永久/临时版本是 308/307。"
                ),
            },
            {
                "type": "short",
                "stem": "面试官问：GET 和 POST 有什么区别？",
                "keywords": ["语义", "幂等", "安全方法", "缓存", "重试", "请求体"],
                "reference": (
                    "面试官问这题时，最想听的是**幂等性**，而不是「GET 有长度限制、POST 没有」这种已经过时的说法"
                    "（长度限制来自浏览器和服务器实现，不是 HTTP 规范规定的）—— 张口就说长度限制会被认为知识陈旧。\n"
                    "正确的答法是按 RFC 的语义讲：\n"
                    "1. **语义不同**：GET 是「获取」一个资源，不应该改变服务端状态（规范上叫**安全方法**）；POST 是「提交」数据，"
                    "会让服务端产生副作用（创建资源、下单、支付）。\n"
                    "2. **幂等性不同**：GET 幂等 —— 重复执行多少次，服务端状态都一样，所以**可以安全地缓存和自动重试**；"
                    "POST 非幂等 —— 重复提交会重复创建，所以浏览器会弹「是否重新提交表单」，"
                    "微服务里做重试也必须配合**幂等键（idempotency key）**才敢重试 POST。\n"
                    "3. **数据位置与缓存**：GET 参数在 URL 上，会被浏览器历史、代理日志、CDN 记录下来，所以**绝不能放敏感信息**；"
                    "POST 数据在 body 里，默认不被缓存（除非显式设置）。\n"
                    "4. **实际工程**：明确 POST 用于「需要幂等保证的写操作」时，应该由业务层加唯一请求 ID 去重，而不是靠传输层。"
                ),
                "explanation": (
                    "「GET 长度有限制、POST 没有」是这道题最经典的陷阱式回答。把话题引到幂等性和重试语义上，答案立刻就上了层次。"
                ),
            },
        ],
    },
    # ------------------------------------------------------------------ net-08
    {
        "code": "net-08",
        "subject": "计算机网络",
        "stage": "应用层与安全",
        "runner": "none",
        "title": "从 HTTP/1.1 到 HTTP/3",
        "summary": "三次大版本升级，一直在解决队头阻塞",
        "definition": (
            "**HTTP/1.1**：纯文本协议，一个 TCP 连接上**请求—响应是串行的** —— 上一个响应没回来，下一个请求即使发出去了也得排队等。"
            "虽然规范里有 pipelining，但因为**队头阻塞（head-of-line blocking）**严重，浏览器基本禁用。"
            "实际做法是**开 6 个左右的并发 TCP 连接**绕过去，代价是连接多、握手多、拥塞控制各管各的。"
            "`Keep-Alive` 只是**复用连接**（省掉反复握手），请求依然是串行的。\n\n"
            "**HTTP/2**：核心是**二进制分帧 + 多路复用**。\n\n"
            "- **二进制分帧**：把报文拆成带类型的二进制帧（HEADERS、DATA、SETTINGS…），比文本解析更快、更省、更不易出错\n"
            "- **多路复用**：同一个 TCP 连接上跑多条 **stream**，每条流有 stream id，帧按 id 交错发送、到对端再重组。"
            "于是**一个连接就能并发所有请求**，不用再开 6 个连接\n"
            "- **HPACK 头压缩**：用静态表 + 动态表 + Huffman 编码压缩头部，大量重复头（Cookie、User-Agent）只传一次\n"
            "- **服务端推送、流优先级**：前者实践中效果不佳，Chrome 已经移除\n\n"
            "**HTTP/2 没有解决的：TCP 层队头阻塞。** 所有流共享同一个 TCP 连接，"
            "只要有一个 TCP 段丢了，**内核必须等它重传完成才能把后续字节交给 HTTP/2 层**，"
            "于是所有流全被卡住。这是 HTTP/2 最被诟病的缺陷。\n\n"
            "**HTTP/3**：把传输层从 TCP 换成 **QUIC（基于 UDP）**，在用户态实现了可靠传输、流控和拥塞控制。"
            "QUIC 的流是**真正互相独立**的：一个流的数据丢了只阻塞那一条流，其他流照常交付 —— 这才彻底解决了队头阻塞。"
            "另外 QUIC 把 TLS 1.3 握手并进了传输握手（首次 1-RTT，恢复会话 0-RTT），"
            "并且用 **Connection ID** 标识连接，换网络、换 IP 时连接不断（连接迁移）。"
        ),
        "plain": (
            "把 HTTP/1.1 想成**一条单车道**：车（请求）只能一辆一辆过，前车抛锚，后面全堵着。"
            "浏览器的对策是「我再多修 6 条车道」，堵是缓解了，但修路本身也要成本（握手、TLS、拥塞窗口各自从零开始爬）。\n\n"
            "**HTTP/2 是「多车道 + 按车流编号」**：所有车在同一条路上交错前进，各自带编号，"
            "到了终点按编号重新拼装。单车道变成多车道，这是巨大提升。\n"
            "**但注意：路只有一条。** 只要路面塌了一个坑（丢了一个 TCP 段），整条路的车全部停下来等修好 —— "
            "这就是「HTTP/2 解决了应用层队头阻塞，但没解决 TCP 层队头阻塞」的通俗版本。\n\n"
            "**HTTP/3 的做法是「不走这条路了，改走空运」**（QUIC/UDP）：每架飞机（流）独立飞，"
            "一架延误不影响别的飞机。代价是空管系统要自己重建（可靠传输、拥塞控制在用户态重新实现了一遍），"
            "而且有些网络会限制 UDP 流量 —— 这也是 HTTP/3 推广中的真实阻力。"
        ),
        "example": (
            "三个版本在相同场景下的行为对比（一次页面加载要发 5 个请求）：\n"
            "\n"
            "【HTTP/1.1】\n"
            "  连接 1：请求 A ──等待──▶ 响应 A ... 请求 B ...\n"
            "  连接 2：请求 C ...\n"
            "  连接 3：请求 D ...\n"
            "  → 靠开多个连接绕过串行限制，代价是每个连接都要握手 + TLS + 各自爬慢启动\n"
            "\n"
            "【HTTP/2】\n"
            "  同一条 TCP 连接上：\n"
            "    stream 1: ──HEADERS(DATA)──\n"
            "    stream 3: ──HEADERS(DATA)──   帧交错发送，5 个请求同时进行\n"
            "    stream 5: ──HEADERS(DATA)──\n"
            "    stream 7: ──HEADERS(DATA)──\n"
            "    stream 9: ──HEADERS(DATA)──\n"
            "  ✓ 应用层队头阻塞解决：不用排队等上一个响应\n"
            "  ✗ TCP 层队头阻塞仍在：某个 TCP 段丢了 → 内核不交付 → 5 条流全停\n"
            "\n"
            "【HTTP/3】\n"
            "  同一条 QUIC 连接上：\n"
            "    stream 1: ────────\n"
            "    stream 3: ──✗ 丢包，只重传这一条流的数据\n"
            "    stream 5: ──────── ✓ 照常交付\n"
            "    stream 7: ──────── ✓ 照常交付\n"
            "  ✓ 两条队头阻塞都解决"
        ),
        "example_output": (
            "结论一句话就能记住：\n"
            "**HTTP/1.1 的问题是「应用层串行」，HTTP/2 用多路复用解决了它；"
            "HTTP/3 用 QUIC 解决了「TCP 层丢包拖累所有流」这个 HTTP/2 留下的问题。**"
            "另外别忘了 HTTP/3 的附带好处：握手从「TCP 1-RTT + TLS 2RTT」压缩到 1-RTT（恢复会话 0-RTT），"
            "以及换 WiFi/4G 时连接不中断（连接迁移）。"
        ),
        "pitfalls": [
            "**说「HTTP/2 的多路复用彻底解决了队头阻塞」**：这是最常见的失分点。多路复用只解决了**应用层**队头阻塞（请求可以并发下发），**TCP 层的队头阻塞依然存在** —— 所有流共享一个 TCP 连接，一个段丢失会让内核停止向上交付，全部流一起等。这一层要等 **HTTP/3 用 QUIC** 才解决。答题时把「两个队头阻塞」分开说，层次立刻高一级。",
            "**把 Keep-Alive 和多路复用当成一回事**：`Keep-Alive` 只是**复用同一个 TCP 连接**，请求仍然串行；多路复用是**同一时刻并发多个请求**。前者省的是握手成本，后者解决的是排队问题。",
            "**以为 HTTP/3 用 UDP 就等于不可靠**：可靠性由 **QUIC 在用户态实现**（确认、重传、流控、拥塞控制一样不少）。换成 UDP 的动机是：绕过内核 TCP 实现的僵化（改不动、升级慢、无法按流隔离），而不是放弃可靠性。",
            "**忽略 0-RTT 的重放风险**：0-RTT 允许客户端在握手第一个包里就带上应用数据，省一个 RTT，但**这些数据可以被攻击者重放**。所以规范上只允许对**幂等**请求使用 0-RTT —— 拿 0-RTT 发「下单」「转账」这类请求是真实存在的安全坑。",
        ],
        "task": "",
        "setup": "",
        "starter": "",
        "hint": "",
        "quizzes": [
            {
                "type": "choice",
                "stem": "面试官问：HTTP/2 的多路复用和 HTTP/1.1 的 Keep-Alive 有什么区别？",
                "options": [
                    "Keep-Alive 只是复用同一条 TCP 连接，请求仍然串行；多路复用是在同一条连接上并发出多条流（stream），请求—响应可以交错进行、互不等待",
                    "两者都是复用 TCP 连接，区别只是 HTTP/2 的头部压缩更省流量",
                    "Keep-Alive 是客户端行为，多路复用是服务端行为",
                    "Keep-Alive 在 HTTP/2 里被废弃了，因为多路复用已经完全取代了它",
                ],
                "answer_index": 0,
                "explanation": (
                    "A 准确抓住了本质：Keep-Alive 解决「反复握手」的成本问题，多路复用解决「请求要排队」的并发问题。"
                    "B 把差异归到头部压缩上，答错了重点；D 也不对 —— HTTP/2 仍然复用一条 TCP 连接，只是不需要再显式声明 Keep-Alive。"
                ),
            },
            {
                "type": "choice",
                "stem": "TCP 层的队头阻塞，是在哪个版本里被真正解决的？",
                "options": [
                    "HTTP/3，用基于 UDP 的 QUIC 实现流之间的独立传输，一条流丢包不阻塞其他流",
                    "HTTP/2，靠多路复用让每个 stream 独立收发",
                    "HTTP/1.1，靠 Keep-Alive 复用连接",
                    "HTTP/2，靠服务端推送提前把资源发过去",
                ],
                "answer_index": 0,
                "explanation": (
                    "HTTP/2 的所有流共用一条 TCP 连接，内核按字节序交付，所以一个段丢失必然拖住所有流 ——"
                    "**这是 TCP 实现层面的限制，HTTP/2 再怎么设计也绕不过去**。"
                    "HTTP/3 换成 QUIC 后，每条流有独立的序号空间和重传逻辑，才做到真正互不阻塞。"
                ),
            },
            {
                "type": "judge",
                "stem": "HTTP/2 在同一条 TCP 连接上并发多个流，如果其中某个 TCP 段丢失，所有流都要停下来等它重传。",
                "answer": True,
                "explanation": (
                    "这正是「HTTP/2 没有解决 TCP 层队头阻塞」的具体表现。"
                    "在丢包率高的网络（移动网络、跨国链路）上，这个缺陷会把多路复用的收益吃掉大半 ——"
                    "也解释了为什么 HTTP/3 先在高丢包的移动场景被大规模采用。"
                ),
            },
            {
                "type": "blank",
                "stem": "HTTP/3 使用的传输层协议是 ___（它基于 UDP 实现了可靠传输与拥塞控制）。",
                "hint": "填一个英文单词",
                "answer": "QUIC",
                "accept": ["quic", "Quic"],
                "explanation": (
                    "QUIC（Quick UDP Internet Connections）最初由 Google 提出并贡献给 IETF。"
                    "它的关键设计是：把可靠传输、流控、拥塞控制从内核搬到**用户态**，"
                    "这样能快速迭代，也能按流做更细粒度的重传控制。"
                ),
            },
            {
                "type": "short",
                "stem": "面试官问：HTTP/2 为什么要把 HTTP/1.1 的纯文本报文改成二进制分帧？好处是什么？",
                "keywords": ["解析效率", "多路复用", "边界模糊", "头部压缩", "扩展性", "紧凑"],
                "reference": (
                    "改二进制不是为了「看起来高级」，有三个具体动机：\n"
                    "1. **多路复用需要帧**。HTTP/1.1 是文本流，报文之间靠 `Content-Length` / `chunked` 和空行来划分边界，"
                    "一旦要在同一条连接上交错发送多个请求的片段，**文本边界就彻底糊掉了**。"
                    "二进制分帧给每个帧一个长度前缀、类型和 stream id，接收端靠长度就能精确定位边界并把帧归到对应的流上 ——"
                    "**多路复用是建立在分帧之上的**。\n"
                    "2. **解析更高效也更安全**。文本协议要处理大小写、空白、行长限制、行折叠等一堆规则，解析器复杂且易出错；"
                    "二进制帧是定长头部 + 长度字段，解析快、歧义少，也不容易像 HTTP/1.1 那样被构造出**请求走私**。\n"
                    "3. **头部压缩和扩展性**。HPACK 基于帧结构做静态表 + 动态表 + Huffman 编码，把重复头部压到很小；"
                    "而且帧类型编号留了扩展空间，新特性（优先级、SETTINGS、PUSH_PROMISE）可以按需添加，不用改协议主结构。\n"
                    "代价是可读性：不能再像 HTTP/1.1 那样直接用 `telnet` 手写请求调试了，必须依赖工具。"
                ),
                "explanation": (
                    "能把「多路复用依赖分帧」这层因果关系说出来，比只说「二进制更省空间」有说服力得多 —— "
                    "分帧不是优化，是多路复用的前提。"
                ),
            },
        ],
    },
    # ------------------------------------------------------------------ net-09
    {
        "code": "net-09",
        "subject": "计算机网络",
        "stage": "应用层与安全",
        "runner": "none",
        "title": "HTTPS 与 TLS 握手",
        "summary": "先验明身份，再换一把只有双方知道的钥匙",
        "definition": (
            "**HTTPS = HTTP + TLS**。TLS 提供三样东西，缺一不可：\n\n"
            "- **机密性**：数据用对称加密传输，中间人看不懂\n"
            "- **完整性**：每段数据带 MAC/AEAD 校验，篡改会被发现\n"
            "- **身份认证**：通过**证书链**确认「对面确实是这个域名的主人」\n\n"
            "**核心设计：混合加密。** 为什么不全用非对称加密？因为它**慢**（比对称慢几个数量级），"
            "而且 RSA 单次能加密的数据长度受密钥长度限制。所以 TLS 的做法是：\n"
            "**用非对称（或 ECDHE）安全地协商出一个对称会话密钥，之后所有业务数据都用这个对称密钥加密。**\n\n"
            "**TLS 1.2 的完整握手（以 ECDHE 为例，共 2 个 RTT）**：\n\n"
            "1. **ClientHello**：客户端支持的 TLS 版本、密码套件列表、随机数 `random1`、**SNI**（说明自己要访问哪个域名）\n"
            "2. **ServerHello + Certificate + ServerKeyExchange + ServerHelloDone**：服务端选定套件、给出 `random2`、"
            "下发**证书链**、给出 ECDHE 参数并用证书私钥**签名**\n"
            "3. **ClientKeyExchange + ChangeCipherSpec + Finished**：客户端校验证书链 → 用 ECDHE 算出共享密钥 → "
            "发自己的参数 → 切换加密 → 发握手完成校验值\n"
            "4. **ChangeCipherSpec + Finished**：服务端也切换加密并确认\n\n"
            "**TLS 1.3 的改进**：握手压缩到 **1-RTT**（恢复会话支持 **0-RTT**）；"
            "**删掉了 RSA 密钥交换**，只保留 (EC)DHE；砍掉了一批不安全的密码套件；"
            "因此**默认具备前向安全（forward secrecy）**。\n\n"
            "**前向安全的含义**：ECDHE 每次会话都临时生成一对密钥，用完就扔。"
            "这样即使服务器的长期私钥日后泄露，攻击者也**解不开历史流量**。"
            "而老的 RSA 密钥交换用的是服务器长期私钥直接加密会话密钥，私钥一泄露，历史流量全部可解 —— "
            "这就是 TLS 1.3 果断删掉它的原因。"
        ),
        "plain": (
            "**为什么不能全程用非对称加密？** 因为它太慢了。可以这样想：\n\n"
            "非对称加密像是**用保险箱送东西**——安全，但每次只能装一点点，而且开关箱子极其费劲。"
            "对称加密像是**用一把普通锁的箱子**——装多少都行、开合飞快，但前提是双方得先有同一把钥匙。\n\n"
            "所以现实做法是：**先用保险箱把钥匙寄过去，之后所有货物都用这把钥匙锁箱子**。"
            "TLS 握手干的就是「寄钥匙」这件事，握手完成后的数据传输全部走对称加密 —— 这就是「混合加密」。\n\n"
            "**那怎么确定保险箱是寄给了对的人？** 靠**证书**。"
            "证书就像一个「由权威机构（CA）盖章的身份证」，里面写着「这个公钥属于 example.com」，"
            "并且有 CA 的数字签名。浏览器内置了受信任 CA 的根证书，顺着一层层的签名**逐级验证**，"
            "验到最后对上根证书，就确认了身份 —— 这条链就是**证书链**。\n\n"
            "如果验证失败（自签证书、过期、域名不匹配、被吊销），浏览器就会弹红色警告。"
            "**这个警告不能随手点「继续访问」**，因为它意味着你可能正在和中间人说话。"
        ),
        "example": (
            "TLS 1.2（ECDHE）vs TLS 1.3 的握手对比：\n"
            "\n"
            "【TLS 1.2 —— 2 个 RTT】\n"
            "  客户端                                服务端\n"
            "    │── ClientHello ──────────────────▶│\n"
            "    │   (版本、密码套件、random1、SNI)   │\n"
            "    │◀── ServerHello ─────────────────│\n"
            "    │◀── Certificate（证书链）─────────│\n"
            "    │◀── ServerKeyExchange（ECDHE 参数 + 签名）\n"
            "    │◀── ServerHelloDone ─────────────│\n"
            "    │  ★ 验证证书链 → 算共享密钥\n"
            "    │── ClientKeyExchange ───────────▶│\n"
            "    │── ChangeCipherSpec ────────────▶│\n"
            "    │── Finished（加密）─────────────▶│\n"
            "    │◀── ChangeCipherSpec ────────────│\n"
            "    │◀── Finished（加密）─────────────│\n"
            "    │  ← 之后全部走对称加密 →            │\n"
            "\n"
            "【TLS 1.3 —— 1 个 RTT】\n"
            "    │── ClientHello ──────────────────▶│\n"
            "    │   (+ key_share：直接带上自己的 ECDHE 公钥)\n"
            "    │◀── ServerHello ─────────────────│\n"
            "    │◀── {EncryptedExtensions} ────────│  ← 从这里开始就已经加密了\n"
            "    │◀── {Certificate} ────────────────│\n"
            "    │◀── {CertificateVerify} ──────────│\n"
            "    │◀── {Finished} ───────────────────│\n"
            "    │── {Finished} ───────────────────▶│\n"
            "    │  首次 1-RTT；会话恢复可 0-RTT     │"
        ),
        "example_output": (
            "TLS 1.3 省掉一个 RTT 的关键动作是：**客户端在第一个报文里就把 ECDHE 的公钥参数（key_share）带上了**，"
            "不用等对方先回应；而且证书和 Finished 一上来就是加密的，中间人连「你在访问哪个站」的元数据都不容易拿到。"
            "代价是 0-RTT 的早期数据存在**重放风险**，所以只能用于幂等请求。"
        ),
        "pitfalls": [
            "**说「HTTPS 就是把数据加密了」**：只答了机密性。完整答案必须包含**完整性校验**（防篡改）和**身份认证**（防中间人）。少掉身份认证这一条，加密就毫无意义 —— 你可以和中间人「加着密」通信。",
            "**以为证书是用来加密数据的**：证书只负责**身份认证和分发公钥**；真正加密业务数据的是握手协商出来的**对称会话密钥**。这两件事搞混是 TLS 相关问题的重灾区。",
            "**不区分 TLS 1.2 和 1.3 就笼统讲握手**：两者的报文数量和密码套件差别很大（1.2 是 2-RTT 且允许 RSA 密钥交换，1.3 是 1-RTT 且只保留 (EC)DHE）。回答时先说明版本，讲 1.3 再说「比 1.2 少一个 RTT、删掉了不安全的套件、默认前向安全」，条理立刻清晰。",
            "**忽略 SNI 和证书校验的失败原因**：一个 IP 上托管多个域名时，服务端靠 ClientHello 里的 **SNI** 决定返回哪张证书。而客户端要依次校验证书链是否可信、是否在有效期内、**域名是否匹配**、是否被吊销 —— 常见报错（自签证书、证书过期、`NET::ERR_CERT_COMMON_NAME_INVALID`）都能对到这几步上。另外 SNI 在明文里会暴露访问的域名，所以才有 Encrypted Client Hello（ECH）。",
        ],
        "task": "",
        "setup": "",
        "starter": "",
        "hint": "",
        "quizzes": [
            {
                "type": "choice",
                "stem": "面试官问：HTTPS 握手为什么要用非对称加密协商出对称密钥，而不是全程用非对称加密？",
                "options": [
                    "非对称加密比对称加密慢几个数量级，且单次能加密的数据量受密钥长度限制；所以只用它安全地协商出一个对称会话密钥，之后的数据传输全走对称加密",
                    "因为非对称加密无法保证数据的完整性，必须配合对称加密的 MAC 才能校验",
                    "因为非对称加密的密钥需要定期更换，而对称密钥可以一直复用",
                    "因为现代浏览器已经不支持 RSA 等非对称算法，只能用它来签名",
                ],
                "answer_index": 0,
                "explanation": (
                    "核心就是「性能 + 数据长度限制」这两个理由，这就是所谓的**混合加密**。"
                    "B 说反了 —— 完整性靠 MAC/AEAD，和用哪种加密算法无关；C 也不对，对称密钥每次会话都重新协商，正是它需要被保护的原因。"
                    "补充一句加分点：这也是为什么 TLS 1.3 干脆删掉了 RSA 密钥交换、只留 (EC)DHE。"
                ),
            },
            {
                "type": "choice",
                "stem": "TLS 1.3 相比 TLS 1.2，最主要的改进是什么？",
                "options": [
                    "握手从 2-RTT 压缩到 1-RTT（会话恢复可 0-RTT），删除 RSA 密钥交换、只保留 (EC)DHE，因而默认具备前向安全",
                    "把传输层从 TCP 换成 UDP，从而消除队头阻塞",
                    "不再需要服务器证书，改为使用 PSK 预共享密钥",
                    "取消对称加密，全程使用 ECC 非对称加密",
                ],
                "answer_index": 0,
                "explanation": (
                    "B 描述的是 **HTTP/3 + QUIC**，和 TLS 版本无关，这是最容易混淆的一点"
                    "（QUIC 确实集成了 TLS 1.3，但「换传输层」是 QUIC 做的事，不是 TLS 1.3 的改进）。"
                    "C 不对，证书仍然需要，PSK 只是恢复会话时的可选路径；D 完全违背混合加密的设计。"
                ),
            },
            {
                "type": "judge",
                "stem": "TLS 1.3 只保留了 (EC)DHE 系列的密钥交换方式，因此默认具备前向安全。",
                "answer": True,
                "explanation": (
                    "ECDHE 每次会话都临时生成密钥对，用完即弃，所以服务器长期私钥泄露也解不开历史流量 —— 这就是**前向安全**。"
                    "而 TLS 1.2 允许的 RSA 密钥交换用的是长期私钥直接加密会话密钥，不具备这个性质，所以 1.3 把它删了。"
                ),
            },
            {
                "type": "blank",
                "stem": "HTTPS 传输业务数据时使用的是 ___ 加密（它比非对称加密快几个数量级）。",
                "hint": "填两个字",
                "answer": "对称",
                "accept": ["对称加密", "symmetric", "对称密钥"],
                "explanation": (
                    "握手阶段用非对称（或 ECDHE）安全地协商出会话密钥，数据传输阶段用对称加密 ——"
                    "这个组合叫**混合加密**，是 HTTPS 性能与安全兼顾的关键。"
                ),
            },
            {
                "type": "short",
                "stem": "面试官问：客户端是怎么确认自己连的是真服务器，而不是中间人的？",
                "keywords": ["证书链", "CA", "根证书", "签名", "域名匹配", "吊销"],
                "reference": (
                    "靠**证书链的逐级验证**，核心逻辑是「用签发者的公钥去验下一级的签名」，一直验到浏览器内置的受信任根证书。\n"
                    "具体步骤：\n"
                    "1. **建立信任锚**：操作系统和浏览器预装了受信任 CA 的根证书（信任锚），这是整条链的起点；\n"
                    "2. **验签名**：服务端下发的是「叶子证书 + 中间 CA 证书」组成的链，"
                    "客户端用中间 CA 的公钥验证叶子证书的签名，再用根证书的公钥验证中间 CA 的签名，逐级往上；\n"
                    "3. **验内容**：光验签名不够，还要检查证书**是否在有效期内**、**域名是否与访问的域名匹配**"
                    "（含通配符规则）、**是否被吊销**（CRL / OCSP）、以及证书用途是否允许做服务器认证；\n"
                    "4. **验持有者**：最后用叶子证书里的公钥去验证 TLS 握手中的签名（`CertificateVerify`），"
                    "这一步才真正证明「**对方手里有这张证书对应的私钥**」—— 光有证书谁都能复制，能签名才算数。\n"
                    "任何一步失败，浏览器就会报证书错误并阻止连接。"
                    "要说清的一点是：**中间人可以把证书原样转发**，但他没有对应的私钥，"
                    "所以第 4 步的签名验证过不了 —— 这才是防中间人的关键一环。"
                ),
                "explanation": (
                    "很多人的答案停在「校验证书是否可信」，只有说出「用私钥签名验证持有者」这一步，才算真的想明白了为什么中间人冒充不了。"
                ),
            },
        ],
    },
    # ------------------------------------------------------------------ net-10
    {
        "code": "net-10",
        "subject": "计算机网络",
        "stage": "网络全流程",
        "runner": "none",
        "title": "DNS 与「输入 URL 到页面展示」全流程",
        "summary": "面试最爱问的一道题，串起所有知识点",
        "definition": (
            "**第一部分：DNS 把域名变成 IP。**\n\n"
            "查询过程（从近到远逐级查）：\n\n"
            "1. **浏览器 DNS 缓存** → **操作系统缓存 / hosts 文件** → **路由器 / 本地 DNS 服务器缓存**\n"
            "2. 缓存未命中时，本机向**本地 DNS 服务器（递归解析器）**发起查询（这是**递归查询**：我只要答案）\n"
            "3. 本地 DNS 依次问 **根域名服务器**（告诉你 `.com` 的顶级域名服务器在哪）→ "
            "**顶级域名服务器 TLD**（告诉你 `example.com` 的权威服务器在哪）→ "
            "**权威域名服务器**（返回最终的 A / AAAA 记录）。本地 DNS 与它们之间是**迭代查询**：每次只拿到「下一步该问谁」\n"
            "4. 结果按 **TTL** 缓存起来，本地 DNS 把答案返回给客户端\n\n"
            "常见记录类型：**A**（IPv4）、**AAAA**（IPv6）、**CNAME**（别名）、**NS**（授权域名服务器）、**MX**（邮件）、**TXT**（校验/SPF）。"
            "默认走 **UDP 53**，响应被截断（TC=1）或区域传送（AXFR）时改用 **TCP 53**。\n\n"
            "**第二部分：拿到 IP 之后的完整链路。**\n\n"
            "1. 解析 URL：拆出 scheme、host、port（默认 80/443）、path、query\n"
            "2. **DNS 解析**拿到目标 IP（CDN 会返回离你最近的边缘节点 IP）\n"
            "3. **TCP 三次握手**建立连接（若是 HTTPS，接着做 **TLS 握手**，1~2 个 RTT，用 ALPN 协商出 h2 / h3）\n"
            "4. 发送 **HTTP 请求**，请求可能被 CDN 边缘节点命中缓存直接返回，也可能回源到后端应用\n"
            "5. 收到响应（HTML 首字节到达的时间就是 **TTFB**）\n"
            "6. 浏览器解析 HTML → 遇到 CSS/JS/图片等子资源继续发起请求（也复用连接）\n"
            "7. **渲染**：解析成 DOM 树 + CSSOM → 合成渲染树 → 布局（layout）→ 绘制（paint）→ 合成（composite）→ 上屏\n\n"
            "注意还有一个常被漏掉的环节：数据包发出去之前，本机要先通过 **ARP** 拿到网关的 MAC 地址，"
            "出公网还要经过 **NAT** 转换，到达后再由网卡中断 → 内核协议栈 → socket 缓冲区，最后才交给应用进程。"
        ),
        "plain": (
            "这道题之所以经典，是因为**它一次性考完了你学过的所有东西**：DNS、TCP、TLS、HTTP、缓存、渲染。"
            "面试官不是要你背流程，而是想听你有没有「一条链路串到底」的理解。\n\n"
            "打个比方，整个过程就像**你要给一个只知道名字的人寄东西**：\n\n"
            "1. **DNS = 查电话簿**。你只有名字「example.com」，得先查到地址。查电话簿的顺序也是从近到远："
            "先翻自己的小本子（浏览器缓存），再问前台（系统缓存），最后打电话给查号台（本地 DNS），"
            "查号台一层层往上级问，直到问到对方的直管单位（权威服务器）\n"
            "2. **TCP 三次握手 = 打个电话确认双方都能说话**\n"
            "3. **TLS 握手 = 互相核对证件，然后约定一套只有你俩懂的暗语**\n"
            "4. **HTTP 请求 = 正式说事**\n"
            "5. **渲染 = 对方把材料寄过来，你自己组装成成品**\n\n"
            "**答这道题最有效的方法是「先给骨架，再按面试官的兴趣往下钻」**："
            "先用一句话把七个步骤说全，然后主动提一句「这里面每一步都有优化点，比如 DNS 有缓存、TCP 有慢启动、"
            "HTTP 有连接复用和 CDN」，面试官就会顺着你指的方向追问 —— 这样就把主动权拿在自己手上了。"
        ),
        "example": (
            "在浏览器里敲下 https://www.example.com/index.html 之后：\n"
            "\n"
            "① 解析 URL\n"
            "   scheme=https  host=www.example.com  port=443（默认）  path=/index.html\n"
            "\n"
            "② DNS 解析（目标：拿到 IP）\n"
            "   浏览器缓存 → OS 缓存/hosts → 本地 DNS（递归）\n"
            "   本地 DNS ─问─▶ 根服务器：.com 的 TLD 在哪？\n"
            "   本地 DNS ─问─▶ .com TLD：example.com 的权威服务器在哪？\n"
            "   本地 DNS ─问─▶ 权威服务器：www.example.com 的 A 记录？\n"
            "   权威服务器 ─答─▶ 93.184.216.34（CDN 会返回离你最近的节点）\n"
            "   结果按 TTL 缓存\n"
            "\n"
            "③ TCP 三次握手（约 1 个 RTT）\n"
            "   SYN → SYN+ACK → ACK\n"
            "\n"
            "④ TLS 握手（TLS 1.3 约 1 个 RTT）\n"
            "   校验证书链 → ECDHE 协商会话密钥 → Finished\n"
            "   顺便用 ALPN 协商好用 h2 还是 h3\n"
            "\n"
            "⑤ 发 HTTP 请求 / 收响应\n"
            "   GET /index.html HTTP/2\n"
            "   Host: www.example.com\n"
            "   → 如果 CDN 缓存命中，直接从边缘节点返回（没有回源）\n"
            "   → TTFB 从发请求到收到第一个字节\n"
            "\n"
            "⑥ 解析与渲染\n"
            "   HTML → DOM 树；CSS → CSSOM；遇到 <script>/<img> 再发子请求\n"
            "   DOM + CSSOM → 渲染树 → 布局 → 绘制 → 合成 → 上屏\n"
            "\n"
            "⑦ 用户看到页面"
        ),
        "example_output": (
            "把耗时拆开看会更清楚（数字只是示意）：\n"
            "  DNS 解析 20ms + TCP 握手 1 RTT + TLS 握手 1~2 RTT + 服务端处理时间（TTFB）+ 传输时间 + 解析渲染。\n"
            "  **优化也正是对着这几段来的**：DNS 用预解析/HTTPDNS 减少解析时间，"
            "连接用连接池/`keep-alive` 省掉握手，TLS 用会话复用和 0-RTT 压 RTT，"
            "内容用 CDN 把 TTFB 降下来，渲染用「关键渲染路径优化」减少首屏时间。"
        ),
        "pitfalls": [
            "**顺序答错，比如先握手再 DNS**：必须是**先 DNS 拿到 IP，才可能发起 TCP 连接**（没有目标地址无法建连）。"
            "正确顺序固定为：解析 URL → DNS → TCP 三次握手 → TLS 握手 → HTTP 请求/响应 → 解析回车渲染。"
            "另外别漏了 TLS 只在 HTTPS 下才有，HTTP 是跳过它直接发请求的。",
            "**不知道 DNS 默认走 UDP 53，什么时候切 TCP**：普通查询用 UDP（省掉握手 RTT），"
            "响应被截断（**TC=1**）或做**区域传送 AXFR/IXFR** 时改用 **TCP 53**。"
            "回答时说清这两个触发条件，比笼统说「DNS 用 UDP」要专业得多。",
            "**忽略缓存层级和 CDN 的存在**：DNS 结果在浏览器、系统、本地 DNS、各级服务器上都被缓存，"
            "缓存时长由 **TTL** 控制；同时 **CDN 会把请求调度到离用户最近的边缘节点**，"
            "所以「同一个域名在不同地区解析出的 IP 不同」是完全正常的 —— 这不是劫持。",
            "**链路只讲到应用层就没了**：完整答案还要带上**本机到网关这一段**：用 **ARP** 解析出网关的 MAC 地址，"
            "数据包经内核协议栈发到网卡，出公网前经过 **NAT** 转换，回来时再反向解封装，"
            "最后由网卡中断触发内核把数据放进 socket 缓冲区，应用进程才能读到。"
            "提到这一层，面试官基本能确认你理解的是「真链路」而不是「背流程」。",
        ],
        "task": "",
        "setup": "",
        "starter": "",
        "hint": "",
        "quizzes": [
            {
                "type": "choice",
                "stem": "面试官问：从浏览器输入 URL 到页面展示，中间发生了什么？下面哪个顺序是正确的？",
                "options": [
                    "解析 URL → DNS 解析拿到 IP → TCP 三次握手 → TLS 握手（HTTPS）→ 发送 HTTP 请求并收到响应 → 解析 HTML 并渲染",
                    "解析 URL → TCP 三次握手 → DNS 解析 → TLS 握手 → 发送 HTTP 请求 → 渲染",
                    "解析 URL → TLS 握手 → DNS 解析 → TCP 三次握手 → 发送 HTTP 请求 → 渲染",
                    "解析 URL → ARP 解析域名 → TLS 握手 → TCP 三次握手 → 发送 HTTP 请求 → 渲染",
                ],
                "answer_index": 0,
                "explanation": (
                    "**必须先有 IP 才能建 TCP 连接**，所以 DNS 一定在三次握手之前，B、C 直接错。"
                    "D 把工具用错了：ARP 解析的是**局域网内的 MAC 地址**，它不解析域名；"
                    "而且 ARP 发生在链路层，位置在拿到 IP、准备发包之后，不可能排在 TLS 之前。"
                    "答这题时最好主动补一句「TLS 只在 HTTPS 场景下才有」。"
                ),
            },
            {
                "type": "choice",
                "stem": "DNS 查询默认使用哪个传输层协议和端口？",
                "options": [
                    "默认 UDP 53；响应被截断（TC=1）或做区域传送时改用 TCP 53",
                    "默认 TCP 53；只有当响应很小时才降级用 UDP",
                    "默认 UDP 5353，也用于 mDNS",
                    "默认 TCP 443，走 HTTPS（即 DoH）",
                ],
                "answer_index": 0,
                "explanation": (
                    "默认 **UDP 53**，因为查询是一问一答的小报文，UDP 省掉了握手的一个 RTT。"
                    "超过承载长度时服务端置 **TC=1** 截断标志，客户端改用 **TCP 53** 重查。"
                    "C 里的 5353 是 **mDNS**（局域网内 `xxx.local` 的解析）的端口，不是常规 DNS；"
                    "D 的 DoH 是加密 DNS 的一种实现，属于另一种可选方案，不是默认行为。"
                ),
            },
            {
                "type": "judge",
                "stem": "在使用了 CDN 的场景下，同一个域名在不同地区解析出的 IP 可能不同，这是正常现象。",
                "answer": True,
                "explanation": (
                    "CDN 的调度依赖 DNS：权威服务器会根据请求来源的运营商和地理位置，返回**最近的边缘节点 IP**。"
                    "所以「同域名不同 IP」是调度结果，不是 DNS 劫持。"
                    "顺带回答一个常见追问：这也是为什么用 `ping` 得到的 IP 和对端真实 IP 不一样是正常的。"
                ),
            },
            {
                "type": "blank",
                "stem": "浏览器必须先通过 DNS 解析拿到目标主机的 ___ 地址，才可能发起 TCP 连接。",
                "hint": "两个字，或者英文缩写",
                "answer": "IP",
                "accept": ["IP地址", "ip", "IP 地址", "ip地址"],
                "explanation": (
                    "TCP 连接由「源 IP、源端口、目的 IP、目的端口」这**四元组**唯一标识，"
                    "所以没有目标 IP 就无从建连 —— 这就是 DNS 必须排在最前面的根本原因。"
                ),
            },
            {
                "type": "short",
                "stem": "面试官问：一个页面首屏加载很慢，你会沿着这条网络链路怎么排查？",
                "keywords": ["DNS", "TTL", "RTT", "TTFB", "CDN", "关键渲染路径"],
                "reference": (
                    "按链路的顺序逐段定位，而不是一上来就猜：\n"
                    "1. **DNS 段**：看解析耗时。TTL 设得过短会让每次访问都重新解析；"
                    "用 `dig` 看解析结果是否指向就近的 CDN 节点，必要时上 DNS 预解析或 HTTPDNS。\n"
                    "2. **握手段**：看 TCP + TLS 占了多少时间。判断是否每次请求都新建连接（连接没有复用）、"
                    "TLS 是否开了会话复用/0-RTT、是否因为跨地域导致 RTT 本身就高。\n"
                    "3. **服务端段**：看 **TTFB**。TTFB 高说明瓶颈在后端（数据库慢、缓存没命中、回源），"
                    "这一步最关键的分叉是「**首字节慢**」还是「**传输/渲染慢**」。\n"
                    "4. **传输段**：看资源体积、是否开了压缩（gzip/br）、大文件是否走 CDN、"
                    "是否因为 HTTP/1.1 的并发连接数限制而排队。\n"
                    "5. **渲染段**：TTFB 正常但首屏慢，问题多半在**关键渲染路径** —— "
                    "阻塞渲染的同步 JS/CSS、图片未懒加载、字体未预加载。\n"
                    "最后给结论：**先用 DevTools 的 Network/Performance 面板按时间轴分段测量，"
                    "拿到数字再定位**，别凭感觉优化。"
                ),
                "explanation": (
                    "这题考的是「把一条链路拆成分段、逐段测量」的排查思路。"
                    "能说出「先区分 TTFB 慢还是渲染慢」这个分叉点，就说明真的做过性能排查。"
                ),
            },
        ],
    },
]
