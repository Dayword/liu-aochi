"""操作系统：进程、内存、存储与 IO 的面试高频考点。

不写代码，用状态迁移、算法对比和线上排查场景讲清楚。
"""

LESSONS: list[dict] = [
    # ---------------- stage：进程与线程 ----------------
    {
        "code": "os-01",
        "subject": "操作系统",
        "stage": "进程与线程",
        "runner": "none",
        "title": "进程与线程的区别",
        "summary": "进程是资源单位，线程是调度单位",
        "definition": (
            "**进程**是一个正在运行的程序实例，是操作系统**资源分配的基本单位**。"
            "它有独立的虚拟地址空间、自己的页表、文件描述符表、信号处理表，进程之间默认互相看不见。\n\n"
            "**线程**是进程内部的一条执行流，是 CPU **调度的基本单位**。同一进程里的线程共享地址空间、堆、"
            "全局变量和打开的文件，但每个线程有**私有的栈、寄存器、程序计数器**。\n\n"
            "为什么要有线程？因为并发往往需要多个执行流**共享同一份数据**。用进程做，共享只能靠 IPC，"
            "又慢又麻烦；用线程，直接读写同一个变量即可，代价是必须自己加锁。"
            "另外线程的创建、销毁、切换都比进程便宜得多。\n\n"
            "在 Linux 里两者本质是同一个内核对象 `task_struct`，"
            "`fork` 和 `pthread_create` 最终都走 `clone`，区别只是**共享哪些资源**。"
        ),
        "plain": (
            "把**进程想成一间独立的工作室**：有自己的门牌号（地址空间）、自己的水电表（资源），别人进不来。"
            "**线程就是这间工作室里的工人**：厂房、工具、原料都是共用的，"
            "但每个人手里有自己正在做的那道工序（栈和寄存器）。\n\n"
            "共用会带来两个后果：\n"
            "1. **好处**：工人之间喊一声就能协作（直接读写共享变量），不用隔着墙打报告；"
            "换人成本也低（创建和切换线程比进程快）。\n"
            "2. **坏处**：一个人把原料弄脏了（共享数据没加锁），所有人都遭殃；"
            "一个人捅了大篓子（段错误），整间工作室直接被封（整个进程挂掉）。\n\n"
            "面试时记住一句话就够：**进程管资源，线程管调度；进程独立但贵，线程便宜但危险。**"
        ),
        "example": (
            "# 一张表看清区别（面试照着这个顺序答）\n"
            "# 维度        进程 Process                     线程 Thread\n"
            "# 定位        资源分配的基本单位                 CPU 调度的基本单位\n"
            "# 地址空间     独立，各有一张页表                 共享进程的地址空间\n"
            "# 私有部分     几乎全部（互不可见）               栈、寄存器、PC、errno、TLS\n"
            "# 共享部分     —（互通要靠 IPC）                 堆、全局变量、文件描述符、信号处理\n"
            "# 切换开销     大：要换页表、刷 TLB               小：同进程内只换栈和寄存器\n"
            "# 通信方式     IPC（管道 / 消息队列 / 共享内存）  直接读写共享变量（必须加锁）\n"
            "# 崩溃影响     只死自己，其他进程不受影响         一个线程段错误，整个进程被内核杀死\n"
            "# 创建代价     fork 要复制页表                     pthread_create 只分配一个栈"
        ),
        "example_output": (
            "表的读法（面试官想听的三句话）：\n"
            "1. 「进程是资源分配的单位，线程是 CPU 调度的单位」——先给结论。\n"
            "2. 「线程共享地址空间，所以通信便宜但要加锁；进程地址空间独立，通信得走 IPC」——讲清代价。\n"
            "3. 「线程切换省掉了换页表和刷 TLB，所以比进程切换快；但一个线程崩了整个进程就没了」——讲清取舍。\n\n"
            "补一句加分项：Linux 里进程和线程都用 task_struct 表示，"
            "`fork` 与 `pthread_create` 底层都是 `clone`，只是共享的资源不同。"
        ),
        "pitfalls": [
            "**别把「线程切换一定比进程切换快」说死**：同进程内线程切换省掉的是换页表和刷 TLB；"
            "如果两个线程本来就属于同一个进程，这笔开销本来也不用付，真正的差距来自地址空间是否要换。",
            "**线程共享 ≠ 全都共享**：栈、寄存器、程序计数器、errno、线程局部存储（TLS）都是线程私有的，"
            "共享的是堆、全局变量、代码段和文件描述符。面试官最爱问「线程私有的是什么」，答错了很扎眼。",
            "**并发不等于并行**：并发是「一段时间内交替推进」，并行是「同一时刻真的在多核上同时跑」。"
            "单核上多线程只能并发，切换本身还有开销。",
            "**别拿 Python 的多线程去顶 CPU 密集任务**：CPython 有 GIL，同一时刻只有一个线程执行字节码，"
            "CPU 密集要用多进程；IO 密集才是多线程的主场。",
        ],
        "task": "",
        "setup": "",
        "starter": "",
        "hint": "",
        "quizzes": [
            {
                "type": "choice",
                "stem": "下面哪一组资源是**线程私有**的？",
                "options": [
                    "栈、寄存器、程序计数器",
                    "堆和全局变量",
                    "打开的文件描述符表",
                    "代码段和常量区",
                ],
                "answer_index": 0,
                "explanation": (
                    "线程私有的是栈、寄存器、PC、errno、TLS —— 因为每条执行流要有自己的执行现场。"
                    "堆、全局变量、代码段、文件描述符表都是同进程内所有线程共享的。"
                ),
            },
            {
                "type": "choice",
                "stem": "面试官问：一个进程里有 4 个线程，其中线程 0 做了一次非法内存访问（段错误），会发生什么？",
                "options": [
                    "整个进程被内核杀死，另外 3 个线程一起消失",
                    "只有线程 0 退出，另外 3 个线程照常运行",
                    "内核会自动重启线程 0",
                    "进程转入阻塞态，等待人工处理",
                ],
                "answer_index": 0,
                "explanation": (
                    "段错误是进程级的致命错误，内核会给整个进程发 SIGSEGV，进程内所有线程一起没。"
                    "这也正是「线程共享地址空间」的代价；换成独立进程，只死一个，其他进程不受影响。"
                ),
            },
            {
                "type": "judge",
                "stem": "线程是资源分配的基本单位，进程是 CPU 调度的基本单位。",
                "answer": False,
                "explanation": "正好说反了：**进程是资源分配的基本单位，线程是 CPU 调度的基本单位**。这句话必须背准，是最高频的第一问。",
            },
            {
                "type": "blank",
                "stem": "Linux 里创建线程最终落到内核的系统调用是 `___`（创建进程的 `fork` 只是它的一个特例）。",
                "hint": "一个系统调用名",
                "answer": "clone",
                "accept": ["clone()", "clone 系统调用"],
                "explanation": (
                    "`fork`、`vfork`、`pthread_create` 在 Linux 上最终都走 `clone`，"
                    "传入的 CLONE_VM、CLONE_FILES 等标志决定共享哪些资源。"
                ),
            },
            {
                "type": "short",
                "stem": "面试官问：什么情况下你会选多进程而不是多线程？说说你的判断依据。",
                "keywords": ["隔离", "崩溃", "CPU 密集", "GIL", "共享数据", "稳定性"],
                "reference": (
                    "主要看三点。**稳定性**：一个模块的崩溃不能拖垮别人时用多进程，比如浏览器每个标签页一个渲染进程；"
                    "线程共享地址空间，一个段错误全挂。**是否要大量共享数据**：要频繁共享就用多线程，"
                    "要隔离就用多进程，免得靠 IPC 或共享内存加锁。**任务类型**：CPU 密集且在 Python 里，"
                    "多线程受 GIL 限制跑不满多核，就该用多进程。"
                ),
                "explanation": "答这题的关键是给判断依据，而不是背定义。隔离性、共享需求、任务类型是最常被认可的三条。",
            },
        ],
    },
    {
        "code": "os-02",
        "subject": "操作系统",
        "stage": "进程与线程",
        "runner": "none",
        "title": "进程状态与调度算法",
        "summary": "状态迁移与 CPU 调度策略怎么答",
        "definition": (
            "进程的五态模型：**新建 new → 就绪 ready → 运行 running → 阻塞 blocked → 终止 terminated**。\n\n"
            "关键的迁移只有四条，面试就考这四条：\n\n"
            "- **就绪 → 运行**：调度器选中了它\n"
            "- **运行 → 就绪**：时间片用完或被更高优先级抢占（**不是阻塞**）\n"
            "- **运行 → 阻塞**：主动等待资源，比如 read 磁盘、等锁、等条件变量\n"
            "- **阻塞 → 就绪**：等待的事件完成了（IO 回来、拿到锁），但它**不能直接上 CPU**\n\n"
            "常见调度算法：\n\n"
            "- **FCFS** 先来先服务：实现最简单，长作业在前面会让后面全体排队（护航效应）\n"
            "- **SJF** 最短作业优先：理论平均等待时间最短，但长作业可能饥饿\n"
            "- **RR** 时间片轮转：响应快，时间片太小则上下文切换开销占比高，太大退化成 FCFS\n"
            "- **优先级调度**：高优先级先跑，要配老化（aging）防饥饿\n"
            "- **MLFQ** 多级反馈队列：多队列 + 动态调整优先级，兼顾交互型与计算型，现代通用系统的思路\n"
            "- **CFS**（Linux 实际使用）：按虚拟运行时间 vruntime 用红黑树选最左节点，不再用固定时间片"
        ),
        "plain": (
            "拿**银行办业务**理解：\n\n"
            "- **就绪**：坐在等候区，一切材料齐了，就等叫号\n"
            "- **运行**：正在窗口办业务，柜员在服务你\n"
            "- **阻塞**：发现少带了一份材料，必须回家拿。这时候你**离开队伍**，窗口不可能干等你\n"
            "- **阻塞 → 就绪**：材料拿回来了，重新取号回到等候区——注意是回等候区，不是直接插到窗口前\n\n"
            "**运行 → 就绪和运行 → 阻塞的区别**是这章最爱考的点："
            "被叫号系统换下来（时间片到）是「回到等候区」；自己要去拿材料（等 IO）才是「离场」。"
            "前者是被动的、随时能再上；后者是被动的等别人，跟 CPU 没关系。"
        ),
        "example": (
            "# 四个作业同时到达（t=0），运行时间分别是 7、4、1、4\n"
            "# 维度        执行顺序       完成时间        周转时间        平均周转\n"
            "# FCFS        P1 P2 P3 P4    7 11 12 16      7 11 12 16      11.5\n"
            "# SJF         P3 P2 P4 P1    1  5  9 16      1  5  9 16      7.75\n"
            "# RR(片=2)    P1 P2 P3 P4    15 12 6 14      15 12 6 14      11.75\n"
            "#             ...循环           （每 2 个时间片换一次人）\n"
            "\n"
            "# 状态迁移速查（箭头一定要背准）\n"
            "# 就绪 --调度--> 运行      运行 --时间片到/被抢占--> 就绪\n"
            "# 运行 --等 IO/锁--> 阻塞   阻塞 --事件完成--> 就绪\n"
            "# 阻塞 --X--> 运行   直接被禁止，必须先回就绪态排队"
        ),
        "example_output": (
            "结果说明：\n"
            "1. **SJF 的平均周转时间最短**（7.75 < 11.5 < 11.75），这就是它的理论优势；代价是要能预知运行时间，"
            "真实系统只能用历史数据估计，而且长作业可能一直排不上队（饥饿）。\n"
            "2. **FCFS 被长作业拖累**：P1 先跑完 7 个时间单位，后面三个全在等，这就是护航效应。\n"
            "3. **RR 的平均周转时间反而比 FCFS 差，但响应时间最好**——第一个作业很快就能先拿到 CPU，"
            "交互型任务要的是这个，不是平均周转。面试时把「要吞吐还是要响应」这层说出来就是加分项。"
        ),
        "pitfalls": [
            "**阻塞态不能直接回到运行态**：必须经就绪态被调度器重新选中。这是本课最经典的判断题陷阱。",
            "**时间片用完是「运行 → 就绪」，不是「运行 → 阻塞」**：阻塞一定要有一个「主动等某个事件」的动作，"
            "单纯被换下 CPU 不算。",
            "**别把 SJF 说成无脑最优**：它只保证平均等待时间最短，且需要预知运行时间，"
            "在通用系统里做不到；另外它可能让长作业饿死，要配老化机制。",
            "**时间片不是越小越好**：太小会让上下文切换占满 CPU（切换本身不干活），"
            "太大会退化成 FCFS。Linux 干脆换成 CFS，按 vruntime 挑最欠跑的那个，而不是发固定时间片。",
        ],
        "task": "",
        "setup": "",
        "starter": "",
        "hint": "",
        "quizzes": [
            {
                "type": "choice",
                "stem": "一个进程正在 CPU 上运行，时间片用完了被换下来，它进入什么状态？",
                "options": ["就绪态", "阻塞态", "终止态", "新建态"],
                "answer_index": 0,
                "explanation": "时间片用完只是被剥夺 CPU，条件依然具备，回到就绪队列等下一次调度。阻塞态必须是主动等某个事件。",
            },
            {
                "type": "choice",
                "stem": "关于 SJF（最短作业优先）调度算法，下面哪个说法是对的？",
                "options": [
                    "平均等待时间理论最短，但长作业可能饥饿",
                    "任何情况下都比 FCFS 更好",
                    "实现简单，因为不需要预知作业的运行时间",
                    "它是一种抢占式算法，时间片一到就切换",
                ],
                "answer_index": 0,
                "explanation": (
                    "SJF 的最优性只体现在「平均等待时间」这一个指标上，且前提是能预知运行时间（真实系统做不到）。"
                    "B 太绝对；C 说反了，预知运行时间正是它最大的落地障碍；D 描述的是 RR。"
                ),
            },
            {
                "type": "judge",
                "stem": "进程从阻塞态被唤醒后，可以直接进入运行态继续执行。",
                "answer": False,
                "explanation": "唤醒后只能进入就绪态，要等调度器再次选中才能上 CPU。这条迁移线在状态图上是禁止直接连的。",
            },
            {
                "type": "blank",
                "stem": "进程因等待 IO 完成而进入阻塞态，等 IO 完成后它必须先进入 `___` 态，才有机会被调度运行。",
                "hint": "填一个状态名，两个字",
                "answer": "就绪",
                "accept": ["ready", "就绪态"],
                "explanation": "阻塞 → 就绪 → 运行，中间那步不能省。这是状态迁移题的标准答案。",
            },
            {
                "type": "short",
                "stem": "面试官问：Linux 为什么不再用固定时间片轮转，而是用 CFS？说说 CFS 解决了什么问题。",
                "keywords": ["公平", "vruntime", "红黑树", "时间片", "交互", "切换开销"],
                "reference": (
                    "固定时间片的难题是「多大多小」：小了切换开销占比高，大了交互任务响应变差，"
                    "而且一刀切对 IO 密集和 CPU 密集不公平。CFS 换了个思路，用红黑树按 vruntime（虚拟运行时间）"
                    "排序，每次都挑「欠跑最多」的那个进程，还按优先级给不同的权重，"
                    "让实际获得的 CPU 时间按权重成比例分配。这样不需要手工调时间片，公平性好，"
                    "交互型任务也不会被算力型任务压死。"
                ),
                "explanation": "答「调度算法」类问题的套路：先说旧方案的矛盾（时间片大小两难），再说新方案的核心机制，最后给效果。",
            },
        ],
    },
    {
        "code": "os-03",
        "subject": "操作系统",
        "stage": "进程与线程",
        "runner": "none",
        "title": "进程间通信 IPC 方式对比",
        "summary": "管道、消息队列、共享内存怎么选",
        "definition": (
            "进程地址空间互相独立，想交换数据必须让内核帮忙，或者人为共享一块内存。常见方式：\n\n"
            "- **匿名管道 pipe**：字节流、半双工、只能用于父子/兄弟等有亲缘关系的进程，默认容量 64KB\n"
            "- **命名管道 FIFO**：有文件名，任意进程都能打开，突破了亲缘限制，本质还是字节流\n"
            "- **消息队列 Message Queue**：内核维护的消息链表，有边界、有类型，可以按类型取，无需自己切包\n"
            "- **共享内存 Shared Memory**：把同一块物理内存映射进两个进程的地址空间，**拷贝次数最少、速度最快**，"
            "但内核**不提供同步**，必须自己配信号量或互斥锁\n"
            "- **信号量 Semaphore**：本身不传数据，只做同步和互斥\n"
            "- **信号 Signal**：异步通知，只能带一个整数，用来告诉对方「出事了」而不是「数据是什么」\n"
            "- **socket**：唯一能跨主机的方式；本机通信用 Unix domain socket，比 TCP 快不少\n\n"
            "选型口诀：**要快选共享内存，要省事选消息队列，要跨机器选 socket，只是通知一下用信号。**"
        ),
        "plain": (
            "两个进程就像**两间互不相通的屋子**，中间隔着墙（地址空间独立）。想让东西过去，有几种办法：\n\n"
            "- **管道**：在墙上钻一根管子传纸条。只能单向传（半双工），管子还有容量上限，"
            "塞满了递纸条的人就得站着等。\n"
            "- **消息队列**：给对面设一个信箱。每封信自带编号和类型，对面可以按类型挑着取，不用自己拆包。\n"
            "- **共享内存**：干脆把墙拆了，中间隔出一间公共会议室，两边都能直接进去。"
            "这是**最快的**，因为东西不用搬来搬去；但会议室里两个人同时改白板就乱套了，所以还得推一个「谁用谁锁门」的规矩"
            "（信号量）。\n"
            "- **socket**：打电话。慢一些（要经协议栈），但对面可以远在天边（跨主机）。"
        ),
        "example": (
            "# 1) 匿名管道：shell 的 `|` 就是它，父子进程通信\n"
            "ls -l /tmp | grep log          # 两个进程通过内核里的一根管子传数据\n"
            "\n"
            "# 2) 命名管道：有名字，任意进程都能用\n"
            "mkfifo /tmp/myfifo            # 创建\n"
            "cat /tmp/myfifo &             # 一端读（会阻塞等写端）\n"
            "echo hello > /tmp/myfifo      # 另一端写\n"
            "\n"
            "# 3) 共享内存：看看现在系统里有哪些共享内存段\n"
            "ipcs -m                       # shmid / 大小 / 挂接进程数\n"
            "ipcrm -m 32768                # 用完必须显式删除，否则会一直留在内核里\n"
            "\n"
            "# 4) 信号：通知 + 抓一下看看\n"
            "kill -l                       # 列出所有信号\n"
            "kill -USR1 12345              # 给指定进程发一个用户自定义信号\n"
            "\n"
            "# 5) socket：跨主机只能靠它，本机也常用 Unix domain socket\n"
            "ss -lxnp | head               # 看当前系统上的 Unix domain socket"
        ),
        "example_output": (
            "运行结果说明：\n"
            "1. `ls -l /tmp | grep log` 能看到过滤后的结果——shell 建了一根匿名管道，"
            "把 ls 的 stdout 接到 grep 的 stdin，两个进程谁也没见到对方。\n"
            "2. `ipcs -m` 会列出共享内存段，注意那列 `nattch`（挂接的进程数）；"
            "共享内存段**不会因为进程退出自动回收**，忘了 ipcrm 会一直占着内存，这是线上常见的手工事故。\n"
            "3. 共享内存本身只是一块可读写的内存，没有任何同步语义："
            "两个进程同时写同一个位置，结果就是脏数据，必须自己用信号量或互斥锁圈起来。"
        ),
        "pitfalls": [
            "**管道是字节流，没有消息边界**：写两次 100 字节，读端可能一次读回 200 字节，"
            "也可能分三次读到——想按「一条消息」读，得自己在应用层定长度或分隔符（或者直接用消息队列）。",
            "**管道容量有限（默认 64KB）**：写满后写端阻塞；如果读端已经关闭，写端还会收到 SIGPIPE 信号，"
            "默认动作是进程直接被杀，排查时会看到「程序莫名其妙没了」。",
            "**共享内存最快，但不带同步**：这是面试最爱追问的点。「共享内存为什么快」答「少拷贝」；"
            "「那它会有什么问题」必须答出「要自己配信号量/互斥锁，否则读到脏数据」。",
            "**信号不适合传数据**：它只能带一个整数，语义是「通知有件事发生了」，"
            "而且信号处理函数里能调用的函数非常有限，业务逻辑别写在里面。",
        ],
        "task": "",
        "setup": "",
        "starter": "",
        "hint": "",
        "quizzes": [
            {
                "type": "choice",
                "stem": "下面哪种 IPC 方式的数据拷贝次数最少、吞吐最高？",
                "options": ["共享内存", "匿名管道", "消息队列", "信号"],
                "answer_index": 0,
                "explanation": (
                    "共享内存把同一块物理页映射进两个进程，数据不需要经过内核中转拷贝，所以最快。"
                    "管道和消息队列都要「用户态 → 内核 → 用户态」拷两次。代价是同步要自己做。"
                ),
            },
            {
                "type": "choice",
                "stem": "在 shell 里执行 `ls -l | grep log`，这两个进程用的是哪种 IPC？",
                "options": ["匿名管道", "命名管道 FIFO", "共享内存", "消息队列"],
                "answer_index": 0,
                "explanation": (
                    "`|` 就是 shell 帮你建的匿名管道：内核里的一根缓冲区，ls 写、grep 读。"
                    "匿名管道没有文件名，只靠 fork 继承文件描述符，所以只能用于有亲缘关系的进程。"
                ),
            },
            {
                "type": "judge",
                "stem": "共享内存是进程间通信最快的方式，因为内核已经帮你把同步也做好了，多个进程同时写不会出错。",
                "answer": False,
                "explanation": "快是因为拷贝少，恰恰**不提供任何同步**。必须自己用信号量或互斥锁保护，否则会读到写了一半的脏数据。",
            },
            {
                "type": "blank",
                "stem": "要创建一根有名字、任意无亲缘关系的进程都能打开的管道，使用的命令是 `___ /tmp/myfifo`。",
                "hint": "一个命令名",
                "answer": "mkfifo",
                "accept": ["mkfifo /tmp/myfifo"],
                "explanation": "`mkfifo` 创建命名管道（FIFO）。它比匿名管道多了一个文件系统入口，代价是要自己管理这个文件。",
            },
            {
                "type": "short",
                "stem": "面试官问：两个进程要每秒传递十万条小消息（每条几十字节），你会选哪种 IPC？说明理由和要注意的坑。",
                "keywords": ["共享内存", "拷贝", "同步", "缓冲区", "锁", "无锁环形队列"],
                "reference": (
                    "这种高频小消息场景首选**共享内存**：消息小，但频率极高，瓶颈就在「拷贝次数」和「系统调用次数」上，"
                    "管道和消息队列每条都要经过内核两次拷贝加一次陷入，开销吃不消。做法是在共享内存里放一个环形缓冲区，"
                    "不用时靠信号量或 futex 唤醒，能再进一步就做无锁环形队列。"
                    "坑有两个：一是必须自己做同步（内存屏障、可见性都要考虑），"
                    "二是共享内存段要显式回收，否则进程崩了也一直占着。"
                ),
                "explanation": "这类题的得分点是「先说瓶颈在哪，再选工具，最后说代价」——直接把最快的方法说出来而不讲拷贝次数，说服力差一半。",
            },
        ],
    },
    {
        "code": "os-04",
        "subject": "操作系统",
        "stage": "进程与线程",
        "runner": "none",
        "title": "死锁的四个条件与预防",
        "summary": "四个必要条件，怎么打破循环等待",
        "definition": (
            "**死锁**：一组进程各自持有一些资源，又都在等别人手里的资源，谁都不肯放手，"
            "结果全体永久阻塞。\n\n"
            "死锁的**四个必要条件**（四个同时成立才会死锁，缺一不可）：\n\n"
            "1. **互斥**：资源同一时刻只能被一个进程占用\n"
            "2. **占有并等待**：已经拿到了资源，同时还在申请新资源且不释放手里的\n"
            "3. **不可抢占**：资源只能由持有者主动释放，别人抢不走\n"
            "4. **循环等待**：存在一个进程与资源的环形等待链 P1 → P2 → … → Pn → P1\n\n"
            "处理策略分四类：**预防**（设计上破坏某个条件）、**避免**（运行时用银行家算法判断是否安全）、"
            "**检测与恢复**（允许发生，定期查资源分配图有没有环，有就杀进程/回滚）、**鸵鸟策略**（假装看不见，"
            "大多数通用系统就是这么干的）。\n\n"
            "工程上真正落地的核心手段只有一条：**给所有锁规定全局统一的加锁顺序**，直接破坏循环等待。"
        ),
        "plain": (
            "最经典的画面：**两个人过独木桥，都走到中间了，谁都不肯退**。"
            "或者两辆车在十字路口，A 车要等 B 车先走、B 车要等 A 车先走，谁都动不了。\n\n"
            "把这个画面拆成四个条件，就明白为什么要同时满足才会死锁：\n"
            "桥只能一个人走（互斥）；两个人都已经站在桥上了（占有并等待）；谁也不能把对方推下去（不可抢占）；"
            "而且正好形成一个「你等我、我等你」的圈（循环等待）。\n\n"
            "**只要打破任意一个，死锁就成立不了。** 桥不能改成双向（互斥破不了），"
            "也不能推人下桥（抢占不现实），但我们可以**规定上桥顺序**——"
            "比如「永远先走东边来的车」，那就再也凑不成环了。这就是加锁顺序的由来。"
        ),
        "example": (
            "# 死锁的经典写法：两把锁，加锁顺序相反\n"
            "# 线程 A                    线程 B\n"
            "# lock(mutex1)              lock(mutex2)\n"
            "# lock(mutex2)  <- 等 B     lock(mutex1)  <- 等 A\n"
            "# ...                       ...\n"
            "\n"
            "# 资源分配图（有环 = 死锁）\n"
            "# A 持有 R1  --等待-->  R2 由 B 持有\n"
            "# B 持有 R2  --等待-->  R1 由 A 持有\n"
            "# 方框=资源，圆=进程；A -> R2 -> B -> R1 -> A 形成闭环\n"
            "\n"
            "# 修复：全局规定「先锁编号小的」\n"
            "# 线程 A 和 线程 B 都改成：lock(mutex1) 然后 lock(mutex2)\n"
            "# 谁都拿不到第二把锁时会先释放第一把的等待（配合 trylock + 超时更好）\n"
            "\n"
            "# 线上定位（Java 为例）\n"
            "# jstack <pid> | grep -A 30 'Found one Java-level deadlock'\n"
            "# 输出会直接指出「哪个线程持有哪把锁、在等哪把锁」"
        ),
        "example_output": (
            "说明：\n"
            "1. 只要两个线程的加锁顺序相反，就存在「A 拿 1 等 2、B 拿 2 等 1」的环，"
            "压力小的时候可能几年不出事，并发一上来就卡死——这也是死锁难查的原因。\n"
            "2. 统一加锁顺序之后，等待关系变成单向的：要么 A 先拿到两把锁，要么 B 先拿到，不会成环。\n"
            "3. `jstack` 直接在进程外把环画出来，是排查死锁最快的工具；"
            "没有 jstack 的语言就用 gdb 看线程栈，或者上 pstack / perf。"
        ),
        "pitfalls": [
            "**四个条件必须同时满足**：这是判断题高频点。少任何一个都不会死锁，反过来说，只要破坏一个就能预防。",
            "**「避免」和「预防」不是一回事**：预防是设计阶段破坏条件（静态），"
            "避免是运行中用银行家算法判断这次分配是否会让系统进入不安全状态（动态）；"
            "银行家算法要求预知每个进程的最大资源需求，工程上几乎不用，能说出来就是加分项。",
            "**死锁 ≠ 饥饿**：死锁是「环形等待、永久卡住」，饥饿是「优先级低，一直排不上队但系统还在跑」。"
            "面试官经常用这个来分辨你是不是真的懂。",
            "**破坏互斥通常做不到**：有些资源天生不可共享（打印机、写锁），所以实际手段集中在"
            "「统一加锁顺序」「trylock + 超时退避」「一次性申请全部资源」这三条上。",
        ],
        "task": "",
        "setup": "",
        "starter": "",
        "hint": "",
        "quizzes": [
            {
                "type": "choice",
                "stem": "下面哪一项**不是**死锁的四个必要条件？",
                "options": ["资源可以无限共享", "互斥条件", "占有并等待", "循环等待"],
                "answer_index": 0,
                "explanation": (
                    "四个必要条件是：互斥、占有并等待、不可抢占、循环等待。"
                    "「资源可以无限共享」恰恰是互斥的反面，如果资源真能无限共享，本来就不会有死锁。"
                ),
            },
            {
                "type": "choice",
                "stem": "工程上最常用、代价最小的死锁预防手段是？",
                "options": [
                    "给所有锁规定统一的加锁顺序，破坏循环等待",
                    "用银行家算法在每次分配前做安全性检查",
                    "规定所有进程都不允许同时持有两把锁",
                    "把资源都改造成可以被抢占的",
                ],
                "answer_index": 0,
                "explanation": (
                    "统一加锁顺序只靠编码规范就能落地，不用改内核也不用预知资源需求，破坏的正是最容易破的循环等待。"
                    "B 需要预知最大需求，几乎没人真用；C 限制太死，很多业务做不到；D 对锁这类资源不现实。"
                ),
            },
            {
                "type": "judge",
                "stem": "死锁和饥饿是一回事，都是进程长时间拿不到资源。",
                "answer": False,
                "explanation": "死锁是环形等待导致全体永久卡死；饥饿是调度不公平导致某些进程一直轮不上，但系统整体还在推进。",
            },
            {
                "type": "blank",
                "stem": "四个必要条件里，与「资源本身同一时刻只能被一个进程使用」对应的是 `___` 条件，通常无法破坏。",
                "hint": "填两个字",
                "answer": "互斥",
                "accept": ["互斥条件"],
                "explanation": "互斥由资源本身的物理性质决定（比如写锁、打印机），所以预防手段一般绕开它，去破坏另外三个。",
            },
            {
                "type": "short",
                "stem": "面试官问：线上服务偶发卡死，你怀疑是死锁，怎么确认？确认之后怎么改？",
                "keywords": ["jstack", "线程栈", "BLOCKED", "等待环", "加锁顺序", "超时"],
                "reference": (
                    "先取证：用 `jstack <pid>`（或 pstack / gdb）导出全部线程栈，"
                    "看有没有大量线程停在同一个加锁点上，jstack 还会直接打印 Java-level deadlock 报告，"
                    "把「谁持有 A 等 B、谁持有 B 等 A」画出来。同时用 top -Hp 看是哪个线程在空转还是全部挂起。"
                    "确认是死锁后，改法是统一加锁顺序、缩小锁范围、要加第二把锁时先用 trylock 带超时，"
                    "拿不到就释放手里的锁退避重试，避免永久阻塞。"
                ),
                "explanation": "这题的标准结构是「先复现/取证，再定位环，最后改加锁顺序或加超时」，光说加锁顺序会显得没有排查能力。",
            },
        ],
    },
    # ---------------- stage：内存管理 ----------------
    {
        "code": "os-05",
        "subject": "操作系统",
        "stage": "内存管理",
        "runner": "none",
        "title": "分页、分段与段页式",
        "summary": "地址怎么翻译，碎片从哪来",
        "definition": (
            "**分页 Paging**：把逻辑地址空间切成大小固定的**页 page**（常见 4KB），"
            "物理内存切成同样大小的**页框 frame**，用**页表**记录「第几页 → 第几个页框」的映射。"
            "逻辑地址被硬件拆成「页号 + 页内偏移」两段，翻译过程："
            "页号查页表得页框号，物理地址 = 页框号 × 页大小 + 页内偏移。\n\n"
            "**分段 Segmentation**：按程序的**逻辑意义**切分（代码段、数据段、栈段、堆段），"
            "每段长度不固定，逻辑地址是二维的「段号 + 段内偏移」，段表里存基址和段长，还要做越界检查。\n\n"
            "**段页式**：先按逻辑分段，每段内部再分页——兼得分段的逻辑性和分页的碎片优势，"
            "代价是一次地址翻译要查两次表。\n\n"
            "碎片问题的结论必须记牢：**分页没有外部碎片，但有页内碎片**（一个 1KB 的对象也要占一整个 4KB 页）；"
            "**分段有外部碎片，没有页内碎片**（段本身就是变长的，分配器要为它找足够大的连续空间）。"
        ),
        "plain": (
            "**分页像把书撕成固定大小的页**：不管每章多长，统统裁成 4KB 一张，"
            "装进同样大小的盒子里，盒子随便放，用一张索引表记录「第 3 页在 7 号盒子」。"
            "好处是任何盒子都能装任何页，不会剩下放不下的空档；坏处是最后一页往往没写满，"
            "那半张空白也占一个盒子（**页内碎片**）。\n\n"
            "**分段像按章节装订**：第 1 章 30 页、第 2 章 5 页，各订各的，长短不一。"
            "好处是符合人的理解，方便整段共享和整段保护（代码段设只读，谁也别想改）；"
            "坏处是章节长短不一，搬来搬去容易在书架上留下塞不进去的缝隙（**外部碎片**）。\n\n"
            "**段页式就是两者结合**：先按章节分好，再统一裁成标准大小的页。"
            "所以真实系统（x86-64）就是段页式思路的延续——只不过现代系统把分段弱化了，分页是主力。"
        ),
        "example": (
            "# 例题：页大小 4KB，逻辑地址 0x00003A2F，查表得该页对应页框号 0x2，求物理地址\n"
            "\n"
            "# 第 1 步：拆地址（4KB = 2^12，所以低 12 位是页内偏移）\n"
            "# 0x00003A2F = 0000 0000 0000 0000 0011 1010 0010 1111\n"
            "# 页号     = 高 20 位 = 0x3\n"
            "# 页内偏移 = 低 12 位 = 0xA2F\n"
            "\n"
            "# 第 2 步：查页表：页号 0x3 -> 页框号 0x2\n"
            "\n"
            "# 第 3 步：拼物理地址\n"
            "# 物理地址 = 页框号 << 12 | 页内偏移 = 0x2000 | 0xA2F = 0x2A2F\n"
            "\n"
            "# 注意：页大小是 2 的幂，所以「除以 4KB」就是右移 12 位，「取余」就是 & 0xFFF\n"
            "\n"
            "# 页表项常见标志位（面试常追问这几位）\n"
            "# 有效位 P   该页在不在物理内存里（为 0 就触发缺页中断）\n"
            "# 修改位 D   脏页标记，换出时必须写回磁盘\n"
            "# 访问位 A   最近有没有被访问，给页面置换算法用\n"
            "# 读写/执行位 权限控制，越权访问直接段错误"
        ),
        "example_output": (
            "结果是 **0x2A2F**。把这个过程拆开看，能看出三件事：\n"
            "1. 页号到页框号的映射就是**替换高位**：低 12 位（页内偏移）原封不动，只把高 20 位换成页框号。"
            "所以页大小必须是 2 的幂，这样拆位和拼位都是位运算，硬件能做得非常快。\n"
            "2. 一次访问要**多访问一次内存**去读页表，这就是「页表开销」；"
            "两级页表就是多两次，这也是 TLB 存在的意义。\n"
            "3. 标志位决定了「有效位为 0 → 缺页中断 → 从磁盘换入 → 重试这条指令」这条完整链路。"
        ),
        "pitfalls": [
            "**页大小是 2 的幂，所以用位运算而不是除法**：4KB 对应右移 12 位、低 12 位是偏移。"
            "考试题里如果写成「地址除以 4096」，过程对但会被认为不熟硬件实现。",
            "**页表项不只有页框号**：还有有效位、修改位（dirty）、访问位和权限位。"
            "只答「页表存物理地址」会被追问。",
            "**多级页表省的是内存，不是时间**：它的目的是让未使用的地址区域不用分配页表页面，"
            "顺带让页表可以离散存放，不用一大块连续内存。加快翻译靠的是 TLB 命中。",
            "**别把两种碎片说反**：分页有**页内碎片**（最后一页浪费），分段有**外部碎片**（变长分配留下缝隙）。"
            "这是本节最经典的送分/送命题。",
        ],
        "task": "",
        "setup": "",
        "starter": "",
        "hint": "",
        "quizzes": [
            {
                "type": "choice",
                "stem": "页大小为 4KB，逻辑地址 0x00003A2F 的**页内偏移**是？",
                "options": ["0xA2F", "0x3A2", "0x3", "0x2A2F"],
                "answer_index": 0,
                "explanation": "4KB = 2^12，低 12 位是页内偏移，即 0xA2F；高 20 位是页号 0x3。C 是页号，D 是拼出来的物理地址。",
            },
            {
                "type": "choice",
                "stem": "关于分页和分段，下面哪个说法是正确的？",
                "options": [
                    "分页对用户透明、可能有页内碎片；分段按逻辑划分、可能有外部碎片",
                    "分页有外部碎片，分段有内部碎片",
                    "两者都是按程序的逻辑意义划分的",
                    "分段对用户透明，分页需要程序员显式指定",
                ],
                "answer_index": 0,
                "explanation": (
                    "分页是为了解决内存分配问题，大小固定，用户看不见，浪费在「最后一页没用满」；"
                    "分段是按代码段/数据段这类逻辑单位划分的，长度可变，分配时会产生外部碎片。B 正好说反。"
                ),
            },
            {
                "type": "judge",
                "stem": "引入多级页表的主要目的是加快地址翻译的速度。",
                "answer": False,
                "explanation": (
                    "多级页表是为了**省内存**（未使用的地址区间不需要分配下级页表），"
                    "而且多查一级反而更慢。加快翻译靠的是 TLB 缓存最近用过的页表项。"
                ),
            },
            {
                "type": "blank",
                "stem": "为了减少「每次访存都要先读页表」的开销，CPU 内部用 `___` 来缓存最近使用的页表项（填英文缩写）。",
                "hint": "三个字母的缩写",
                "answer": "TLB",
                "accept": ["tlb", "Translation Lookaside Buffer", "快表"],
                "explanation": "TLB 命中时地址翻译不需要访存；进程切换时要刷新 TLB（或用 ASID 区分），所以切换代价大。",
            },
            {
                "type": "short",
                "stem": "面试官问：页大小为什么通常取 4KB？再大一点或者再小一点会怎样？",
                "keywords": ["页内碎片", "页表大小", "TLB", "缺页", "磁盘 IO", "局部性"],
                "reference": (
                    "这是个权衡。页**大**了：页表项变少、页表更小、TLB 能覆盖的内存范围更大、"
                    "每次缺页换入的数据更多（顺序 IO 友好），但最后一页没用满造成的**页内碎片**会变多，"
                    "小文件和小对象浪费严重。页**小**了：内部碎片少、粒度细，但页表项暴增、页表占的内存变大、"
                    "TLB 覆盖范围缩小、缺页次数也会变多，而每次缺页都要一次磁盘 IO，代价极高。"
                    "4KB 是这两端的经验折中，现代系统也有 2MB/1GB 的大页（huge page）"
                    "专门给大内存应用减少 TLB miss。"
                ),
                "explanation": "答「为什么是某个值」的问题，一律先说它在哪两端之间权衡，再给结论和补丁（比如 huge page）。",
            },
        ],
    },
    {
        "code": "os-06",
        "subject": "操作系统",
        "stage": "内存管理",
        "runner": "none",
        "title": "虚拟内存与页面置换",
        "summary": "缺页、置换算法与抖动",
        "definition": (
            "**虚拟内存**给每个进程一个连续、独立、看起来很大的地址空间，实际上只有正在用的部分留在物理内存里，"
            "其余放在磁盘（swap / 页面文件）。好处有三个：进程之间隔离、可以运行比物理内存大的程序、"
            "可以只加载真正用到的部分（按需调页 demand paging）。\n\n"
            "**缺页中断（page fault）**的完整流程：CPU 访问一个页 → 页表项有效位为 0 → 触发缺页异常 → "
            "内核检查地址合法性 →（不合法就报段错误）→ 找一个空闲页框，没有就从置换算法挑一个受害者 → "
            "若受害者是脏页先写回磁盘 → 从磁盘读入所需页 → 更新页表、刷新 TLB → **重新执行引发缺页的那条指令**。\n\n"
            "**页面置换算法**：\n\n"
            "- **OPT** 最优置换：换掉未来最久不会被用的页，理论最优但无法预知未来，只用来当评测基准\n"
            "- **FIFO** 先进先出：实现最简单，可能出现 **Belady 异常**（页框变多，缺页反而变多）\n"
            "- **LRU** 最近最少使用：换掉最久没被访问的页，是**栈式算法**，不会出现 Belady 异常，但要精确记录访问时间，硬件代价高\n"
            "- **Clock / 二次机会**：用访问位把环形队列转一圈做近似 LRU，是真实系统里 LRU 的廉价替代\n"
            "- **LFU** 最少使用频率：按访问次数淘汰，可能把曾经的热点页长期占着不放"
        ),
        "plain": (
            "**书桌和书架**的比喻最贴切：书桌就是物理内存，只放得下几本正在看的书；"
            "书架是磁盘，所有书都堆在那儿。\n\n"
            "- 要看的内容不在桌上 → **缺页中断**：起身去书架把那本书搬过来\n"
            "- 桌面满了 → **置换**：先腾个位置出来。腾哪本？这就是置换算法要回答的问题\n"
            "- 如果被腾掉的那本是你**自己刚写满了笔记的**（脏页），还得先把笔记抄到书架上再腾（写回磁盘），"
            "成本翻倍；如果只是原封不动搬来的（干净页），直接扔掉就行\n\n"
            "最惨的情况叫**抖动 thrashing**：桌子太小，你每看两行就得去书架换一次书，"
            "搬书的时间远超看书的时间——系统表现出来就是 CPU 没满、磁盘狂转、什么请求都慢。"
            "解法只有一个：**减少同时在桌上摊开的书（降低并发进程数）或者换张大桌子（加内存）**，"
            "调参数是没用的。"
        ),
        "example": (
            "# 引用串：7 0 1 2 0 3 0 4 2 3，物理页框只有 3 个\n"
            "\n"
            "# FIFO（换掉最早进来的）\n"
            "# 7 -> [7]      缺页\n"
            "# 0 -> [7,0]    缺页\n"
            "# 1 -> [7,0,1]  缺页\n"
            "# 2 -> [2,0,1]  缺页，淘汰 7\n"
            "# 0 -> [2,0,1]  命中\n"
            "# 3 -> [2,3,1]  缺页，淘汰 0\n"
            "# 0 -> [2,3,0]  缺页，淘汰 1\n"
            "# 4 -> [4,3,0]  缺页，淘汰 2\n"
            "# 2 -> [4,2,0]  缺页，淘汰 3\n"
            "# 3 -> [4,2,3]  缺页，淘汰 0\n"
            "# 合计：缺页 9 次，命中 1 次\n"
            "\n"
            "# LRU（换掉最久没被访问的）\n"
            "# 7 -> [7]      缺页\n"
            "# 0 -> [7,0]    缺页\n"
            "# 1 -> [7,0,1]  缺页\n"
            "# 2 -> [0,1,2]  缺页，淘汰 7（最久没用）\n"
            "# 0 -> [1,2,0]  命中\n"
            "# 3 -> [2,0,3]  缺页，淘汰 1\n"
            "# 0 -> [2,3,0]  命中\n"
            "# 4 -> [3,0,4]  缺页，淘汰 2\n"
            "# 2 -> [0,4,2]  缺页，淘汰 3\n"
            "# 3 -> [4,2,3]  缺页，淘汰 0\n"
            "# 合计：缺页 8 次，命中 2 次"
        ),
        "example_output": (
            "对比结论：\n"
            "1. **LRU（8 次）优于 FIFO（9 次）**，因为它利用了程序的局部性："
            "刚刚访问过的页很可能马上又用（时间局部性）。\n"
            "2. **FIFO 只有 1 次命中**，因为它只看「谁进来得早」，"
            "完全不关心这页是不是刚刚才用过——一个刚用过的热门页照样会被淘汰掉。\n"
            "3. 面试时把这三句话说出来就够了："
            "「FIFO 实现最简单但命中率差且可能出现 Belady 异常；LRU 命中率接近最优、是栈式算法不会出现 Belady 异常，"
            "但需要精确的访问时间戳，硬件上代价高；所以真实系统用访问位做近似，也就是 Clock 算法。」"
        ),
        "pitfalls": [
            "**Belady 异常只发生在 FIFO 这类非栈式算法上**：LRU、OPT 都是栈式算法，"
            "增大页框数缺页次数只会减少或不变，不会变多。这个对应关系是常考点。",
            "**缺页异常处理完是「重新执行」那条指令，不是接着往下走**：它属于 fault（故障）而不是 trap（陷入），"
            "因为指令根本没有执行成功。",
            "**脏页和干净页的换出代价差一倍**：被修改过的页（dirty）必须先写回磁盘，"
            "干净页直接丢弃即可。所以淘汰时优先挑干净页，这也是 Clock 算法同时看访问位和修改位的原因。",
            "**抖动不是靠调参数能解决的**：它本质是「并发度 × 工作集 > 可用页框数」，"
            "只能降低并发或加内存。看到 CPU 不满、磁盘 100%、响应时间暴涨就是它，别去查 CPU 代码。",
        ],
        "task": "",
        "setup": "",
        "starter": "",
        "hint": "",
        "quizzes": [
            {
                "type": "choice",
                "stem": "下面哪种页面置换算法可能出现 Belady 异常（页框变多，缺页反而变多）？",
                "options": ["FIFO", "LRU", "OPT", "Clock"],
                "answer_index": 0,
                "explanation": (
                    "FIFO 只看进入时间，不看访问情况，不是栈式算法，所以会出现 Belady 异常。"
                    "LRU 和 OPT 是栈式算法，增加页框数缺页次数只会减少或不变。"
                ),
            },
            {
                "type": "choice",
                "stem": "面试官问：一台机器 CPU 使用率不高，但内核态（sy）占用高、磁盘一直忙、请求响应越来越慢，最可能是什么问题？",
                "options": [
                    "内存不足导致抖动（thrashing），CPU 时间耗在缺页处理上",
                    "CPU 主频太低",
                    "网卡丢包导致重传",
                    "某个进程在死循环",
                ],
                "answer_index": 0,
                "explanation": (
                    "CPU 不满 + 磁盘 100% + sy 高，是抖动的典型特征：进程频繁缺页，"
                    "内核时间都花在换入换出上，真正干活的时间很少。如果真是死循环，用户态 CPU 会飙高而不是忙在 sy 和磁盘上。"
                ),
            },
            {
                "type": "judge",
                "stem": "LRU 是栈式置换算法，因此不会出现 Belady 异常。",
                "answer": True,
                "explanation": "栈式算法的性质是「n 个页框命中的页集合，一定是 n+1 个页框命中集合的子集」，所以加页框只会更好或持平。",
            },
            {
                "type": "blank",
                "stem": "缺页中断属于 `___` 类型的中断（不是 trap），处理完成后 CPU 会重新执行引发它的那条指令。",
                "hint": "填两个字，英文是 fault",
                "answer": "故障",
                "accept": ["fault", "异常"],
                "explanation": "缺页属于 fault：指令因为缺页没能执行成功，修好条件后必须重试同一条指令，所以是「重新执行」而不是「继续下一条」。",
            },
            {
                "type": "short",
                "stem": "面试官问：真实操作系统为什么不用严格的 LRU，而用 Clock 近似？",
                "keywords": ["精确时间戳", "硬件代价", "访问位", "环形队列", "近似", "开销"],
                "reference": (
                    "严格 LRU 要求每次访问都更新访问时间戳并对所有页排序，"
                    "这意味着每次访存都要改数据结构、还要加锁，纯软件实现根本跟不上访存频率，"
                    "纯硬件实现又要为每个页配时间戳和比较逻辑，成本太高。"
                    "现实做法是利用 MMU 自带的**访问位**：把页框排成环形队列，指针扫过去，"
                    "访问位是 1 就清零并放过（给它二次机会），是 0 就淘汰。"
                    "这样既不需要排序也不需要时间戳，用极小的开销拿到了接近 LRU 的效果，"
                    "还能顺便看修改位优先淘汰干净页。"
                ),
                "explanation": "这题的得分点是「LRU 精确实现的代价在哪」，能指出「每次访存都要更新」就已经答到点上。",
            },
        ],
    },
    # ---------------- stage：存储与 IO ----------------
    {
        "code": "os-07",
        "subject": "操作系统",
        "stage": "存储与 IO",
        "runner": "none",
        "title": "文件系统与 inode 机制",
        "summary": "inode 存什么，软硬链接差在哪",
        "definition": (
            "文件在磁盘上分两部分存放：**数据块**（文件内容）和 **inode**（索引节点，存元数据）。\n\n"
            "**inode 里存的**：文件类型、权限位、属主/属组、大小、三个时间戳（atime/mtime/ctime）、"
            "链接计数、**数据块的指针**（通常是 12 个直接块 + 1 个一级间接 + 1 个二级间接 + 1 个三级间接）。"
            "**inode 里不存文件名**——文件名放在目录里，目录本质是一张「文件名 → inode 号」的映射表。\n\n"
            "**硬链接 hard link**：在目录里再加一条名字，指向**同一个 inode**，"
            "inode 的链接计数 +1。本质是「一本书贴了两个书名标签」。\n\n"
            "**软链接 / 符号链接 symlink**：一个**独立的文件**，有自己的 inode，内容就是目标路径字符串。"
            "本质是「一张写着『去 XX 找那本书』的便条」。\n\n"
            "**删除的真正语义**：`rm` 只是把链接计数减 1 并删掉那条目录项，"
            "只有链接计数为 0 **并且没有进程还打开着这个文件**，数据块才会真正被释放。"
        ),
        "plain": (
            "把文件想象成图书馆里的一本书：\n\n"
            "- **inode 是索引卡**：记着这本书在几号书架的哪几格、多厚、什么时候还、谁能借。"
            "**卡片上没有书名**。\n"
            "- **目录是书名目录柜**：你查「算法导论」，柜子告诉你「对应的卡片编号是 1024」。"
            "所以同一个文件名可以在不同目录里出现，指向同一张卡片。\n"
            "- **硬链接**：同一张索引卡，被贴了两个书名标签。撕掉一个标签，书还在。"
            "所以 `rm` 一个硬链接，另一个名字照样能读。\n"
            "- **软链接**：卡片上只写了一句话「请去 3 号架找那本书」。"
            "如果那本书被移走了，这张便条就变成了**悬空链接**（dangling link），打开它只会报「No such file」。\n\n"
            "最容易被追问的是这一条：**`rm` 了文件，但磁盘空间没释放**——"
            "因为还有进程拿着这张卡的借阅单（打开的文件描述符）。"
            "图书馆的规定是「标签撕完 + 没人借着，才真正下架」。"
        ),
        "example": (
            "# 1) 看 inode 号与完整元数据\n"
            "ls -i a.txt                    # 只显示 inode 号\n"
            "stat a.txt                     # 大小、权限、链接数、atime/mtime/ctime、Inode\n"
            "\n"
            "# 2) 硬链接：两个名字，同一个 inode\n"
            "ln a.txt b.txt                 # 链接数变成 2\n"
            "ls -i a.txt b.txt              # 两行显示的 inode 号完全相同\n"
            "\n"
            "# 3) 软链接：新 inode，内容是路径字符串\n"
            "ln -s a.txt c.txt              # c.txt -> a.txt\n"
            "ls -l c.txt                    # 显示 l 类型，并且有箭头 c.txt -> a.txt\n"
            "\n"
            "# 4) 删掉原名，看两者的区别\n"
            "rm a.txt\n"
            "cat b.txt                      # 正常输出（硬链接还指向那个 inode）\n"
            "cat c.txt                      # No such file or directory（悬空软链接）\n"
            "\n"
            "# 5) 排查「rm 了文件磁盘还是满的」\n"
            "lsof +L1                       # 列出链接数为 0 但仍被进程打开的文件\n"
            "# 找到 pid 之后重启或 kill 该进程，空间立刻释放\n"
            "# 不想重启服务也可以：> /proc/<pid>/fd/<n>  把文件清空\n"
            "\n"
            "# 6) 排查 inode 用完（磁盘还有空间也建不了新文件）\n"
            "df -h                          # 看块使用率\n"
            "df -i                          # 看 inode 使用率，100% 就写不进新文件了"
        ),
        "example_output": (
            "结果说明：\n"
            "1. `ls -i a.txt b.txt` 两行 inode 号相同，链接计数是 2；删掉 `a.txt` 后 `b.txt` 依然可读，"
            "数据块**没有被释放**。\n"
            "2. `ls -l c.txt` 显示箭头 `c.txt -> a.txt`，`stat c.txt` 的大小是路径字符串的长度（比如 5 字节），"
            "因为它本身就是一个内容为「a.txt」的小文件。\n"
            "3. `lsof +L1` 是排查「磁盘空间不释放」的标准命令：那个 `L1` 就是「链接计数小于 1」的意思，"
            "输出里能看到进程名、pid 和被删文件原本的大小。"
        ),
        "pitfalls": [
            "**硬链接不能跨文件系统、不能指向目录**：它靠 inode 号实现，inode 号只在单个文件系统内唯一；"
            "目录如果允许硬链接，就会让目录树出现环，`find` 直接死循环。",
            "**软链接的相对路径是相对于链接所在目录**，不是相对于你当前所在的目录。"
            "这是「链接明明存在却打不开」的常见原因，排查时用 `ls -l` 看箭头指向、或 `readlink -f` 解析真实路径。",
            "**`rm` 不等于立刻释放磁盘**：只要有进程还持有该文件的 fd，链接计数为 0 也不释放空间。"
            "日志写爆磁盘后 `rm` 了却没效果，就是这个原因，用 `lsof +L1` 找出来。",
            "**inode 数量是格式化时定死的**：可能出现 `df -h` 显示还有空间、但 `df -i` 显示 inode 100%，"
            "此时一样是「No space left on device」。海量小文件的场景必须提前算 inode。",
        ],
        "task": "",
        "setup": "",
        "starter": "",
        "hint": "",
        "quizzes": [
            {
                "type": "choice",
                "stem": "下面哪一项**不会**存放在 inode 里？",
                "options": ["文件名", "文件大小", "权限位", "数据块指针"],
                "answer_index": 0,
                "explanation": (
                    "文件名放在目录项里（目录本质是「文件名 → inode 号」的映射表），"
                    "inode 只存元数据和数据块指针。这也正是硬链接能实现的原因：多个名字指向同一个 inode。"
                ),
            },
            {
                "type": "choice",
                "stem": "面试官问：日志文件已经用 rm 删掉了，但 df 显示磁盘使用率还是 100%，最可能的原因是什么？",
                "options": [
                    "还有进程持有该文件的文件描述符，链接数为 0 但数据块没有释放",
                    "rm 只是把文件移到了回收站",
                    "inode 被用完了",
                    "文件系统损坏，需要 fsck",
                ],
                "answer_index": 0,
                "explanation": (
                    "删除的前提是「链接计数为 0 且无人打开」。进程还开着这个 fd，空间就还占着。"
                    "用 `lsof +L1` 找到持有者，重启/结束进程，或者 `> /proc/<pid>/fd/<n>` 清空文件即可释放。"
                ),
            },
            {
                "type": "judge",
                "stem": "硬链接既可以跨文件系统创建，也可以给目录创建。",
                "answer": False,
                "explanation": "两者都不行。inode 号只在单个文件系统内唯一，所以不能跨文件系统；给目录做硬链接会让目录树成环，所以内核直接禁止。",
            },
            {
                "type": "blank",
                "stem": "除了 `ls -i` 之外，想看一个文件的完整元数据（inode 号、链接数、三个时间戳），可以用 `___` 命令。",
                "hint": "一个命令名，四个字母",
                "answer": "stat",
                "accept": ["stat a.txt", "stat file"],
                "explanation": "`stat` 会输出 Inode、Links、Access/Modify/Change 三个时间和块数量，是排查文件属性问题的首选。",
            },
            {
                "type": "short",
                "stem": "面试官问：硬链接和软链接有什么区别？各适合什么场景？",
                "keywords": ["inode", "链接计数", "独立文件", "跨文件系统", "悬空", "目录"],
                "reference": (
                    "硬链接是在目录里新增一条指向**同一个 inode** 的名字，链接计数 +1，"
                    "删掉任意一个名字数据都还在，但它不能跨文件系统、也不能指向目录。"
                    "软链接是一个**独立的小文件**，内容是目标路径，有自己的 inode，"
                    "可以跨文件系统、可以指向目录，目标不存在时就成了悬空链接。"
                    "适用场景：硬链接适合做同一份数据在同一文件系统内的多个入口（比如备份时的快照式引用）；"
                    "软链接适合做版本切换（`current -> v2.3`）、跨文件系统的快捷方式，"
                    "因为它能随时改指向，代价是多一次路径解析。"
                ),
                "explanation": "把「链接计数」这个词说出来，基本就能证明你真理解硬链接了；再补一句「软链接要多一次路径解析」更完整。",
            },
        ],
    },
    {
        "code": "os-08",
        "subject": "操作系统",
        "stage": "存储与 IO",
        "runner": "none",
        "title": "五种 IO 模型详解",
        "summary": "从阻塞到异步，以及 epoll 的本质",
        "definition": (
            "一次网络 IO 其实是**两段**：① 等数据到达内核缓冲区（wait for data）；"
            "② 把数据从内核缓冲区拷贝到用户空间（copy to user）。五种模型的差别，全在这两段怎么处理。\n\n"
            "- **阻塞 IO**：两段都阻塞，线程从调用 read 开始一直睡到数据拷完。"
            "最省事，但一个线程只能盯一个连接。\n"
            "- **非阻塞 IO**：第一段不阻塞，数据没到就立刻返回 `EWOULDBLOCK`，需要**自己循环重试（轮询）**。"
            "CPU 空转，实际很少单独用，通常配合多路复用。\n"
            "- **IO 多路复用**：`select/poll/epoll` 一个线程同时监听**多个** fd，"
            "内核告诉你「哪些 fd 可读了」，然后你自己再去 read。第一段由内核帮你等，**第二段仍然阻塞**。\n"
            "- **信号驱动 IO**：注册 SIGIO，数据就绪时内核发信号通知你，然后你再去 read。第二段仍然阻塞。\n"
            "- **异步 IO（AIO / io_uring）**：`io_submit` 提交后立刻返回，"
            "内核把数据**连拷贝都做完**才通知你，两段都不需要你参与。\n\n"
            "**关键结论**：前四种都属于**同步 IO**（因为第二阶段都要你自己参与数据拷贝），"
            "只有最后一种是真正的异步 IO。这是面试的必答点。\n\n"
            "**select / poll / epoll 的本质区别**：\n\n"
            "- `select`：fd 集合是位图，**上限 1024（FD_SETSIZE）**；每次调用都要把整个集合从用户态拷到内核态；"
            "返回后还得 **O(n) 遍历**找哪几个就绪；每次调用都要重建集合。\n"
            "- `poll`：用数组 + `revents` 解决 1024 上限，但**拷贝和 O(n) 遍历两个问题依旧存在**。\n"
            "- `epoll`：`epoll_ctl` 注册时**只拷贝一次**，内核用**红黑树**维护所有被监听的 fd，"
            "用**就绪链表**记录已经就绪的 fd，`epoll_wait` 直接返回就绪链表，复杂度 **O(1)**（准确说是 O(k)，k 是就绪数）。"
            "代价是只能用于 Linux，而且 fd 少时和 select 差别不大。\n\n"
            "**LT 与 ET**：LT（水平触发）只要缓冲区还有数据就每次 `epoll_wait` 都通知你，是**默认模式**，"
            "编程简单、不怕漏读；ET（边沿触发）只在状态**变化**时通知一次，必须配**非阻塞 fd** 并且"
            "**循环读到返回 EAGAIN**，否则剩下的数据就丢了。"
        ),
        "plain": (
            "用**收快递**来理解最直观。你住在一个小区，快递到了要到门口取。\n\n"
            "- **阻塞 IO**：你站在门口一直等，快递不来你就不走。什么都不干得了——但你就一个人，只等一个包裹。\n"
            "- **非阻塞 IO**：你每 5 分钟下楼瞄一眼，没到就回去。虽然没白等，但来回跑的腿儿（CPU）全浪费了。\n"
            "- **IO 多路复用**：门口装了门铃（epoll），你可以在屋里干活，门铃一响就知道有包裹到了。"
            "区别在通知方式：`select` 的物业每次敲门都把**整栋楼所有房间号**念一遍让你看哪个有包裹（O(n)）；"
            "`epoll` 的物业只念**真的有包裹的那几户**（就绪链表，O(1)）。\n"
            "- **信号驱动**：物业到了直接打电话给你。\n"
            "- **异步 IO**：你直接叫物业代收并送到你桌上，送到了再喊你一声。**你全程没出过门**——"
            "这才是真异步。前面几种，你最后都还得自己下楼把包裹搬上来（第二阶段的数据拷贝）。"
        ),
        "example": (
            "# 对比表：一次 IO 的两个阶段分别怎么处理\n"
            "# 模型              等数据阶段          拷数据阶段        是否同步IO\n"
            "# 阻塞 IO           阻塞               阻塞              是\n"
            "# 非阻塞 IO         轮询，不阻塞        阻塞              是\n"
            "# IO 多路复用       内核帮你等         阻塞              是\n"
            "# 信号驱动          信号通知，不阻塞    阻塞              是\n"
            "# 异步 IO           不阻塞             内核帮你拷，不阻塞  否（真异步）\n"
            "\n"
            "# select / poll / epoll 本质区别\n"
            "# 维度             select            poll              epoll\n"
            "# fd 上限          1024             无上限            无上限\n"
            "# 用户态到内核态   每次调用全量拷贝   每次调用全量拷贝   只在 ctl 时拷一次\n"
            "# 找就绪 fd        O(n) 遍历集合     O(n) 遍历数组     就绪链表，O(k)\n"
            "# 内核数据结构     位图              数组              红黑树 + 就绪链表\n"
            "\n"
            "# epoll 的典型用法（骨架）\n"
            "ep = epoll_create1(0)                       # 建一个 epoll 实例\n"
            "epoll_ctl(ep, EPOLL_CTL_ADD, fd, &ev)       # 注册 fd，只用注册一次\n"
            "while True:\n"
            "    n = epoll_wait(ep, events, MAX, -1)     # 只返回就绪的 fd\n"
            "    for i in range(n):\n"
            "        handle(events[i].data.fd)           # 注意：这里要循环 read 到 EAGAIN（ET 模式）\n"
            "\n"
            "# 怎么看出一个服务用的是不是 epoll\n"
            "strace -f -e trace=epoll_wait,epoll_ctl -p <pid>"
        ),
        "example_output": (
            "解读：\n"
            "1. 表中第三列「拷数据阶段」全是「阻塞」，只有一个例外——这就是判断同步/异步的唯一标准："
            "**第二阶段要不要你自己参与**。所以 IO 多路复用虽然能同时处理上万连接，它依然是**同步 IO**，"
            "面试时说「epoll 是异步 IO」是直接出局的错误。\n"
            "2. 三者的性能差异不在「谁更快」，而在**开销随 fd 数量增长的方式**："
            "select/poll 是每次调用 O(n)，fd 越多越慢；epoll 是每次只处理就绪的 k 个，"
            "所以「十万连接、同时活跃的只有几百个」这种场景 epoll 碾压 select。\n"
            "3. ET 模式必须配非阻塞 fd 并循环读到 EAGAIN："
            "因为内核只在状态变化时通知一次，你这次没把数据读干净，它就不会再提醒你了。"
        ),
        "pitfalls": [
            "**「epoll 是异步 IO」是高频错误答案**：epoll 只解决了「等数据」这一阶段，"
            "数据就绪后还是要你调用 read 去拷贝，所以它属于同步 IO。真正异步只有 AIO / io_uring。",
            "**ET 模式不循环读到 EAGAIN 就会丢数据**：这是 ET 最经典的线上事故，"
            "所以工程建议是「默认用 LT，除非你明确知道自己在优化什么」。",
            "**「epoll 一定比 select 快」要分场景**：fd 数量少（几十个）时两者差不多，"
            "甚至 select 因为实现简单还更快；epoll 的优势体现在 fd 多、且活跃比例低的场景。"
            "如果十万个连接几乎全部时刻活跃，多路复用本身就成了负担，反而要多线程/多进程分流。",
            "**fd 要设成非阻塞**：多路复用的标准写法是「epoll 通知 → read 循环直到 EAGAIN」，"
            "如果 fd 是阻塞的，最后一次 read 会永远卡住整个 event loop，前面所有的优化白做。",
        ],
        "task": "",
        "setup": "",
        "starter": "",
        "hint": "",
        "quizzes": [
            {
                "type": "choice",
                "stem": "五种 IO 模型里，真正属于「异步 IO」的是哪一个？",
                "options": [
                    "异步 IO（AIO / io_uring）",
                    "IO 多路复用（select / poll / epoll）",
                    "信号驱动 IO",
                    "非阻塞 IO",
                ],
                "answer_index": 0,
                "explanation": (
                    "判断标准只有一个：**数据从内核拷到用户空间这一步要不要你自己做**。"
                    "前三种都还要自己调 read 去拷贝，属于同步 IO；只有 AIO/io_uring 连拷贝都由内核完成。"
                ),
            },
            {
                "type": "choice",
                "stem": "epoll 比 select 高效，本质原因是什么？",
                "options": [
                    "内核用红黑树维护被监听的 fd、用就绪链表返回结果，只在注册时拷贝一次，不需要每次 O(n) 遍历",
                    "epoll 使用了更快的网络协议栈",
                    "epoll 把数据拷贝那一步也省掉了",
                    "epoll 只支持 TCP，所以路径更短",
                ],
                "answer_index": 0,
                "explanation": (
                    "三处改进：① 注册时只拷贝一次，不用每次调用都全量 copy；"
                    "② 内核用红黑树管理 fd，增删是 O(log n)；③ 用就绪链表直接返回就绪的 fd，避免用户态 O(n) 遍历。"
                    "C 是错的，数据拷贝照样要做。"
                ),
            },
            {
                "type": "judge",
                "stem": "非阻塞 IO 的 read 调用在任何情况下都不会让线程阻塞。",
                "answer": False,
                "explanation": (
                    "非阻塞只影响「等数据」这一阶段。如果数据已经就绪，read 在把数据从内核拷到用户空间时依然会阻塞；"
                    "而且拿不到数据时要自己轮询，CPU 空转的成本也不小。"
                ),
            },
            {
                "type": "blank",
                "stem": "epoll 有两种触发模式，其中编程更简单、不怕漏读事件、也是默认模式的是 `___` 触发（填英文缩写）。",
                "hint": "两个字母的缩写",
                "answer": "LT",
                "accept": ["lt", "水平触发", "level trigger", "level-triggered"],
                "explanation": "LT 只要缓冲区还有数据就继续通知，不怕漏读；ET 只在状态变化时通知一次，必须循环读到 EAGAIN。",
            },
            {
                "type": "short",
                "stem": "面试官问：一个要扛十万长连接的网关，为什么用 epoll 而不是给每个连接开一个线程？",
                "keywords": ["线程开销", "上下文切换", "内存", "就绪链表", "C10K", "活跃比例"],
                "reference": (
                    "核心矛盾是**资源开销**和**活跃比例**。每个线程至少占 8MB 左右虚拟栈加内核栈、"
                    "task_struct 等结构，十万线程光内存就撑不住，而且内核调度十万个线程的上下文切换开销"
                    "会把 CPU 吃干净。而十万长连接里，同一时刻真正有数据来往的往往只有几百到几千个，"
                    "绝大多数时间在等待。epoll 用一个线程把「等」这件事交给内核的红黑树和就绪链表，"
                    "只有真的就绪了才回调处理，复杂度只跟**就绪数**相关，不跟连接总数相关。"
                    "这就是经典的 C10K 思路：把「等待」从线程里拿出来。"
                ),
                "explanation": "答题结构：先说每连接一线程的开销（内存 + 切换），再说活跃比例低这个事实，最后落到 epoll 的 O(就绪数) 上。",
            },
        ],
    },
    {
        "code": "os-09",
        "subject": "操作系统",
        "stage": "存储与 IO",
        "runner": "none",
        "title": "Linux 高频排查命令",
        "summary": "CPU、内存、IO 排查套路",
        "definition": (
            "线上排查的原则是**从粗到细，逐层收敛**：先看全局指标，再定位到进程，再定位到线程，最后看栈和日志。\n\n"
            "**第一层 · 全局**：`uptime` 看 load average（要和核数比）、`top` 看整体 CPU 和内存、"
            "`vmstat 1` 看上下文切换和换页、`iostat -x 1` 看磁盘、`free -h` 看内存、`df -h / df -i` 看磁盘和 inode。\n\n"
            "**第二层 · 定位进程**：`top` 里按 `P` 按 CPU 排、按 `M` 按内存排；"
            "`pidstat -u -p <pid> 1` 单进程持续观察。\n\n"
            "**第三层 · 定位线程**：多线程程序只看到进程是不够的——"
            "`top -Hp <pid>` 按线程粒度列出 CPU 占用，拿到线程号（TID）后转成十六进制，"
            "再去 `jstack` / `gdb` 的线程栈里按 `nid` 找对应的调用栈。\n\n"
            "**第四层 · 看内核日志和现场**：`dmesg -T | tail` 看 OOM Killer、磁盘错误；"
            "`ss -lntp` 看端口和连接；`lsof -p <pid>` 看进程打开的文件和网络连接。\n\n"
            "四个方向的典型信号：**CPU 高看谁在烧**、**load 高但 CPU 低看是不是卡在 IO（D 状态）**、"
            "**内存看 available 而不是 free**、**磁盘看 %util 和 await**。"
        ),
        "plain": (
            "排查就像**去医院看病**，不可能一上来就做全身 CT：\n\n"
            "1. **分诊台量体温**（`uptime` / `top`）：先知道整体是发烧还是正常，负载高不高。\n"
            "2. **挂号到具体科室**（定位到进程）：是内科（CPU）的问题还是消化科（磁盘）的问题？"
            "`top` 按 P 排序，一眼看出是哪个进程在闹。\n"
            "3. **做专项检查**（定位到线程和栈）：一个进程里有几十个线程，"
            "`top -Hp` 就是「把这个病人细分到具体器官」，拿到线程号再去看它的调用栈，"
            "才知道它到底卡在哪一行代码上。\n"
            "4. **翻病历**（`dmesg` / 应用日志）：很多时候答案早就写在日志里了，"
            "比如 OOM Killer 杀进程时一定会留一条记录。\n\n"
            "最忌讳的是**跳过前两步直接猜**：看到慢就重启、看到内存高就加内存，"
            "结果下次同样的问题照旧发生。"
        ),
        "example": (
            "# 1) 先看整体负载（load average 要除以核数看）\n"
            "uptime                          # load average: 1 / 5 / 15 分钟\n"
            "nproc                           # 核数，load 超过核数就说明有排队\n"
            "\n"
            "# 2) CPU 飙高，定位到线程（最高频的面试场景）\n"
            "top                             # 按 P 按 CPU 排序、按 M 按内存排序、按 1 展开每个核\n"
            "top -Hp 12345                   # 按线程看 CPU，找出吃 CPU 的线程（TID）\n"
            "printf '%x\\n' 12346             # 线程号转十六进制，去 jstack 里找对应栈\n"
            "jstack 12345 | grep -A 30 'nid=0x303a'   # Java：看这个线程在干什么\n"
            "\n"
            "# 3) 内存：看 available 而不是 free\n"
            "free -h                         # available 才是「真正还能用」的量\n"
            "cat /proc/12345/status | grep VmRSS      # 单进程实际占用的物理内存\n"
            "dmesg -T | grep -i 'killed process'      # 有没有被 OOM Killer 干掉\n"
            "\n"
            "# 4) 磁盘 IO：%util 接近 100% 就是打满了\n"
            "iostat -x 1                     # 看 %util、await、r/s、w/s\n"
            "iotop -o                        # 只看真正在做 IO 的进程\n"
            "df -h && df -i                  # 空间和 inode 都要看\n"
            "\n"
            "# 5) 网络：ss 比 netstat 快得多\n"
            "ss -lntp                        # 监听端口 + 对应进程\n"
            "ss -s                           # 连接数总览（TIME_WAIT 多不多）\n"
            "\n"
            "# 6) 进程在等什么：看内核栈\n"
            "cat /proc/12345/stack           # 需要 root；D 状态的进程会在这里显示卡在哪个 IO"
        ),
        "example_output": (
            "怎么读这些输出：\n"
            "1. `uptime` 的 load average 是 1/5/15 分钟的**平均值**，要和 `nproc` 的核数比："
            "4 核机器的 load 是 8，说明平均有 4 个任务在排队。\n"
            "2. `top -Hp 12345` 输出的 `PID` 列其实是 TID。拿到 12346 后 `printf '%x\\n' 12346` 得到 `303a`，"
            "去 `jstack` 输出里搜 `nid=0x303a`，就能看到那一瞬间这个线程的完整 Java 调用栈——"
            "**这是「CPU 100% 怎么定位」这道题的标准答案链路**。\n"
            "3. `free -h` 里 `available` 才是能用的内存，`buff/cache` 是可回收的，"
            "只看 `free` 那一列会误判成「内存不够」。\n"
            "4. `iostat -x` 里 `%util` 接近 100% 表示设备已经打满；`await` 是平均完成一次 IO 的耗时（毫秒），"
            "它突然从 1ms 涨到 50ms 就说明磁盘撑不住了。\n"
            "5. `dmesg -T` 里出现 `Killed process 12345 (java)` 就是被 OOM Killer 杀了，"
            "这类「进程莫名其妙消失」的案子基本都在这里破。"
        ),
        "pitfalls": [
            "**只看 free 不看 available**：`buff/cache` 是文件缓存，内存紧张时内核会自动回收，"
            "它算作可用。把 `free` 那一列当成可用内存，会得出「内存不足」的错误结论。",
            "**以为 load average 高就是 CPU 忙**：load 统计的是「可运行 + 不可中断」的进程数，"
            "大量进程卡在 D 状态（等磁盘 IO、等 NFS）一样会把 load 顶上去，而这时 CPU 使用率可能很低。"
            "看到 load 高 CPU 低，要去查 IO。",
            "**只 top 看进程就下结论**：多线程服务里进程粒度的 CPU 是几十个线程的平均值，"
            "几百个线程里一个烧到 100% 也可能被摊平看不出来。必须 `top -Hp` 下钻到线程粒度。",
            "**排查完不保留现场**：直接重启服务虽然能恢复，但证据没了，下次还会复现。"
            "正确做法是先把 `top`、`jstack`、`dmesg`、应用日志的输出重定向到文件，再决定动不动它。",
        ],
        "task": "",
        "setup": "",
        "starter": "",
        "hint": "",
        "quizzes": [
            {
                "type": "choice",
                "stem": "面试官问：线上某进程 CPU 飙到 100%，你怎么定位是哪个线程、哪段代码出了问题？",
                "options": [
                    "top 找到进程 pid → top -Hp pid 找出占用高的线程 tid → printf '%x' 转十六进制 → 到 jstack（或 gdb/pstack）里按 nid 找线程栈",
                    "直接重启服务，观察是否恢复",
                    "用 df -h 看是不是磁盘满了",
                    "用 free -h 看是不是内存不足",
                ],
                "answer_index": 0,
                "explanation": (
                    "这是标准链路：**进程 → 线程 → 十六进制线程号 → 线程栈**。"
                    "B 只是掩盖现象；C、D 排查的是别的方向。能完整说出这条链路，这题就答满了。"
                ),
            },
            {
                "type": "choice",
                "stem": "一台机器 load average 很高，但 CPU 使用率只有 15%，最可能的原因是什么？",
                "options": [
                    "大量进程处于不可中断的 D 状态，卡在磁盘 IO 或网络文件系统上",
                    "CPU 主频被降频了",
                    "内存溢出导致进程被 OOM Killer 杀掉",
                    "网卡丢包需要重传",
                ],
                "answer_index": 0,
                "explanation": (
                    "load average 统计的是「可运行 + 不可中断」的进程数。D 状态的进程在等 IO，"
                    "它们不消耗 CPU 但会算进 load，所以会出现「load 爆表、CPU 很闲」这种经典组合。"
                    "这时该去看 `iostat -x` 和 `/proc/<pid>/stack`。"
                ),
            },
            {
                "type": "judge",
                "stem": "free -h 显示 free 那一列很小，就说明内存不够用了，应该立刻加内存。",
                "answer": False,
                "explanation": (
                    "要看的列是 `available`。`buff/cache` 里的文件缓存会在内存紧张时被内核回收，"
                    "Linux 的设计本来就是「空闲内存拿来做缓存，别浪费」，所以 free 小是正常的。"
                ),
            },
            {
                "type": "blank",
                "stem": "想查看每块磁盘的 `%util`、`await` 等详细指标，用 `___ -x 1` 命令（提示：IO statistics）。",
                "hint": "一个命令名",
                "answer": "iostat",
                "accept": ["iostat -x 1"],
                "explanation": "`iostat -x 1` 每秒刷新一次扩展磁盘统计：%util 看是否打满，await 看单次 IO 平均耗时，非常直接。",
            },
            {
                "type": "short",
                "stem": "面试官问：一个接口的 P99 响应时间突然从 50ms 涨到 2s，服务本身没改过代码，你会按什么顺序排查？",
                "keywords": ["分层", "先看全局", "CPU", "内存", "磁盘", "依赖"],
                "reference": (
                    "先从全局往细里收。第一步看机器层：`uptime` 看 load、`top` 看 CPU 和哪个进程在忙、"
                    "`free -h` 看 available、`iostat -x 1` 看磁盘、`ss -s` 看连接数和 TIME_WAIT，"
                    "先判断是资源不够还是自己在等别人。第二步看应用层：线程栈（jstack/pstack）看是不是都在等锁和 DB，"
                    "GC 日志看有没有长停顿，慢查询日志看下游 DB。"
                    "第三步看依赖：DB、缓存、下游 HTTP 服务的耗时有没有变化。"
                    "整个过程先保留现场（输出重定向到文件）再动手，避免重启丢失证据。"
                ),
                "explanation": (
                    "答题要点是「有层次、有顺序」。上来就说「重启看看」或者只盯一个方向（比如只查 CPU），"
                    "是这题最主要的失分点。把「先全局后局部、先机器后应用、最后看依赖」这条线说出来，"
                    "再补一句「先留现场再动手」，这道题就完整了。"
                ),
            },
        ],
    },
]